# アクセスログのノイズ源(UAベースの簡易フィルタをすり抜けるもの)

`scripts/check-access.sh`が除外する、User-Agent文字列だけでは見分けられないIP一覧。
新しいIPを追記するときは、なぜノイズと判断したか(挙動)と発見日を書く。

## BOT_REGEXの見落とし(2026-09-22発見・修正)

`GoogleOther`(GoogleのUA。`Googlebot`という文字列を含まないため、それまでの`BOT_REGEX`では
除外できていなかった)からのアクセスが、192.178.4.x台のIPから2026-09-21・22の2日間だけで
1,600件超あり、「既知のノイズ除外後」の数字のほとんどを占めていた(2026-09-22時点)。
`scripts/check-access.sh`と本番VMの`/usr/local/bin/clash-royale-access-stats.sh`双方の
`BOT_REGEX`に`GoogleOther`・`Google-Extended`を追加して修正済み。IPアドレス自体は
Google側の割り当てで変わり得るため、`known_noise.md`にIPとして追記するのではなくUA文字列側で
除外する方針とした。

## 既知のスキャナー(Chrome偽装・巡回パターンから判断、2026-09-20発見)

- `158.69.55.82`
- `158.69.117.45`
- `158.69.55.148`(2026-09-21発見。同一パターン。存在しなさそうなランダムなプレイヤータグを
  数秒おきに連打しつつ`/favorites`・`/cards`・`/ranking`・`/ranking/players`も44秒間で一通り
  巡回)

いずれも本物のChromeそっくりのUser-Agentを名乗りつつ、`/player/`・`/cards`・`/ranking`・
`/ranking/players`・`/favorites`など全ページ種別を数秒間隔で機械的に巡回する。人間の閲覧速度・
遷移パターンとして不自然なため、スキャナーと判断した。

## 判断材料は薄いが除外対象としたIP(2026-09-21発見)

- `173.249.217.7`(`/favorites`への単発アクセスのみ。ユーザー本人に確認したところ心当たりなし。
  巡回パターン等の明確な自動化の証跡はないが、ユーザーの申告により実訪問者ではないと判断)

## 既知の脆弱性スキャナー(2026-09-20発見)

- `204.76.203.31`

`/.git/config`・`/eureka/apps`・`/management/env`や、AWS/GCPのメタデータエンドポイント
(`169.254.169.254`・`metadata.google.internal`)への到達をクエリパラメータ経由で試みる
(SSRF脆弱性を狙った典型的な探索パターン)。

## 既知の脆弱性スキャナー(2026-09-21発見)

- `87.192.253.110`
- `144.172.105.41`
- `31.132.90.3`
- `47.236.93.38`
- `103.63.101.24`(2026-09-22発見。同一パターン)

`libredtail-http`というUAを名乗り、`/cgi-bin/.%2e/.%2e/...`(パストラバーサルでshell実行を試みる)、
`auto_prepend_file=php://input`(PHPのRFI脆弱性を狙う)など、複数のPHP脆弱性を短時間で機械的に
試すリクエストを送っている。UAが「Chrome偽装」ではなくツール名そのままのため、ブラウザ偽装型より
判別は容易(2026-09-21、当日のアクセス数の大半(49件ずつ)を占めていた)。

## データセンター由来の一括除外(2026-09-22導入)

IPのCIDR一覧を手で持つ方式はやめ、`check-access.sh`が**逆引き(rDNS)のホスト名**で機械的に
判定する。ホスティング事業者は自分のIPにその事業者と分かるホスト名を付けるため、
割り当てが変わっても追随できる(CIDRを追いかけるより変化に強い)。判定は3種類で、
どれに当たったかは実行時に件数と内訳が出る。

1. **逆引きがホスティング事業者・クラウド**(`DC_REGEX`): `ovh`・`your-server.de`(Hetzner)・
   `amazonaws`・`linodeusercontent`・`contaboserver`・`googleusercontent`・`hwclouds-dns.com`(Huawei Cloud)・`pfcloud.network`・`vps`・`v22025…`のような
   VPSの自動採番ホスト名など。
2. **逆引きが素性を名乗るスキャナー**(`SCANNER_REGEX`): `censys-scanner.com`・
   `internet-measurement.com`・`scan.bufferover.run`・`security.criminalip.com`・
   `internet-census.org`・`infrawat.ch`・`deepfield.net`・`googlebot.com`・
   `no-reverse-dns-configured.com`など。**UAはChromeを偽装していても逆引きは自分のドメインのまま**
   なので、ここで捕まえられる。
3. **逆引きが引けない**: 一般のISP(固定回線・モバイルとも)はほぼ必ずPTRを設定しているため、
   逆引きなしはデータセンター・スキャナーの強い目印。ただし断定はできないので、上の2つとは
   分けて数えている。`x.y.z.in-addr.arpa.`のような事業者の手がかりが無いPTRも同じ扱い。

## t.co経由を装う分散スキャナー群(2026-09-22発見)

2026-09-22に`?from=x`付きのアクセスが26件あり、一見「Xからの流入が来ている」ように見えたが、
実際にはほぼ全てが自動巡回だった。判別の決め手は次の3つ。

- 送信元が**すべてデータセンター**(OVH・Hetzner・Contabo・Linode等)。家庭回線・モバイルのIPが皆無。
- **多数の別IPが揃って同じ古いUA `Chrome/103.0.0.0`**(2022年頃のバージョン)を名乗り、OS部分だけ
  `Mac OS X 10_14` `11_2` `12_5`…とバラバラ。実在ユーザーの分布としては起こり得ない。
  `check-access.sh`はこのUAを`STALE_UA`として除外対象にしている。
- **同一URLに数秒差で別IPが2回ずつ**到達する(例: `/cards` に 03:21:04 が2件)。人間の閲覧ではなく、
  リンクを機械的に展開している動き。

X(t.co)自身のURL安全性チェックと、それに便乗した第三者のリンククローラーが混ざったものと見られる。
**`?from=x`の件数をそのまま宣伝効果として読まないこと**。同日に実ブラウザ計測(ビーコン)を
導入したのはこのため(`進捗ログ.md`のフェーズ1.61)。

なお同日、この除外を通しても残った**本物の訪問**が1件あった:
`60.56.133.9`(`eonet.ne.jp`=関西の固定回線)から`Twitter for iPhone`のアプリ内ブラウザで
`/player/VU9GCGUV9?lang=ja&from=xreply`。**Xのリプライ宣伝からの実訪問者が確認できた初の例**。

## ユーザー本人の開発用IP

- `60.113.8.212`(2026-09-20時点。ユーザー本人が2026-09-20に「これは自分のIP」と申告)
- **ユーザー本人のスマホはワイモバイル**(2026-09-23に申告)。逆引きはソフトバンク系のホスト名になり、
  ドコモの`spmode.ne.jp`にはならない。モバイル回線のIPは毎回変わり他人にも割り当てられるため、
  IPでの除外リストには入れない。ソフトバンク系の回線から来た訪問は「本人の可能性あり」と添えて報告する。
  - 2026-09-22 20:05 JSTの`49.109.99.228`(`sp49-109-99-228.smd02.spmode.ne.jp`=ドコモ、
    `from=xreply`経由で2分ほど回遊、ビーコン9件)は、ユーザーが一度「多分私」と答えたが、
    回線がドコモなので**本人ではなく他の人の実訪問と判断**した。

**注意**: ローカル開発機のグローバルIPは固定でない可能性がある(`CLAUDE.md`の
「Clash Royale API連携に関する注意点」参照。IPが変わるとAPIキーの許可リストも更新が必要になる)。
このIPが変わった場合はここも更新が必要。長時間・広範囲にサイトを巡回している単一IPを見つけても、
自動的にスキャナー扱いにせず、ユーザー本人のアクセスである可能性を先に確認すること
(2026-09-20にこのIPをスキャナーと誤判定した経緯があるため)。

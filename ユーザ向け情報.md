# ユーザ向け情報

本番環境の中身を自分で確認するための手順とコマンド集(2026-09-18作成)。

## 0. サイトへのアクセス

- ドメイン名: `https://princess-tower.duckdns.org/`(DuckDNSの無料サブドメイン。**2026-09-21に商標問題解消のため旧`clashroyale-api.duckdns.org`から改名**、詳細は`進捗ログ.md`のフェーズ1.50)。IP直打ち(`http://161.33.136.175/`)でも引き続きアクセスできる。
- DuckDNSはドメイン名を本番VMのIPアドレスに変換するだけの無料サービスで、本番VMの固定IP(`161.33.136.175`)を指すよう設定済み。VMのIPが今後変わった場合は、[duckdns.org](https://www.duckdns.org)にログインして「current ip」欄を新しいIPに更新する必要がある(自動更新されないため手動)。**サブドメインの削除ボタンには注意**(2026-09-21に旧ドメインを誤って削除した経緯がある)。
- HTTPS化済み(`https://`でアクセス可能、`http://`は自動で`https://`にリダイレクトされる)。

## 1. 本番環境にSSHで入る(Tera Term)

| 項目 | 値 |
|---|---|
| ホスト | `161.33.136.175` |
| ポート | `22`(SSH2) |
| ユーザー名 | `opc` |
| 認証方式 | 公開鍵(「RSA/DSA/ECDSA/ED25519鍵を使う」) |
| 秘密鍵ファイル | `C:\Users\Norio Fukuchi\.ssh\oci_clash_royale_api` |
| パスフレーズ | なし(空欄のまま) |

手順:

1. Tera Termを起動し、「新しい接続」で「TCP/IP」を選ぶ。ホストに`161.33.136.175`、サービスは「SSH」、SSHバージョンは「SSH2」にして「OK」。
2. 初回だけ「既知のホストリストにない」旨の警告が出るので、「続行」を押す(次回からは出ない)。
3. 「SSH認証」画面でユーザー名に`opc`を入力し、「RSA/DSA/ECDSA/ED25519鍵を使う」を選んで「秘密鍵」ボタンから上記のファイルを指定する。ファイル選択画面では、種類を「すべてのファイル(\*.\*)」にしないと拡張子の無い鍵ファイルが表示されない。
4. 「OK」でログインできる。終わるときは`exit`。

- 鍵はED25519形式(OpenSSHの新しい書式)なので、Tera Term 4.105以降または5系が必要。古い版では鍵を読み込めない。
- Tera Termを使わない場合は、PowerShellから次の1行でも入れる。

  ```powershell
  ssh -i "$env:USERPROFILE\.ssh\oci_clash_royale_api" opc@161.33.136.175
  ```

- 旧VM(1GBメモリ)ではつながらなくなることがあったが、2026-09-20に現行VM(A1.Flex、1 OCPU・約5.5GBメモリ)へ乗り換えてからは発生していない。それでも数分待って入れなければ、OCIコンソールからVMを再起動する。

## 2. 蓄積しているプレイヤー名のデータ

### 置き場所と形式

- 本番: `/var/lib/clash-royale-api/player-index/inbox/`
- ローカルで`mvn spring-boot:run`したとき: プロジェクト直下の`data/player-index/inbox/`(Git管理外)
- ファイルは1時間ごとに分かれる。ファイル名は`yyyyMMdd-HH.tsv`で、時刻は**UTC**(日本時間から9時間引いた値。例: `20260918-10.tsv`は日本時間の9/18 19時台)。
- 中身はタブ区切りのテキストで、1行が1人分。

  ```
  #208R8PQJP9	AK Tanjiro	2026-09-18T10:50:03Z
  ```

  左から、プレイヤータグ、プレイヤー名、確認日時(UTC)。

- 記録されるタイミングは、誰かがサイトでプレイヤー情報・クラン情報・個人ランキングを表示したとき(詳細は`プレイヤー名検索（検討中）.md`)。
- **行数と人数は一致しない**。同じ人は短時間では重複して書かないようにしているが、アプリを再起動するとその記憶が消えるため、同じ人がもう一度書かれる。名前を変えた人も別の行になる。人数を知りたいときは、下の「何人分たまったか」のコマンドを使う。

### 整理後の索引(by-tag / by-name)

`inbox/`は「見かけた順に書き足すだけ」の生ログで、名前検索が実際に引くのは整理バッチ(毎時5分UTCに自動実行、2026-09-19から稼働中)が作る下記の索引。

- `/var/lib/clash-royale-api/player-index/by-tag/` … タグごとの最新の名前。同じ人が何度書かれていても1件にまとまる
- `/var/lib/clash-royale-api/player-index/by-name/` … 名前から引くための索引。プレイヤー名検索結果画面はこれを使う
- `inbox/`に書かれてから索引に載るまで最大2時間ほどかかる(巡回で見かけた直後は検索に出ない)
- 溜まった人数は`cat /var/lib/clash-royale-api/player-index/by-tag/*.tsv | wc -l`で正確に数えられる(`inbox/`と違って重複が無いため)

### 確認コマンド

SSHで入ったあと、まず次のコマンドで置き場所へ移動する(以下のコマンドはすべてVM上のbash用)。

```bash
cd /var/lib/clash-royale-api/player-index/inbox
```

| 知りたいこと | コマンド |
|---|---|
| ファイルの一覧とサイズ | `ls -lh` |
| 全部で何行あるか | `cat *.tsv \| wc -l` |
| 何人分たまったか(タグの重複を除く) | `cut -f1 *.tsv \| sort -u \| wc -l` |
| ファイルごとの行数(1時間ごとの増え方) | `wc -l *.tsv` |
| データ全体の容量 | `du -sh /var/lib/clash-royale-api/player-index` |
| 最新のファイルの末尾10行 | `tail -n 10 "$(ls \| tail -1)"` |
| 名前で探す(大文字・小文字を区別しない) | `grep -i "tanjiro" *.tsv` |
| タグで探す | `grep "#208R8PQJP9" *.tsv` |
| 記録される様子をリアルタイムで見る(`Ctrl+C`で終了) | `tail -f "$(ls \| tail -1)"` |

- 1時間ごとにファイルが増えるので、`*.tsv`と書けば全ファイルがまとめて対象になる。
- ディスクの空き容量は`df -h /`で見られる(2026-09-18時点で30GB中23GB空き)。

## 3. アプリの状態を確認する

| 知りたいこと | コマンド |
|---|---|
| アプリが動いているか | `systemctl status clash-royale-api`(`active (running)`なら稼働中。`q`で閉じる) |
| アプリのログ(直近100行) | `journalctl -u clash-royale-api -n 100 --no-pager` |
| アプリのログを流しっぱなしで見る(`Ctrl+C`で終了) | `journalctl -u clash-royale-api -f` |
| 蓄積の書き込みに失敗していないか | `journalctl -u clash-royale-api --no-pager \| grep "player sightings"`(何も出なければ失敗していない) |
| メモリの使用状況 | `free -m` |

## 4. 注意

- 上のコマンドはすべて読み取るだけのもの。`inbox/`のファイルは整理用のバッチ(2026-09-19から毎時稼働中)の入力になるため、手で編集・削除しない。`by-tag/`・`by-name/`もバッチが作り直すものなので触らない。
- データはopcユーザーの所有なので、見るだけなら`sudo`は要らない。
- アプリの再起動(`sudo systemctl restart clash-royale-api`)などの操作は、デプロイ手順(`CLAUDE.md`)に沿って行う。

# お気に入り（検討中）

2026-09-19にユーザーから依頼。プレイヤーとクランをお気に入りに登録し、すぐ開けるようにする。**条件: DBは使わず、Cookieで保持する。** 以下は2026-09-19時点の具体案(未着手)。コードはまだ変更していない。

## ユーザーと決めたこと(2026-09-19)

| 項目 | 決定 |
|---|---|
| 表示場所 | トップページ+専用画面(お気に入り画面、`/favorites`) |
| 表示内容 | 専用画面は最新の情報も出す(公式APIを呼ぶ)。トップページは名前だけ(APIを呼ばない) |
| 件数の上限 | プレイヤー・クランそれぞれ10件 |
| 登録・解除の操作 | プレイヤー情報画面・クラン情報画面の見出しの☆/★ボタン。解除はお気に入り画面でもできる。ランキングや一覧の各行には置かない |
| 並び順 | 登録が新しい順(並べ替え操作は作らない) |
| `TODO.md`のA-3「自分のタグを覚えておく」 | お気に入りで兼ねる。「自分」の区別は持たない。実装したらA-3を閉じる |

## 画面

### プレイヤー情報画面・クラン情報画面

- 見出し(`<h1>`)の名前の横に☆ボタン。登録済みなら★で、押すと解除。
- ボタンには`aria-pressed`と、「お気に入りに追加」「お気に入りから外す」のラベルを付ける(記号だけでは読み上げで意味が伝わらないため)。
- 10件に達しているときに☆を押すと、ボタンの近くに「お気に入りは10件までです。お気に入り画面で外してから追加してください」と出す(お気に入り画面へのリンク付き)。古いものを黙って押し出すことはしない。

### トップページ

- 検索フォームの下、個人ランキングの上に「お気に入り」カードを置く。
- プレイヤー・クランの2行に分け、名前をリンクのチップ状に並べる(登録が新しい順)。名前はCookieに保存したものを使い、公式APIは呼ばない(トップページの表示速度を落とさないため。`TODO.md`のレビュー3回目の1で、すでに遅さが課題になっている)。
- 末尾に「お気に入り画面へ」のリンク。
- 1件も無いときはカードごと出さない(初めての訪問者に空の枠を見せないため)。登録方法の案内はお気に入り画面が受け持つ。

### お気に入り画面(`/favorites`、新設)

- ヘッダーのメニューに「お気に入り」を足す(検索・個人ランキング・クランランキング・カードの後)。メニューが5項目になるので、スマホ幅での折り返しを実描画で確認する。
- 「プレイヤー」「クラン」の2つの表。どちらも各行に「外す」ボタン。

| 表 | 列 |
|---|---|
| プレイヤー | 名前(プレイヤー情報画面へ)・タグ・トロフィー・ランク戦のレーティング(あれば)・所属クラン(クラン情報画面へ) |
| クラン | 名前(クラン情報画面へ)・タグ・クランスコア・メンバー数(n/50) |

- 表の下に注記: 「お気に入りはこのブラウザに保存されます。別の端末・ブラウザとは共有されず、Cookieを消すと消えます」。
- 空のとき: 「プレイヤー情報画面・クラン情報画面の☆で登録できます」と、プレイヤー検索・クラン検索のフォーム。
- 1件ずつの取得失敗はエラー画面にしない(対戦履歴と同じ「画面の一部だけ失敗」の扱い)。
  - 見つからない(404。アカウント削除・クラン解散など): 保存してある名前とタグに「見つかりませんでした」を添えて出す。**自動では外さない**(公式APIの一時的な不調で消えると困るため)。
  - それ以外(429・接続失敗など): 保存してある名前に「最新の情報を取得できませんでした」を添える。
  - `CLAUDE.md`の「エラー画面は`GlobalExceptionHandler`に集約する」の例外一覧に、この画面を追記する。

## Cookieの形式

- Cookieは2つ: `fav_players`、`fav_clans`(プレイヤーとクランで上限を別に数えるため、分けた方が扱いやすい)。
- 値: `1.` + Base64URL(パディングなし)で、中身はUTF-8の「タグ(`#`なし) TAB 名前」を改行でつないだもの。登録が新しい順。
  - 先頭の`1.`は形式のバージョン。後で形式を変えたときに古いCookieを見分けるため。
  - Base64にするのは、名前に日本語・記号・空白が入り、そのままではCookieの値に使えない文字を含むため。パーセントエンコードより短くなる(日本語1文字が約4バイト対9バイト)。
  - 大きさの見積もり: 名前15文字×3バイト+タグ10文字で1件約60バイト、10件でBase64後800バイト程度。1つのCookieの上限(4KB)に十分収まる。
- 属性: `Path=/`、`Max-Age`=365日(国選択のCookieと同じ)、`HttpOnly`、`SameSite=Lax`。
  - **Cookieはサーバー側で書く(JSの`document.cookie`では書かない)**。SafariはJSで書いたCookieを7日で消すため(ITP)。国選択の`CountryPreference#remember`と同じやり方になる。
  - `SameSite=Lax`は、他サイトからのPOSTで勝手に登録・解除されること(CSRF)を防ぐため。Jakarta Servlet 6の`Cookie#setAttribute("SameSite", "Lax")`で付けられる。
- 名前は表示用の控え。**登録時と表示時に最新の名前で書き直す**(下の「名前の更新」)。
- 読み込みは寛容にする: 壊れた値・知らないバージョン・タグの形式でない行は、その行(または全体)を無視して空として扱う。エラー画面にはしない。11件以上あれば先頭10件だけ使う。名前は`GameText.stripFormatting`を通し、50文字で切る。
  - Cookieは利用者が書き換えられるが、影響はその人の画面だけ。名前は`th:text`で出すのでHTMLとして解釈されない。

## 登録・解除の仕組み

- エンドポイント(このアプリ初のPOST):
  - `POST /favorites/players/{tag}` … 登録
  - `POST /favorites/players/{tag}/remove` … 解除
  - `POST /favorites/clans/{tag}`、`POST /favorites/clans/{tag}/remove` … クラン
- **登録時の名前はサーバーが公式APIから取る**(フォームから名前を受け取らない)。直前に情報画面を開いているので、ほぼ必ず2分キャッシュに当たり、APIの呼び出しは増えない。タグの実在確認も兼ねる。
- ボタンは`<form method="post">`で作り、JSで送信を横取りして`fetch`で送る。成功したら画面遷移せずにボタンの☆/★だけを切り替える。
  - **遷移させない理由**: フォーム送信→リダイレクトで同じ画面を開き直すと履歴が1つ積まれ、ブラウザの「戻る」や戻るリンクが「登録前の同じ画面」に戻ってしまう(言語切替で`location.replace`を使っているのと同じ問題)。
  - `fetch`のときは`Accept: application/json`を付け、サーバーは`{"favorite": true}`などのJSONを返す。上限超過は409と`{"error": "limit"}`。
  - JSが動かないときの予備: 通常のフォーム送信として受け、元の情報画面(お気に入り画面から外したときはお気に入り画面)へ303でリダイレクトする。戻り先は`Referer`ではなくタグと固定の値(`back=player|clan|favorites`)から作る(任意のURLへ飛ばせる「オープンリダイレクト」を作らないため)。
- すでに登録済みのタグをもう一度登録しても何もしない(並び位置も変えない)。

## 名前の更新

- プレイヤー情報画面・クラン情報画面を開いたとき、そのタグがお気に入りで、保存している名前と違えば、Cookieを書き直す。
- お気に入り画面でも、取得できた最新の名前でCookieを書き直す。
- こうしておけば、トップページはAPIを呼ばずにほぼ最新の名前を出せる。

## 戻ったときに古い表示が出る問題

- ☆を押した後にブラウザの「戻る」でトップページに戻ると、ブラウザが保存しておいた古いトップページ(bfcache)が出て、お気に入りが反映されていないことがある。
- `layout.html`には、言語切替で同じ問題に対処する仕組み(`sessionStorage`の`lastLang`と`pageshow`)が既にある。これを「言語」だけでなく「お気に入りの変更」も見るように広げる。
  - ☆の切替に成功したら`sessionStorage`に変更時刻を書く。
  - トップページとお気に入り画面は、表示時点の変更時刻を覚えておき、戻ってきたときに食い違っていれば読み直す。
- 戻るリンクの画面判定(`screens`)に`/favorites`を足し、文言「お気に入りに戻る」(`action.backToFavorites`)を追加する。

## お気に入り画面の取得処理

- 最大20件(プレイヤー10+クラン10)を公式APIから取る。順番に呼ぶと、キャッシュが空のとき数秒かかる。
- **仮想スレッド(Java 21)で並列に取得し、同時実行は4本程度に絞る**(`Semaphore`)。公式APIのレート制限の値が未確認なので、一度に20本投げない。
- `PlayerService#findPlayer`・`ClanService#findClan`を通して呼ぶ。キャッシュが効き、名前検索用の蓄積(`PlayerSightingLog`)にもそのまま乗る。
- 各件の結果は「取得できた/見つからない/取得できない」の3通りを表す型(sealed interface)で返し、Controllerで表示用に詰め替える。

## クラス構成(案)

| 層 | クラス | 役割 |
|---|---|---|
| `domain` | `Favorites` | タグと名前の並び。登録(先頭に追加・上限・重複)、解除、名前の書き換え。HTTPに依存しない |
| `web` | `FavoriteCookies` | Cookieの読み書きと、値の形式(バージョン・Base64・寛容な読み込み)。`CountryPreference`と同じ位置付け |
| `service` | `FavoriteService` | お気に入り画面用の並列取得 |
| `web` | `FavoriteController` | `/favorites`の表示と登録・解除のPOST |
| `web/view` | `FavoritePlayerView`・`FavoriteClanView` | お気に入り画面の行 |

- `PlayerController`・`ClanController`・`HomeController`は、`FavoriteCookies`から状態を読んでModelに詰める(`favorite`、`favoritePlayers`など)。
- **命名の注意**: `favoriteCards`は「得意カード」の意味で既に使っている(`PlayerBattleStats`・`BattleStatsView`)。混同しないよう、新しいものは`Favorites`・`FavoriteCookies`のように「お気に入り」だと分かる名前にし、`favoriteCards`側は必要なら別途`strongAgainstCards`などへ改名を検討する(今回の範囲外)。
- 文言は4言語(`messages*.properties`)に追加。ゲーム内の用語ではないので公式表記の裏付けは不要。

## テスト(`project_test_scope`の範囲: 純粋ロジック+Mockito)

- `Favorites`: 先頭に追加、11件目の拒否、重複登録で位置が変わらない、解除、名前の書き換え。
- `FavoriteCookies`: 書いて読むと元に戻る、壊れたBase64・知らないバージョン・タグでない行・11件以上・長すぎる名前の扱い、Cookieの属性(`HttpOnly`・`SameSite`・`Max-Age`)。
- `FavoriteService`: 一部の件だけ404や接続失敗になっても残りは取得できる、登録順が保たれる。

## 進め方(小さく3回に分ける)

1. `Favorites`・`FavoriteCookies`・登録/解除のPOSTと、情報画面の☆ボタン(JSの`fetch`、予備のフォーム送信を含む)。
2. トップページのお気に入りカードと、戻ったときの読み直し。
3. お気に入り画面(並列取得・部分的な失敗の表示)、ヘッダーのメニュー、戻るリンクの文言。

- 各回で、スマホ幅とダークモードを含めて実描画で確認する。

## 決めていないこと(着手時に決める)

- ☆ボタンの見た目(記号のままか、アイコンにするか)。
- お気に入り画面のプレイヤーの列にランク戦の順位まで出すか(今シーズンの順位があるのは上位の人だけ)。
- Cookieの説明をサイトのどこかに載せるか。お気に入りのCookieは利用者自身の操作で保存する機能のためのもので、同意のバナーが必要な種類ではない想定だが、全世界向けに公開する前に確認する。

# 進捗ログ

Claude Codeはセッションをまたいだ記憶を持たないため、作業のキリが良いタイミングでこのファイルを更新し、
「どこまで完了したか」を常に最新の状態に保つ。別セッションで再開する際は、まずこのファイルを確認する。

現在も有効な方針・取り決めは`CLAUDE.md`側を参照。このファイルでは、単純な変更内容の列挙(git logで追える情報)
は簡潔にし、**なぜそうしたかという経緯・既知の制約・ハマりポイント**など再発防止に効く情報を中心に残す。

## フェーズ0: プロジェクト立ち上げ(2026-09-13)

- [x] CLAUDE.md作成、方針決定(機能範囲・DB方針・インフラ方針・進め方など)
- [x] Clash Royale公式APIキー取得(developer.clashroyale.comでSupercell IDログイン)
  - **既知の制約**: Allowed IP Addressesは作成後の編集(追加)が不可。IPを追加したい場合はキーの作り直しが必要。
  - キー本体は`application-local.yml`(gitignore対象)で管理。
- [x] Spring Bootプロジェクト雛形作成、プレイヤー検索(`/player`)・クラン検索(`/clan`)の実装
  - groupId=`com.example`, artifactId=`clash-royale-api`, Java 21 / Spring Boot 3.3.4 / Web + Thymeleaf
  - 設定は`application.yml`(コミット対象、tokenは空)+`application-local.yml`(gitignore対象、実トークン)の2層構成
- [x] ローカル動作確認(実際のAPIキーで`#VRQUQ0QL`・`#PPCQ9R2R`にて正常表示、404エラーハンドリングも確認)
  - **既知の制約**: クランメンバーの`expLevel`は実際のAPIレスポンス自体が0を返す仕様(アプリ側のバグではなく元データの仕様)。
- [x] GitHubリポジトリ作成・連携(https://github.com/yyama694/clash-royale-api, Public)

## フェーズ1: OCIへのデプロイ(2026-09-13、マイルストーン1達成)

- [x] 旧hello-world用VM(`140.245.83.216`)を削除し、新規VM作成
  - リージョン: 東京(ap-tokyo-1)。Ampere A1.Flexは"Out of host capacity"で作成できず、**VM.Standard.E2.1.Micro**(x86_64)で作成(hello-worldと同じ経緯)。
  - OS: Oracle Linux Server 9.8。パブリックIP: `132.226.7.203`。SSH鍵: `oci_clash_royale_api`(hello-world用を流用)。
- [x] Clash Royale APIキーの許可IPリストに、このVMのIPを追加登録
  - **既知の制約**: キー作成/IP変更後、APIへの反映まで最大1分弱のタイムラグがあり、その間は`403 Forbidden`になる。時間を置いて再試行すれば解決する。
- [x] VM上にJava 21(Amazon Corretto、`/opt`に展開)環境構築、jarをsystemdサービス化
  - **ハマりポイント**: `scp`で`/tmp`経由で配置したsystemdユニットファイルは、SELinux(Enforcing)のコンテキストが`user_tmp_t`のままになり、`systemctl enable`が「Unit file does not exist」という分かりにくいエラーで失敗する。`sudo restorecon -v <path>`でコンテキストを`systemd_unit_file_t`に修復すれば解決する。今後`/tmp`経由で設定ファイルを配置する際は同様の問題に注意。
- [x] VM内部のfirewalldとOCIセキュリティリストで8080番ポートを開放し、外部からのアクセス成功を確認

→ プレイヤー検索・クラン検索の最小機能を持つWebアプリが、OCI上へのデプロイまで完了(`http://132.226.7.203:8080/`)。

## フェーズ1.5: 機能追加・改善(2026-09-13〜2026-09-14)

### 名前検索機能(追加 → 撤去)

- [x] 2026-09-13: 名前→タグの検索機能を追加。公式APIに名前検索エンドポイントが存在しないため、プレイヤー検索・クラン検索で判明した「名前→タグ」の対応をテキストファイル(`data/name-index.json`)に蓄積し、部分一致で検索する方式を採用。
- [x] 2026-09-14: **機能自体を撤去**。クラロワの実プレイヤー数は数千万〜1億人規模(2026年時点、調査元によりMAU推計は4,300万〜1億3,200万人程度と幅がある)と判明し、この規模を無料枠のOCI VM(低スペック)+テキストファイル方式で網羅するのは非現実的と判断。関連ファイル(`NameIndexService`等)・設定を全て削除し、VM上の`data/`も削除。
  - **再検討する場合の代替案**: クラン名検索は公式APIに`GET /clans?name=...`という本物の検索エンドポイントがあるため、プレイヤー名検索(公式APIに手段なし)とは切り離してクランのみ実装する案が候補(未着手)。

### 対戦履歴・対戦詳細

- [x] プレイヤー情報画面に直近の対戦履歴表示を追加(`battlelog`使用)。
  - **既知の制約**: 公式APIのbattlelogは件数指定・ページネーション非対応で、取得できる件数は変動し上限は保証されない(40件程度になることもある)。ユーザーから「直近50試合」の要望があったが仕様上不可能なため、画面に注記。
- [x] 対戦モード表示を「日本語ラベル (英語名)」形式に変更(`GameModeLabels`)。マッピング表は主要モードのみ網羅、未知のモードは英語フォールバック。
- [x] 対戦詳細画面(自分・相手それぞれの使用デッキ8枚)を追加(`/player/battle?tag=...&index=N`)。
  - **設計上の注意**: battlelogには対戦を一意に識別するIDが存在しないため、永続化はせず「タグ+一覧内のインデックス」をキーにして詳細アクセス時にbattlelogを再取得する方式にした。そのため、詳細画面を開くまでの間に新しい対戦が発生しているとインデックスがずれる可能性がある(その場合は「指定された対戦が見つかりませんでした」とエラー表示)。

### カード名の日本語表示

- [x] Accept-Languageヘッダーが`ja`の場合のみ、対戦詳細のカード名を日本語表示(`CardNameLabels`)。
  - **判定方法の決定**: 「IPアドレスからGeoIPで国を判定」ではなく「ブラウザのAccept-Languageヘッダーで判定」を採用(ユーザーが選択)。理由: 追加の外部データ(GeoIPデータベース等)が不要でシンプルであり、将来の多言語対応も「ユーザーの言語設定に応じて表示」という形でそのまま拡張できるため。**既知のズレ**: Accept-Language(ブラウザの言語設定)は必ずしも「アクセス国」と一致しない(例: 海外在住の日本語ユーザーは日本語表示になる)。
- [x] カード日本語名の誤訳を修正。
  - **経緯**: 「ファーネス」「キャノンカート」など、初回実装時に単純なカタカナ音訳で当て推量した日本語名が、実際の公式ローカライズ名と異なっていた(公式は単純音訳ではなく、キャラクター名や意訳を用いるケースが多い)。複数の日本語攻略サイト(Smashlogクラロワ攻略の全カード一覧、famitsu、wikiwiki.jp、kabutom氏のnote記事等)を突き合わせて全カード名を再調査し、対応表を全面的に修正(例: Furnace→オーブン、Cannon Cart→60式ムート、Bandit→アサシン ユーノ)。
  - **既知の制約**: ウェブ検索による調査ベースのため、開発者自身がゲーム内表示と一つ一つ完全に照合したわけではない。将来誤りに気づいた場合は都度修正する運用(2026-09-17追記: `CardNameLabels`は2026-09-15に廃止され、現在の修正先は`messages_ja.properties`の`card.*`)。

### 戦績サマリー・得意/苦手カード

- [x] プレイヤー情報画面に「過去◯戦で◯勝◯敗」の戦績サマリーと、カードごとの勝率集計(`PlayerBattleStats`)を追加。
  - **既知の制約**: 集計対象は直近の対戦のみ(取得件数は変動し上限は保証されない)で、母数が少なく統計的信頼性は高くない。使用デッキの種類が少ないプレイヤーでは得意/苦手カードが重複する場合がある。
  - **修正(2026-09-14)**: 初回実装は「自分が使ったカードの勝率」で集計していたが、正しくは「**対戦相手が使ったカードに対する自分の勝率**」が得意/苦手カードの定義だった(ユーザー指摘により`opponent.cards()`を集計対象に修正)。
  - **修正(2026-09-14)**: ランキング対象の最低使用回数(`MIN_USES_FOR_RANKING`)を2回→5回に変更(ユーザーから「最低でも5戦戦ったものから抜粋して」と指摘)。5回未満のカードしかない場合は全カードにフォールバックする挙動は維持。**(2026-09-17追記)** 2026-09-15のコードレビュー対応(`79a67ed`)で記録なく3回に変わっていたため、5回に戻した(`09491a3`)。

### デザイン・UI

- [x] カード型デザインへ刷新(共通CSS`style.css`、共通ヘッダーフラグメント化、対戦履歴の勝敗バッジ化)。
- [x] 小規模改善: テーブルのスマホ横スクロール対応(`.table-scroll`)、統計項目へのアイコン追加、トップページに説明文追加。
  - **経緯**: 別セッション経由でユーザーから「画面デザインについてアドバイスが欲しい、任せる」との依頼があり、改善範囲を確認した上で「安全・実用中心の小修正のみ」を選択(ダークモード対応・レイアウト全面刷新は見送り)。

### その他

- [x] アプリ名を「Clash Royale 検索(by yyama)」に決定、ヘッダー・タイトルに反映。
- [x] `画面一覧.md`・`用語集.md`を作成(ユーザーとのコミュニケーションで使う用語を統一するため)。

### クラン名検索

- [x] クラン検索に「クラン名でも検索できる」機能を追加(2026-09-14実装)。
  - 既存の`/clan?tag=...`はそのまま維持しつつ、まずタグとして完全一致検索を試み、見つからなければ(404/400)公式APIの`GET /clans?name=...`によるクラン名の部分一致検索にフォールバックする方式にした。
  - **既知の制約(公式API仕様)**: `name`パラメータは3文字未満だとAPI側が`400 Bad Request`(`Filtering parameter 'name' has to be at least 3 characters long`)を返す。またタグとして無効な形式の場合も404ではなく400が返る点に注意(フォールバック判定は400・404の両方で発動するようにした)。
  - **回避策(2026-09-14、ユーザーが発見)**: 「償い」のような2文字のクラン名は、末尾に全角スペースを足して3文字以上にすると検索できる(部分一致検索のため、末尾の空白があっても一致する)。ユーザー向けの案内としても有用。
  - 検索結果はクランスコア降順で上位20件のみ表示(一致件数が多い場合、100件近く返ることがあるため)。
  - **ハマりポイント**: 調査中、Bashツール(Git Bash)経由で`curl`に日本語文字列を渡すと、`curl.exe`(ネイティブWindowsバイナリ)側でエンコーディングが化けて文字化けした検索結果が返ることがあった。実際のアプリ(Java/Spring)はUTF-8を正しく扱えており問題なし。今後Windows環境でBashの`curl`から日本語を含むAPIリクエストを手動検証する際は、PowerShellの`Invoke-WebRequest`を使うか、アプリ自体の挙動で判断する方が確実。

## フェーズ1.6: 画面の細部改善(2026-09-15)

- [x] プレイヤー情報画面: 所属クラン名をクラン情報画面へのリンクに、対戦履歴の対戦相手名を相手のプレイヤー情報画面へのリンクに変更。
- [x] デプロイ手順を改善: 新jar配置前に現行jarを`.bak`にリネームして退避する運用に変更(失敗時のロールバック手段を確保)。詳細は`CLAUDE.md`のデプロイ手順セクション参照。
- [x] クラン情報画面のメンバー役職を、Accept-Languageに応じてゲーム内表記に変換する`RoleLabels`を追加。
  - **修正の経緯**: 初回実装でcoLeaderを「バイスリーダー」と訳したが、ユーザーから違和感の指摘があり、Supercell公式サポートポータル(https://support.supercell.com/clash-royale/ja/articles/clan-roles-1.html)で確認したところ正しくは「サブリーダー」だった。カード名(`CardNameLabels`)同様、翻訳はウェブ検索や推測に頼らず公式情報で裏取りする方が安全。
  - **設計変更**: 当初は`boolean useJapanese`方式(`CardNameLabels`/`GameModeLabels`と同じパターン)だったが、ユーザーから「今後の多言語化も意識してほしい」との要望があり、`Map<役職, Map<言語コード, ラベル>>`方式に変更。Thymeleafの`#locale`ビルトイン変数をそのまま`RoleLabels.label(role, #locale)`に渡す形にした。日本語以外はAPIの生値(`coLeader`等)ではなく正式な英語表記(Leader/Co-leader/Elder/Member)にフォールバックする。(2026-09-17追記: `RoleLabels`は2026-09-15に廃止され、現在は`messages*.properties`の`role.*`で管理。別言語の追加は`messages_<言語コード>.properties`を足す形になる。)
  - **既知の制約**: 英語以外の言語(中国語・韓国語など)のラベルは未整備。フォールバックは常に英語。
- [x] クラン情報画面のメンバー一覧に「寄付数は今週分(毎週日曜0時(GMT)にリセット)」という注記を追加(ユーザーから「どの期間の寄付数か分かりにくい」との指摘を受けて対応)。

## フェーズ1.7: 対戦詳細へのタワーユニット表示・ドキュメント整合性修正(2026-09-15)

- [x] 対戦詳細画面に、プリンセスタワーに配置するカード(`battlelog`の`supportCards`)を表示する機能を追加。
  - `BattleLogEntry.Participant`に`supportCards`フィールドを追加し、`CardNameLabels`にタワーユニット4種(タワープリンセス/ブラスター/ダガーガール/ロイヤルシェフ)の日本語名を追加。
  - 見出しは当初「プリンセスタワーカード」としていたが、ユーザーから「プリンセスだけじゃないのに違和感がある」と指摘があり、公式の分類名「タワーユニット」に修正。
  - 日本語名はWeb検索で複数の攻略サイト(wikiwiki.jp、note記事等)を突き合わせて裏取り([[feedback_translation_and_i18n_design]]の方針通り)。ゲーム内表記は「タワープリンセス」「ブラスター」「ダガーガール」「ロイヤルシェフ」。
- [x] 別セッション(`clash-royale-api-cf`)による調査を受けて、ドキュメントと実装の食い違いを3点修正(ユーザー承認済み)。
  1. `CLAUDE.md`・`画面一覧.md`: プレイヤー情報画面の「実績・カードレベル」という記載を、実際の表示内容(戦績サマリー・得意/苦手カード・直近対戦履歴)に修正。
  2. `CLAUDE.md`・`画面一覧.md`: クラン情報画面の「戦争実績」という記載を、実際の表示内容(メンバー一覧・トロフィー・寄付数)に修正(クラン対抗戦の実装自体が存在しないため)。
  3. `用語集.md`・`進捗ログ.md`・`player.html`の注記2箇所・`ClashRoyaleApiClient.java`のコメント: 「直近の対戦履歴は最大25〜35件程度」という固定的な件数表記を、「件数は変動し上限は保証されない(40件程度になることもある)」という表現に統一。
  - **経緯**: ドキュメントと実装の整合性は別セッションが調査し、修正は本セッションで実施するという運用(ユーザーの指示)。
- [x] クラン情報画面のメンバー一覧から「レベル」列を削除。
  - **既知の制約(公式API仕様)**: クランメンバー一覧(`GET /clans/{tag}`の`memberList`)が返す`expLevel`は、実機で確認した結果**全メンバーで常に`0`**だった(プレイヤー個別の詳細エンドポイント`GET /players/{tag}`の`expLevel`は正しい値を返す)。ユーザーから「メンバー一覧のレベルが全部0に見える」との指摘を受けて実APIレスポンスを直接curlで確認し、公式APIの既知の制限と判明。メンバー数分プレイヤー詳細を個別取得すれば正しい値は取れるが、API呼び出し数が膨らむため非現実的と判断し、列自体を削除する対応を選択(ユーザー承認)。`ClanResponse.Member`からも`expLevel`フィールドを削除。
- [x] `CLAUDE.md`の「複数セッション運用時のルール」の文言を強化(`ListAgents`確認を編集**前**の必須手順として明記)。
  - **既知のハマりポイント**: 2026-09-15、このルールを`clash-royale-api-cf`セッションが策定・CLAUDE.mdに追記した直後、そのセッション自身が(自らが書いたルールに反して)`ListAgents`での確認を怠り、別の修正で自己編集してしまう事例が発生。ユーザーの指摘で発覚。原因は、当初のルール文言が「デフォルトとする」という抽象的な方針にとどまり、「編集前に必ず`ListAgents`を実行する」という具体的な手順(ゲート)になっていなかったこと。本セッションが依頼を受けて文言を強化(具体的な手順の明記、運用ルールであり技術的強制ではない旨の明記)。**教訓**: 運用ルールは抽象的な方針だけでなく、実行順序を伴う具体的な手順として書かないと、ルールを定めた本人でも即座に破りうる。
- [x] クラン情報画面のメンバー一覧に、名前・役職・トロフィー・寄付数でのソート機能を追加。
  - 列見出しをリンク化し、クリックのたびに昇順⇔降順をトグルする方式(sortBy/sortDirクエリパラメータ)。未指定時(初回表示)は公式APIが返す元の並び順のまま。
  - 役職ソートは、昇順が「偉い順」(リーダー→サブリーダー→長老→メンバー)、降順が「偉くない順」になるよう、ClanController内に役職ごとの順位マップ(ROLE_RANK)を用意して比較する方式にした(ユーザー指定の仕様)。※同日のコードレビュー対応で`domain/ClanRole`(enum)に移動済み。
  - **ハマりポイント**: このデプロイ作業中、本番VM(132.226.7.203)がSSH・HTTP・pingすべて無応答になる事象が発生(scpも`Connection closed`で失敗)。既知の「低スペックVM(Always Free枠)のフリーズ実績」に該当する事象と考えられる。ユーザーにOCIコンソールからの再起動を依頼したところ復帰し、再起動後は問題なくデプロイできた。今後同様の無応答が起きた場合は、まず数分待って自然復帰しないか確認し、復帰しなければOCIコンソールからの再起動を依頼するのが実践的な対処。
- [x] 「複数セッション運用時のルール」(編集前にListAgentsで確認、修正は別セッションに依頼)を撤回。各セッションが通常通り自分でファイルを編集してよい運用に戻す(ユーザーの判断、`clash-royale-api-cf`セッションにて対応)。`CLAUDE.md`の該当箇所は削除せず「(撤回済み)」として経緯を残す形にした。
- [x] `CardNameLabels`(カード名の日本語表示)を`boolean useJapanese`方式から`RoleLabels`と同じ`Locale`渡し方式にリファクタリング(`clash-royale-api-cf`セッションで対応)。
  - **経緯**: ユーザーから「日本語表示の判定はロケールで判断すべきでは」との指摘。`RoleLabels`は2026-09-14〜15の改修で`Locale`渡し方式に変更済みだったが、`CardNameLabels`は旧方式のまま取り残されていた(設計の一貫性が崩れていた)。
  - **変更内容**: `CardNameLabels.label(String, boolean)` → `label(String, Locale)`に変更、言語判定ロジックをメソッド内に移動。`PlayerController`の`useJapaneseCardNames`属性の付与処理と、それに伴い不要になった`Locale locale`引数を削除(`RoleLabels`同様、テンプレート側で`#locale`ビルトイン変数を直接渡す方式に統一)。`player.html`・`battle-detail.html`の該当箇所(計6箇所)を`#locale`渡しに修正。テストも`Locale`ベースに更新。
  - 本番デプロイ・動作確認済み(`Accept-Language: ja`/`en`両方でカード名の日本語/英語切り替えを確認)。
- [x] 対戦詳細画面の対戦相手名(matchupセクション)を、プレイヤー情報画面へのリンクに変更(`player.html`の対戦履歴一覧と同じパターン)。ユーザーから「意図は分かるよね」との指示のみで詳細説明なく依頼されたが、既存の「対戦相手名からプレイヤー情報へ遷移できる」導線パターンを踏襲する意図と判断し対応。
- [x] 対戦詳細画面の自分の名前(matchupセクション)も同様にプレイヤー情報画面へのリンクに変更(ユーザーからの追加依頼)。
- [x] **全体コードレビュー(2回目)の指摘をすべて対応**(このセッションで実施)。差分ではなくコードベース全体を対象にレビューし、A〜Fの指摘を全対応した。
  - **背景**: 1回目のレビュー(サブエージェント4観点)は差分ベースで、レイヤ構成やi18nの一貫性といった構造面の問題が残っていた。
  - **主な変更**: Service層/domainパッケージ新設、ViewModel化、ラベル3クラス廃止→`messages*.properties`集約、URLパス形式化、対戦詳細のbattleTime参照化、Wilson score intervalによる得意/苦手カードの順位付け、client層の独自例外化と`@ControllerAdvice`集約、Spring Boot 4.1.1へ更新、APIキーのjar排除、Caffeineキャッシュ導入。
  - **ハマりポイント1**: Spring Boot 4 では `RestClient.Builder` の自動設定が `spring-boot-starter-restclient` に分離されており、この依存が無いと起動時にDIエラーになる(`spring-boot-starter-web`だけでは足りない)。
  - **ハマりポイント2**: `ResourceBundleMessageSource` はOSの既定ロケール(この開発機はja_JP)へフォールバックするため、`spring.messages.fallback-to-system-locale: false` にしないと英語要求まで日本語になる。
  - **ハマりポイント3**: 逆に本番VM(OSロケールが英語)では、Accept-Language無しのリクエストが英語になった。`CookieLocaleResolver.setDefaultLocaleFunction`でAccept-Language無しの場合のみ日本語に倒す形で解決。
  - **ハマりポイント4**: デプロイ直後に本番VMが再びSSH/HTTPともに無応答化。OCIコンソールからの再起動で復帰(2026-09-15で2回目)。カーネルログにOOMの痕跡は無く、Always Free枠の低スペックに起因する既知事象と判断。
  - **秘密情報の扱いを変更**: `src/main/resources/application-prod.yml`はjarに焼き込まれていた(誰でもjarから取り出せる状態)ため廃止。本番はVM上の`/etc/clash-royale-api/env`(root:root 600)をsystemdの`EnvironmentFile`で読み込む方式にした。ローカルは`./config/application-local.yml`(パッケージ対象外)。git履歴への混入は元々なし。
  - **テスト**: 旧ラベル3クラスのテストを`LabelResolverTest`に集約し、`ClanServiceTest`/`PlayerServiceTest`/`TagsTest`を追加(計36件)。Service層を切り出したことで、タグ/クラン名の振り分けや全角スペース補完がMockitoだけで検証できるようになった(MockMvc/MockRestServiceServerは引き続き不使用の方針を維持)。
  - 本番デプロイ・動作確認済み(日本語/英語切替、プレイヤー・クラン・対戦詳細・404、キャッシュ効果を実測で確認)。
- [x] **クランメンバー一覧に「最終アクセス」列(非アクティブ日数)を追加**。ユーザーからの依頼。
  - 公式APIの`/clans/{tag}`が返す`memberList`に`lastSeen`(例: `20260915T092124.000Z`、UTC。`battleTime`と同じ形式)が含まれているため、これを使って非アクティブ日数を算出している。実データ(49人)では欠損0件、0〜1113日の分布だった。
  - 表示は「今日 / 1日前 / 12日前」。列ヘッダは`最終アクセス`で、既存の列と同様にソート可能(昇順=最近アクセスした順、降順=非アクティブな順)。`lastSeen`が取れないメンバーは昇順で末尾。
  - **ハマりポイント**: 英語の単複を`MessageFormat`のchoiceフォーマットで書こうとしたが、`{0,choice,1#1 day ago|1<{0,number,#} days ago}`は`ChoiceFormat`の区切り文字`#`と入れ子の数値書式`#`が衝突して`Choice Pattern incorrect`で落ちる。`clan.lastSeen.day`(単数)と`clan.lastSeen.days`(複数)にキーを分け、テンプレートの`th:switch`で分岐する形にして解決した。
  - **ハマりポイント2**: `{0}`のまま渡すと`MessageFormat`が桁区切りを入れて「1,113日前」になる。同じ表の他の列(トロフィー等)は区切り無しなので、`{0,number,#}`で揃えた。
  - ついでに`MemberSortKey`のURL表記を、列挙名からの機械変換ではなく明示的な`code`フィールドに変更(`LAST_SEEN` → `lastSeen`。アンダースコアを含む列挙名に対応するため)。
  - 本番デプロイ・動作確認済み(日英・両方向ソート・他画面の回帰確認)。
- [x] **対戦詳細画面のタワーユニット表示をコンパクト化**。ユーザーから「タワーユニットのカードが場所をとりすぎている」との指摘。
  - **原因**: タワーユニットは通常1枚のみなのに、8枚デッキ用の`deckGrid`フラグメントを流用していた。このグリッドは`repeat(auto-fit, minmax(78px, 1fr))`のため、列数が少ないとカードが残り幅いっぱいまで引き伸ばされ、1枚が巨大に表示されていた。
  - **対応**: `fragments/layout.html`に専用の`supportCardRow(label, cards)`を追加。`h3`見出しを廃止してラベルを行内にインライン配置し、アイコンを56px→28px、名前・レベルを縦積みから横並びにした(CSS: `.support-row` / `.support-card`)。
  - **ハマりポイント**: `th:if`と`th:replace`を同じ要素に書くと`th:if`が効かない。Thymeleafの属性優先順位は`th:insert`/`th:replace`(100)が`th:if`(300)より先に処理されるため、要素が置換されて条件が無視される。タワーユニット情報が無い対戦でもラベルだけ表示されてしまった。`<th:block th:if="...">`で囲む形に修正。
  - 本番デプロイ・動作確認済み(タワーユニットあり/なし両方の対戦で確認)。
  - **デプロイ後に「本番だとタワーユニットが大きいまま」との指摘 → 調査の結果、アプリ側は正しく、ブラウザのCSSキャッシュが原因と判明**(2026-09-16)。
    - 実測: サーバーが配信する`style.css`は最新(9,416バイト、`.support-card img { width: 28px }`を含む)だが、ブラウザが使っていたのは8,277バイトの古い版で`support`系ルールが0件。Resource Timingの`transferSize: 0`から、サーバーに問い合わせすらせずキャッシュから使われていた。画像の実効`width`は285px(原寸)。
    - **原因**: Spring Bootの静的リソース配信はデフォルトで`Cache-Control`も`ETag`も返さない(`Last-Modified`のみ)。この場合ブラウザはヒューリスティックキャッシュ(`Last-Modified`からの経過時間の約10%を勝手に「新鮮」とみなす仕様)を適用し、再検証せずに古いファイルを使う。
    - **今回に限った話ではなく、CSS/JSを変更してデプロイするたびに毎回起きる構造的な問題**。これまでの変更はHTML側中心だったため表面化していなかった。恒久対応は`TODO.md`に記載(ユーザーの指示により、この時点では修正未着手)。
    - 暫定回避は`Ctrl + Shift + R`(スーパーリロード)。

## フェーズ1.8: 対戦詳細のレイアウト再構成・モバイル対応(2026-09-16)

- [x] **対戦詳細画面のデッキを左右2カラムに再構成**。ユーザーから「画面のデザインで改善ポイントを挙げて」との依頼で洗い出した候補のうち、ユーザーが選択した項目。
  - 従来はプレイヤーごとに`.card`を縦に積んでいたため、1v1でも2枚分、2v2では4枚分スクロールしないと相手のデッキが見えなかった。自分側・相手側を`.decks`(2カラムgrid) / `.decks-side`(縦並び)で左右に並べ、1画面で見比べられるようにした。
  - 幅860pxのままだとカード1枚あたりが窮屈(カード名が3行に折り返す)だったため、この画面のみ`.container-wide`(max-width: 1100px)を併用し、デッキ内のカード画像も56px→72pxに拡大した。
  - デッキは常に8枚なので、`.deck-panel .deck-grid`は`repeat(4, 1fr)`固定にしてゲーム内と同じ4列×2行の形を保っている(共用の`.deck-grid`は得意/苦手カード表示でも使うため`auto-fit`のまま)。
- [x] **モバイル表示の改善**(同じく洗い出し候補からユーザーが選択)。
  - **表の横スクロールを解消**: 600px以下で`table`をブロック表示にし、1行=1カードの積み上げに切り替えた。各セルの項目名はCSSに文字列を埋め込まず、`th:attr="data-label=#{...}"`でi18nメッセージを属性に出し、`td::before`の`content: attr(data-label)`で表示している(対象は対戦履歴・クランメンバー一覧)。
  - **ソート可能な表だけはヘッダを残す**: クランメンバー一覧は列ヘッダがソートの唯一の手段なので、`table.table-sortable`のときだけ`thead`をチップ状の行として残す。対戦履歴側の`thead`は各セルに項目名が出るため非表示。
  - **クラン検索結果の可読性**: 名前・タグ・スコア・人数・ボタンが横一列に折り返していたのを、`.result-main`(名前＋タグ) / `.result-meta`(スコア・人数)の2段構成に変更。480px以下ではボタンを下段全幅に回す。日本語の`clanSearch.members`が`{0}人`で何の数値か分からなかったため`メンバー{0}人`に変更した。
  - **中間幅のブレークポイント追加**: 従来は480pxのみだったため、760px(デッキ2カラムを解除)・600px(表のカード化)を追加した。
  - **ハマりポイント**: セルをgridにすると中の勝敗バッジが列幅いっぱいに引き伸ばされる(`justify-items`の既定が`stretch`のため)。`justify-items: start`を指定して解決。
  - **ハマりポイント2**: 表示確認に使ったChrome拡張の`resize_window`がこの環境では効かず(成功は返るがウィンドウ幅が変わらない)、モバイル幅の描画を確認できなかった。`javascript_tool`でCSSOM経由でメディアクエリの条件式(`r.media.mediaText`)を一時的に緩め、`body`の`max-width`を絞ることで実際の描画を確認した。
  - 本番デプロイ・動作確認済み(2026-09-16)。PC幅で対戦詳細の2カラム表示とクランメンバー一覧のソートを実機確認。モバイル幅は上記の事情でウィンドウを縮められないため、CSSOM経由でメディアクエリを有効化した状態での確認に留まる。

## フェーズ1.9: クランランキング画面の追加(2026-09-17)

- [x] **クランランキング画面(`/ranking`)を追加**。ユーザー依頼「ランキングをパチパチ切り替えられるように、裏では両方読み込んでおくこと」→「各国のユーザに対応して」→「クランランキングは不要。別リンクから行けるようにしておけばいい」の順で要件が変わり、最終的にトップページからリンクする独立画面になった。
  - **プレイヤーのトロフィーランキングは使えない**。`/locations/global/rankings/players`・国別のどちらもHTTP 200で`items: []`を返す(2026-09-17に実測)。トロフィーロードのランキングが公式に廃止されたため。
  - パス・オブ・レジェンドのランキング(`/locations/{id}/pathoflegend/{season}/rankings/players`)は**グローバルのみ**データがあり(例: `2026-08`)、国別は404、当月も404。グローバル/国別の切り替えには使えない。
  - 結果、**両方の範囲でデータが取れる唯一のランキングであるクランランキング**(`/locations/{id}/rankings/clans`、クランスコア順・上位10件)を採用。
  - **対象国は訪問者ごとに解決する**(`web/CountryPreference`)。優先順は クエリ`?country=XX` > Cookie`country`(1年) > Accept-Languageの国 > 日本。`request.getLocale()`はLocaleResolverが解決した表示言語(ja/en)になり国を含まないため、国の推定にはAccept-Languageヘッダを直接読んでいる。
  - locationIdは国コードから機械的に導けないため、`/locations`(`service/LocationService`、キャッシュあり)から引く。大陸や"International"は`isCountry`で除外。
  - **国名の訳はJDK(CLDR)の`getDisplayCountry`に任せる**。200件超の訳語を自前で持つとメンテできないため。ISO以外の国コードでJDKが訳せない場合は公式APIの英語名にフォールバック。表示は日本語/英語のどちらかに寄せる(Accept-Languageがde等でも画面言語に合わせる)。ランキング表の「国・地域」列も同じ仕組みで訳しており、そのために`ClanRankingResponse.Location`に`countryCode`を追加した。
  - **切り替えは通信なし**: グローバルと選択国の両方をサーバー側で描画し、非表示側に`hidden`を付ける。タブのクリックで`hidden`と`aria-selected`を入れ替えるインラインJS(当初は`ranking.html`末尾。個人ランキング画面の追加時に`fragments/layout.html`の`rankingTabs`へ移した)。静的JSファイルにしなかったのは、未解決のCSS/JSキャッシュ問題(`TODO.md`)を増やさないため。
  - **ランキング取得の失敗はエラー画面にしない**: `RankingService`/`LocationService`で`ClashRoyaleApiException`を握って空リストを返し、画面には`ranking.unavailable`を出す。検索機能まで巻き込んで落とさないため。
  - テスト13件追加(`RankingServiceTest`・`LocationServiceTest`・`CountryPreferenceTest`、計58件)。
  - **ハマりポイント1**: `th:replace`はホスト要素ごと置き換えるため、`id`/`hidden`を持つタブパネルには`th:insert`を使う必要がある(最初`th:replace`で書いてidが消えた)。
  - **ハマりポイント2**: 国選択フォームの`th:action`がトップページ時代の`@{/}`のままで、「表示」を押すとトップに戻っていた。画面を分割したときはフォームの送信先も追随させる。
  - 本番デプロイ・動作確認済み(2026-09-17)。`http://132.226.7.203:8080/ranking`でグローバル/日本のタブ切り替え、国名の日本語表示を実機確認。
  - **ハマりポイント3(環境)**: `mvn spring-boot:run`で起動したJavaプロセスは、コマンドラインが`-cp @...`(argfile)形式のためプロセス名やコマンドラインに"clash-royale"を含まない。コマンドライン文字列で絞り込んで`Stop-Process`しても古いプロセスが生き残り、ポート8080を掴んだまま新しいプロセスの起動に失敗する(ログには`Port 8080 was already in use`)。**その結果、コード修正が反映されない「幽霊の旧アプリ」に接続し続け、デシリアライズの不具合と誤認して30分ほど調査した**。停止は`Get-NetTCPConnection -LocalPort 8080 -State Listen`のOwningProcessから行うこと。

## ドキュメントとソースの整合性チェック(2026-09-17)

- [x] ユーザーの依頼で、`.md`ドキュメント・`config.example.yml`・`.gitignore`と、ソース・テンプレート・CSS・メッセージリソースを全件突き合わせた。調査時点で別セッションがクランランキング機能を実装中だったため、その作業途中の部分は対象外にした。
- [x] **得意/苦手カードの最低使用回数を3回→5回に戻した**(`09491a3`)。2026-09-14にユーザー指示で5回にしていたが、2026-09-15のコードレビュー対応(`79a67ed`)で、コミットメッセージにも本ログにも記載なく3回に変わっていた(ユーザー判断で5回に戻す)。
  - **再発防止**: 閾値そのものを検証するテストが無かったのが、気付けなかった原因。「4戦4勝のカードは、Wilson scoreの下限でKnight(7戦5勝)を上回っても順位付けから除外される」テストを追加し、閾値3では失敗することを確認した。
  - 既存のWilson scoreのテスト2件は、比較対象のBatsが3戦だったため、閾値5では閾値の段階で除外され、本来の「試行回数による順位補正」の検証が空振りになっていた。Batsを5戦にして比較が成立するよう修正した。
- [x] ドキュメントの食い違いを修正。
  - `画面一覧.md`: URLが旧形式(`/player?tag=`・`index=N`)のままだったのを実装に合わせ、クラン名検索結果画面・エラー画面を追記した。
  - `CLAUDE.md`: 提供機能(フェーズ1)に実装済みの機能を追記し、レート制限の記述を実装状況に合わせた。
  - `TODO.md`: CSSキャッシュ対策案(テンプレート側は対応済み)と絵文字一覧を修正した。
  - 本ログ: 参照先の誤り(ページビュー向上TODOは`TODO.md`にある)を修正した。
  - `画面一覧.md`・`CLAUDE.md`の修正は、同じファイルを編集していた別セッションのコミット(`c9fcf5b`)に含まれた。
- [x] **`.gitignore`の`config/`を`/config/`に修正し、一度もコミットされていなかった`CacheConfig.java`・`WebConfig.java`を追加**(`87cad28`)。ユーザー判断で、別セッションの作業完了を待ってから対応した。
  - **ハマりポイント**: `.gitignore`のパターンは、先頭に`/`が無いと**どの階層にもマッチ**する。`config/`はルート直下の`./config/`だけでなく、`src/main/java/.../config/`にも効いていた。既に追跡済みだった`ClashRoyaleApiProperties.java`だけは影響を受けず、新規追加の2ファイルだけが黙って除外されたため、気付きにくかった。他クラスから参照されないのでcloneしてもビルドは通り、キャッシュと言語切替だけが無効になる状態だった。本番jarはローカルの作業ツリーからビルドしていたので、本番への影響は無い。
  - コミット内容だけを展開した状態でテスト58件が通ることを確認した。
  - 同時に別セッション(`clash-royale-api-46`、worktreeの`design-polish`ブランチ)も同じ問題に気付き、`93a2d88`で修正していた。2ファイルの中身は同一で、違いは`.gitignore`のコメントと`CLAUDE.md`の補足文面だけ。マージ時に衝突するため、masterにrebaseして解消するよう連絡した。
- [x] **エラー画面のフォールバック文言を原因別に出し分けた**(`ac48392`)。
  - 変更前: `GlobalExceptionHandler`を経由しないエラーでも「指定されたタグのプレイヤー・クランが見つかりませんでした」を出していた。
  - 変更後: Spring Bootが渡す`status`を使い、404は`error.pageNotFound`、それ以外は`error.generic`を出す。
  - 実測(ポート18080で起動):
    - 存在しないURL(404): 日英とも「ページが見つからない」旨の文言
    - POSTによる405: 日英とも汎用の文言
    - 公式API由来の404(存在しないタグ・対戦): 従来どおり原因別の文言
  - **判明した制約**: 不正な`?lang=`による500だけは、エラー画面を表示するための転送先でも`LocaleChangeInterceptor`が同じ例外を投げる。そのため、アプリのエラー画面ではなくTomcat既定のエラーページになる。`TODO.md`の多言語化候補3で対応する。
- [x] ユーザーの依頼で、別セッションの完了待ちの間に多言語化の改善候補を洗い出し、`TODO.md`に記録した(`d90c4ec`)。公式APIの実データ(カード一覧、270戦分の対戦履歴)とローカル起動での実測で裏付けを取った。
- [x] **本番デプロイ・動作確認済み**(2026-09-17、`662af89`時点)。
  - 作業ツリーには別セッションの作業途中のコードがあったため、`git archive HEAD`をスクラッチ領域に展開してjarをビルドした(テスト58件成功、jarに`CacheConfig`/`WebConfig`が含まれ、`application-local.yml`は含まれないことを確認)。
  - 本番で確認した内容:
    - 404の文言(日英)と、存在しないタグの文言
    - `?lang=en`で言語切替のCookieが発行される(`WebConfig`が効いている)
    - `/`・`/ranking`・プレイヤー情報・クラン情報がHTTP 200
    - 得意/苦手カードに使用回数5回未満のカードが出ない
  - `.bak`は、別セッションが同日12:33(JST)にデプロイしたjar。
- **ハマりポイント(並行セッションとの作業ツリー共有)**:
  - `mvn test`が、別セッションの編集途中のコード(存在しないクラスへの参照)でコンパイルエラーになった。`git archive HEAD`(ステージ内容を検証するなら`git archive $(git write-tree)`)をスクラッチ領域に展開し、そこで検証した。起動確認も同様に、別ポート18080で行った。
  - **一時ファイルを作業ツリーに置かない**: curlで検証した出力`body1.html`をプロジェクト直下に作ったところ、削除する前に別セッションのコミット(`c9fcf5b`)に入り、pushまでされた。中身は無害なTomcatの500ページで、`19837f9`で削除済み。
  - **ステージ(インデックス)も全セッションで共有される**: `git add`してからコミットまで間を空けると、他セッションの`git commit`に自分のステージ済みの変更が入る。ステージしたらすぐコミットする。
  - 別セッションが本ログのフェーズ1.9節を書き換えた際に、直後にあった本節が丸ごと消えていた(`c9fcf5b`)。同日中に復元した。長い節を置き換えるときは、置き換え範囲に他セッションの追記が含まれていないか確認する。
  - PowerShell 5.1の`Set-Content -Encoding UTF8`はBOM付きで書き出すため、`.java`に使うとjavacがコンパイルエラーになる。`.java`の一時書き換えにはbashの`sed`を使う。

## フェーズ1.10: 個人ランキング画面の追加(2026-09-17)

- [x] **パス・オブ・レジェンドの個人ランキング**を追加(`105bb64`)。トップページに上位3件の概要、`/ranking/players`にグローバル/国別タブで上位50件(レベル・レーティング・クラン)を表示。
  - 別セッションが12:44頃に実装・テストまで済ませ、コミット前で止まっていたものを引き継いだ。
  - 使うAPIは**シーズン指定なし**の`/locations/{id}/pathoflegend/players`。フェーズ1.9で調べたシーズン指定あり(`/pathoflegend/{season}/rankings/players`)と違い、**国別でもデータが返る**(2026-09-17にグローバル・日本・フランスで50件ずつ取れることを実測)。
  - タブと表を`fragments/layout.html`の`rankingTabs`/`playerRankingTable`に切り出し、クランランキング画面(`ranking.html`)もこれを使うようにした。国選択フォームの送信先は画面ごとに引数で渡す。
  - キャッシュ名`playerRankings`を追加。取得失敗時はクランランキングと同じく空リストにして、`ranking.unavailable`を表示する。
  - テスト2件追加(`RankingServiceTest`)。
  - ローカルで表示を確認済み(日本語/英語、タブ切り替え、国選択フォームの送信先、クランランキング画面が崩れていないこと)。本番デプロイ済みで、`http://132.226.7.203:8080/ranking/players`の表示も確認した(2026-09-17)。
  - **ハマりポイント**: Git Bashで`K=~/.ssh/...`としてscpに渡すと、ホームディレクトリのパスにあるスペース(`Norio Fukuchi`)で分割されて転送に失敗する。`K="$HOME/.ssh/oci_clash_royale_api"`とし、使うときも`"$K"`と引用符で囲む。
- [x] **トップページの個人ランキングを上位100人にし、グローバル/国別のタブ切り替えを付けた**(`94d1852`)。ユーザー依頼「100位まで表示して」「グローバルとローカルをパチパチ切り替えられるように」。
  - 個人ランキング画面との役割の重なりはユーザーに確認し、「画面は残してもっと多く表示」を選択。個人ランキング画面は200人にした。
  - **公式APIの上限**: `/locations/{id}/pathoflegend/players`の`limit`は最大1000件(1001を指定しても1000件)。国別は実在件数まで(日本は837件、2026-09-17時点)。200人に留めたのは低スペックVMでの描画コストのため。
  - 対象国の決定とModelへの設定(国一覧・選択国・国別タブを開くか)が3画面で重複するため、`web/RankingScope`に切り出した。
  - トップページの国選択フォームは送信先を`/#player-ranking`にしている。国を選び直した後にページ先頭へ戻らず、ランキングの位置で結果が見えるようにするため(GETフォームでもactionのフラグメントは保持される)。
  - 本番デプロイ・動作確認済み(2026-09-17)。トップページ・個人ランキング画面ともグローバル/日本のタブ切り替えと件数を確認。
  - **ハマりポイント**: デプロイ中にVMが再起動しており(`uptime`が1分)、scpが転送されないまま失敗していた。出力を`2>/dev/null`で捨てていたため気づくのが遅れた。デプロイコマンドのエラー出力は捨てず、失敗したら`uptime`とVM上のjarの時刻を確認する。
- [x] **個人ランキングの取得を1回にまとめた**(`4cc710d`、`TODO.md`のソースコードレビュー3回目の3)。
  - それまでは件数(100/200)がキャッシュのキーに含まれ、トップページと個人ランキング画面で同じデータを公式APIから2回取得していた。
  - `RankingService#topPlayers`は、常に`MAX_PLAYER_RANKING_SIZE`(200)で取得して先頭を切り出す。個人ランキング画面の件数もこの定数を参照するので、画面の件数と取得件数がずれない。最大件数を超える件数を渡すと`IllegalArgumentException`(黙って少なく表示しないため)。
  - テスト2件追加(計62件)。
  - ローカルの実測: トップページ(キャッシュ空)1.33秒の直後に、同じ国の個人ランキング画面が0.08秒。先頭100人の並びも一致。
  - 本番デプロイ済み(2026-09-17)。本番ではキャッシュが効いた状態でも個人ランキング画面に0.6〜0.8秒かかる。HTMLが約230KB(400行)あり、低スペックVMでの描画と転送に時間がかかっているとみられる。そのため本番の応答時間だけでは、キャッシュが効いたかどうかを判別できなかった(効いていることはローカルで確認)。
- [x] **テストの無かったクラスにテストを追加**(`9dd2134`、19件、計81件)。対象は`RankingScope`・`GlobalModelAttributes`・`ApiTimestamp`・`BattleResult`・`SortDirection`・`ClanRole`。
  - テストで`RankingScope`の不具合が見つかり、ユーザー確認のうえ修正した。`?country=ZZ`のように一覧に無い国を指定すると、フォールバック先の国(前回のCookieやAccept-Languageの国)を「選び直した国」としてCookieに保存し、国別タブも開いていた。指定どおりの国に決まったときだけを明示選択とみなすようにした。
  - 別セッションが多言語化の改修を作業ツリーで進めていたため、検証とデプロイ用のjarは`git archive HEAD`をスクラッチ領域に展開して作った(作業途中のコードを本番に入れないため)。
- [x] **トップページの個人ランキングは国別タブを初期表示にした**(`3574f24`、ユーザー依頼)。国が決まらないとき(国一覧の取得失敗)はグローバル。個人ランキング画面・クランランキング画面は従来どおりグローバルが初期表示。
  - `HomeController`は別セッションも編集中(表示人数の変更、未コミット)だったため、自分の差分だけのパッチを作って`git apply --cached`でステージし、相手の変更をコミットに混ぜなかった。
  - 本番デプロイ・確認済み(2026-09-17)。

## フェーズ1.11: 画面デザインのレビュー指摘の反映(2026-09-17)

- [x] **3回の画面デザインレビューで記録した指摘を、ユーザーの指示でまとめて修正**。項目ごとの対応状況は`TODO.md`の「画面デザインの改善候補」の対応結果の表を参照。
  - 仕様の判断が要る2件はユーザーに確認した。
    - トップページの個人ランキング: 100人→**10人**。ページの高さがPC幅で5,574px→1,615pxに縮んだ。
    - 対戦数が少ないプレイヤーの得意/苦手カード: 全カードへのフォールバックを廃止した。使用回数5回以上のカードが無ければ出さずに、案内文を出す。「5回」はユーザー指定の仕様値なので変えていない。
  - **2v2の表示の不具合**: 公式APIの`team`は、見ているプレイヤーが先頭とは限らない。そのため、味方のデッキが一番上に「自分のデッキ」として出ていた。`ViewMapper`で本人を先頭に並べ替え、「味方のデッキ」を追加した。対戦履歴も、相手2人と味方を表示するようにした。
  - **色の変数の分け方**: 以前はダークモードの`--color-primary`が明るい色で、それを白文字のボタンやヘッダーの背景にも使っていた。そのためコントラスト比が1.86〜2.77:1だった。「白文字を載せる背景」(`--color-primary`)と「文字色」(`--color-link`/`--color-heading`)に分けた。
  - **CSSのキャッシュ対策**(`TODO.md`、ユーザー判断で対応案1): HTMLとCSSを大きく変えるので、古いCSSが残ると表示が崩れる。そのため、同時にコンテンツハッシュ付きURLを有効にした。
  - テスト8件追加(`ViewMapperTest`・`DisplayNamesTest`ほか)。フォールバック前提だった既存テスト2件は、対戦を5回分並べる形に直した。
  - 多言語化の改修を並行で進めていた別セッション(`clash-royale-api-46`)と相談し、テンプレート・ViewMapper・CSS・messagesは、こちらが先にコミットする順番にした。名前の色指定タグの除去は、こちらの`web/DisplayNames`と、向こうの`domain/GameText`で役割が重なる。向こうが上に乗せるときに`GameText`へ寄せる予定。
  - **ハマりポイント1(改行コード)**: Git Bashの`grep -c $'\r$'`で数えて「テンプレートはCRLF」と判断し、書き込み後にCRLFへ変換した。ところが実際のリポジトリは、テンプレートがLF、CSSとメッセージがCRLF、`layout.html`は混在だった。変換の結果、変更していない行まで差分に出た。改行コードは`git ls-files --eol <path>`で、リポジトリ側(`i/`)を見て判断する。`layout.html`は多数派のLFに揃えた。
  - **ハマりポイント2**: タブに横スクロール(`overflow-x: auto`)を付けると、選択中の下線を`margin-bottom: -1px`ではみ出させていたために縦方向にも溢れ、タブの右端に縦スクロールバーが出た。下線を`box-shadow: inset`で描き、`overflow-y: hidden`にして解決した。
  - **見送り**: ページごとに本文の幅が変わる件。全画面を1000pxに揃えて試したが、対戦詳細でカード名が折り返したため戻した。
  - 確認は、HEADに自分の変更ファイルだけを重ねたコピーをスクラッチ領域に作り、ポート18080で起動して行った(作業ツリーには別セッションの編集途中のコードがあったため)。確認した内容:
    - PC幅: トップページ、プレイヤー情報(2v2・対戦1件)、対戦詳細(2v2)、クラン情報、クラン検索(結果あり・なし)、404、ランキング
    - ダークモード
    - 360〜375px幅: 個人ランキング、クラン情報のソート用チップ、長い国名のタブ
    - キーボード: タブの左右キー移動とフォーカスリング
  - 本番デプロイ・動作確認済み(2026-09-17、`1e7e66f`)。jarは`git archive`で展開したコミット済みの内容からビルドした(テスト89件成功)。
    - CSSのURLがハッシュ付き(`/css/style-9ce5….css`)になり、`Cache-Control: max-age=31536000`が返る
    - トップページ・両ランキング・プレイヤー情報・クラン情報・クラン検索(該当なし)がHTTP 200、存在しないタグは404
    - 2v2の対戦詳細の見出しが「自分/味方/相手」の順
    - Chromeで、トップページと対戦詳細が新しいCSSで描画されることを確認
    - `.bak`は、同日09:22(VMの時刻)に別セッションがデプロイしたjar

## フェーズ1.12: 多言語化の改修を引き継いで完了(2026-09-18)

- [x] **別セッションが作業途中で止めていた多言語化を引き継ぎ、ビルドが通る状態にして完了させた**(`8757316`・`75340bb`・`87682d1`)。
  - 発端は「中途半端になっている作業はないか」というユーザーの問い。調べたところ、作業ツリー(master)に未コミットの多言語化の改修が残っており、`mvn compile`が通らなかった。同じ内容は`wip/i18n`ブランチ(`286a159`、origin にもpush済み)に退避されており、作業ツリーの追跡ファイル・未追跡ファイルとも内容は一致していた(消失は無し)。
  - **止まっていた原因**: 日時表示を`TimeView`に変える途中で、呼び出し側の`ViewMapper`だけ直し、受け側のレコード(`BattleDetailView`・`BattleSummaryView`・`ClanMemberView`)が古い引数のままだった。`messages*.properties`とテンプレートは1行も手が付いていなかった。
  - 対応した多言語化TODOの番号と内容は`TODO.md`の表を参照(1〜7・9・11・13〜15・21)。
  - **訳語は公式情報で裏取りした**。`Ladder`に付いていた「ランク戦」は誤りで、公式の日本語表記は「トロフィー目標」。「ランク戦」は旧パス・オブ・レジェンド(旧「伝説の道」)の現在の名前(2025年6月の公式アップデート告知で確認)。カード名も`浪人スサノオ`(Ronin)・`ボスアサシン`(Boss Bandit)・`ダイナマイトゴブリン`(Goblin Demolisher)・`ゴブリンの呪い`(Goblin Curse)・`スピリットエンプレス`(Spirit Empress)を裏取りして追加した。
  - **日時はブラウザ側で変換する**。閲覧者のタイムゾーンはサーバーからは分からない(Accept-Languageは言語であって居場所ではない)ため、`<time datetime>`にUTCのISO値を入れ、`Intl.DateTimeFormat`で書き換える。スクリプトが動かない場合はサーバーが入れたUTC表記が残る。クランの最終アクセスだけは「3日前」の相対表記を残したいので、書き換え対象を`data-localtime`付きに限定した。
  - **単複はメッセージ側に寄せた**。ICU MessageFormatを`MessageSource`に組み込み(`IcuMessageSource`)、`{0, plural, one{# crown} other{# crowns}}`と書けるようにした。「1日前」と「N日前」のテンプレート分岐(英語の規則がテンプレートに埋まっていた)も1キーに統合して消した。
  - 実描画で確認した内容(ポート18080、Chrome):
    - 日本語UIでUTC 20:57が日本時間 5:57 に変換される(`Asia/Tokyo`)
    - 英語UIの`Sep 18, 2026, 5:57 AM`、注記の`Note: `、モード名`Rank Battle`
    - 国選択の並びが五十音順になり、日本が254件中158番目(以前はほぼ末尾)。香港が短縮名
    - 横スクロールが発生していないこと(`scrollWidth == clientWidth`)
  - 1件のときの英語表記(`1 crown`・`1 day ago`)は実データに稀にしか出ず画面では確認できないため、`IcuMessageSourceTest`で検証した(テスト113件)。
  - **ハマりポイント**: 既存の`LabelResolverTest`が、廃止した仕様(「ランク戦 (Ladder)」のような英語名併記と、辞書に無い場合の内部ID表示)を検証したまま残っていた。仕様変更のときは、その仕様を書いたテストも一緒に直す必要がある。
  - [x] **本番デプロイ・動作確認済み**(2026-09-18、`1081519`時点)。jarはICU4Jを同梱するため24.8MB→40.0MBに増えた。
    - 本番で確認した内容(curlによるHTML確認):
      - 対戦履歴の日時が`<time datetime="2026-09-17T20:57:04Z">`になり、モード名が「ランク戦」(内部IDではない)
      - 英語UIの注記が`Note: `で始まる
      - 国選択が五十音順で、日本が254件中159番目。香港が短縮名
      - `/`・`/ranking`・`/ranking/players`・プレイヤー情報・クラン検索がHTTP 200、存在しないタグは404
      - **`?lang=a.b`が500から200になった**(多言語化TODO 3の修正が効いている)
    - **ブラウザでの実描画は本番では未確認**(Chrome拡張が切断されたため)。同じコードのローカルでの実描画は確認済み。
    - `.bak`は、前日09:31にデプロイしたjar。

## フェーズ1.13: 多言語化の残り(8・10・12・16〜20)を対応(2026-09-18)

- [x] **`TODO.md`の多言語化セクションで未対応だった8件と、保留していた判断事項を片付けた**。これでこの節は全21件が完了した。対応内容は`TODO.md`の表を参照。
  - **ユーザー判断が要った2件**(選択肢形式で確認):
    - 候補20(国を推定できないときの既定): 「表示言語の代表的な国を既定にする」を採用。`CountryPreference#resolve`に表示ロケールを渡し、CLDRのlikely subtags(ja→JP、en→US)で決める。それも国一覧に無ければ従来どおり日本。
    - ゲームモード名の前方一致: 採用。`gamemodeprefix.*`の辞書を「名前の完全一致」と「`type`」の間に挟み、最初の`_`までを引く。`Challenge_AllCards_EventDeck_NoSet`が「通常バトル」ではなく「チャレンジ」になる。
  - **`<title>`や見出しには`<bdi>`を置けない**(候補16)。要素を入れられない箇所は、メッセージ側にUnicodeの分離文字(FSI U+2068 / PDI U+2069)を書いて名前を囲んだ(`title.format`・`battle.deck.*`・`clanSearch.heading`)。表やリンクの中の名前は`<bdi>`で囲んだ。
    - あわせて、デッキ見出しの全角括弧がテンプレートに残っていたのをメッセージ側に移した(候補7の積み残し)。英語UIでは`Your deck (名前)`になる。
  - **hreflangは絶対URLでないと解釈されない**(候補12)。`GlobalModelAttributes`に`siteBaseUrl`を足し、`<link rel="alternate" hreflang="ja/en/x-default">`を`head`フラグメントに置いた。`Vary`は先行して対応済みだった。
  - **エラー画面の言語リンク**(候補10)は`RequestDispatcher.ERROR_REQUEST_URI`で転送前のパスに差し替えた。`ERROR_REQUEST_QUERY_STRING`という定数は存在しない(サーブレット仕様のエラー属性に無い)ので、クエリは`getQueryString()`のまま使う(エラー転送でも元のリクエストの値が残る)。
  - **件数のハードコード**(候補18)は`{0}`にして、`HomeController`/`RankingController`の定数から渡す。そのために`RankingService.RANKING_LIMIT`を`CLAN_RANKING_SIZE`として公開した。
  - 注記に残っていた「パス・オブ・レジェンド」を、現行名の「ランク戦」に統一した(候補1で辞書側は直っていたが、文言が追随していなかった)。
  - **ハマりポイント**: `messages*.properties`に`⁨`のエスケープ表記で書いても、保存時に実体の文字に変換される。不可視文字がそのまま入るため、何が入っているかはコメントで補った。
  - 実測(ローカルのポート18080): 桁区切り(`140,000`・`12,265`)、hreflang3件、エラー画面の言語リンクが`/nonexistent?x=1&lang=ja`、Accept-Language無しで`?lang=en`のとき国別タブが「United States」、デッキ見出しが`自分のデッキ(名前)`。
    - Chromeの実描画で、クラン名検索の見出し「「مقهى الحظ ☕」の検索結果」の末尾の絵文字が閉じ引用符の外に回り込まないことを確認した(候補16の効果)。
  - テスト116件成功(接頭辞の分類とエラー転送時のURIのテストを追加)。
  - [x] **本番デプロイ・動作確認済み**(2026-09-18、`e00b039`)。jarはコミット済みの内容を`git archive`で展開してビルドした(作業ツリーに別セッションの作業中コードが混ざっていたため)。
    - 本番で確認: hreflang3件、注記の件数、桁区切り(`140,000`・`12,265`)、エラー画面の言語リンクが`/nonexistent?x=1&lang=ja`、Accept-Language無しで`?lang=en`のとき国別タブが「United States」、デッキ見出しの分離文字。
  - [x] **デプロイ後に見つけた回帰を修正**(`78d637f`)。**存在しないタグで500になった**。
    - 原因: `@ExceptionHandler`には`@ControllerAdvice`の`@ModelAttribute`が適用されない。そのため`GlobalExceptionHandler`が描くエラー画面では`currentUri`/`siteBaseUrl`がnullで、今回足したhreflangの`siteBaseUrl + currentUri`がnull同士の連結になり`SpelEvaluationException`で落ちた。
    - サーブレットコンテナのエラー処理(存在しないURL)を通る経路では属性が入るため、ローカルの`/nonexistent`の確認では再現しなかった。**同じ「エラー画面」でも到達経路が2つある**点がハマりポイント。
    - 対応: `GlobalExceptionHandler`が`GlobalModelAttributes`を使って自分で属性を詰める。副次的に、この経路のエラー画面の言語切替リンクが今まで`/`に飛んでいたのも直った。テンプレート側も属性がnullならhreflangを出さないようにした。
    - 実測: `/player/ZZZZZZZZZ`・`/player/2QQQQQQQQ`が500→404。テスト117件成功。

## フェーズ1.14: プレイヤー情報画面に「使用中のデッキ」を追加(2026-09-18)

- ユーザー依頼により、直近の1vs1の対戦で使ったデッキを「使用中のデッキ」として表示(コミット`934942a`)。基本情報の直下に置き、注記で「最近の1vs1で使用したデッキであり、ゲーム内の現在のデッキとは異なる場合がある」旨を明記した。
  - 対象は、両陣営とも1人で、自分のデッキが8枚そろっている直近の対戦(2v2と、ボートバトルの防衛側のような8枚未満の対戦を除く)。フレンドバトル・チャレンジ・ドラフト系も対象に入るため、元の対戦のモード・日時と対戦詳細画面へのリンクを添えて判断できるようにした。
  - 4列×2行で表示し、幅の上限は560pxとした。8列ではPC幅でもカード名が折り返し、480pxでも「アイススピリット」などが折り返したため。
- **並行作業での注意点**: `player.html`と`messages*.properties`の変更は、同時に作業していた別セッションのコミット`e00b039`に先に入った(同じファイルを編集していたため)。Java側は`934942a`で後から揃えた。
- テスト119件成功。本番デプロイ後、`/player/UJ2JR02V0`でデッキ8枚の表示を確認した。

## フェーズ1.14: カードレベルの表示がレアリティごとにずれていたのを修正(2026-09-18)

- [x] **公式APIの`level`を生のまま表示していたため、カードレベルが実際より低く・レアリティごとにばらばらに見えていた**(ユーザーからの指摘)。
  - **原因**: 公式APIの`level`はレアリティごとに1から数え直した値で、`maxLevel`もレアリティごとに違う。実測(`/cards`、通常123枚+タワーユニット4枚)では コモン16・レア14・エピック11・レジェンダリー8・チャンピオン6。
    - そのため、同じ強さのデッキでもレジェンダリーだけ`Lv.3`、チャンピオンだけ`Lv.1`のように見え、**上位プレイヤーほど低レベルに見える**という指摘どおりの症状になる。
  - **対応**: `domain/CardLevel`を追加し、`level + (16 - maxLevel)`でゲーム内表記に直す。16はコモンの`maxLevel`(ゲーム内の上限)。上限の引き上げなど想定外の値では、誤った数字を出すより生値のままにする。
    - `BattleLogEntry.Card`に`maxLevel`を追加。カードを組み立てるのは`ViewMapper#toCards`の1箇所だけなので、対戦詳細・対戦履歴・使用中のデッキがまとめて直る。
  - **実測**: 指摘のあった`/player/2CLV2RP0/battles/20260916T195949.000Z`(2vs2)で、修正前は`11/1/9/11/9/11/6/3`とばらついていたものが、**36枚すべてLv.11**に揃った。2vs2はカードレベルが統一される仕様なので、これが正しい。トロフィー目標の対戦では上限まで育てたデッキが`Lv.16`になることも確認した。
  - これは今回の多言語化とは無関係の、以前からあった不具合。テスト120件成功。

## フェーズ1.15: 個人ランキングの表示件数を増やす(2026-09-18)

- [x] **ユーザーの指示で、トップページの個人ランキングを10人→100人、個人ランキング画面を200人→1000人にした**。
  - 1000件は公式APIが返せる上限。実測でグローバル・日本とも`limit=1000`で1000件返ることを確認した(`RankingService.MAX_PLAYER_RANKING_SIZE`を1000に)。
  - 件数は候補18の対応でメッセージの`{0}`に渡しているため、注記の「上位1,000人」も定数から自動で追随する。文言の直し漏れは起きない。
  - **タブは両方を同時に描く**(グローバル+国別)ので、画面の行数は件数の2倍になる。ローカル実測:
    - トップページ: 200行・144KB・キャッシュ有0.11秒
    - 個人ランキング画面: 2000行・**1,593KB**・キャッシュ無2.64秒 / キャッシュ有0.88秒
  - 2026-09-17に「トップページが縦に長すぎる」として10人に絞った経緯があるが(`TODO.md`の画面デザイン候補1)、今回ユーザーの指示で100人に戻した。
- [x] **本番が遅くなったので、レスポンス圧縮(gzip)を有効にした**。
  - デプロイ直後の実測で、個人ランキング画面がキャッシュ有でも7.5秒かかった。切り分けると**TTFBは0.04秒**で、遅さの原因はサーバーの描画ではなく**1.63MBの転送**だった。Spring Bootは既定で`server.compression`が無効。
  - `server.compression.enabled: true`を追加。1,631,159バイト → **105,823バイト**(約15分の1)になった。
  - 教訓: 「遅い」ときは`%{time_starttransfer}`と`%{time_total}`を分けて測ると、描画の問題か転送の問題かがすぐ分かる。
- [x] **圧縮後も個人ランキング画面が3〜6秒かかるため、タブを切り替えたときに表を取りに行く方式にした**(ユーザーが選択)。
  - `rankingTabs`フラグメントに`lazyUrl`引数を足し、渡された画面では**開いているタブの表だけ**を描く。もう一方は`.ranking-slot`に`data-lazy-url`だけを置き、タブを開いた時に`fetch`して差し込む(取得済みなら再取得しない)。
  - 表だけを返す`GET /ranking/players/table?country=XX`を追加。ビューは`fragments/layout :: playerRankingTable(rows=..., detailed=true)`を直接返す。
  - トップページ(100人)とクランランキング(10件)は軽いので`lazyUrl`にnullを渡し、**今までどおり両方描く**(タブ切替で通信を挟まない)。
  - **ハマりポイント2点**:
    - ビューにフラグメントを指定するときの引数は**名前付きでないと**`IllegalArgumentException: Parameters in a view specification must be named`になる。
    - 「どちらを先に描くか」はテンプレートの`localTabActive`と揃える必要がある。最初`selected.isPresent()`で判定したため、国は決まっているがタブはグローバル、という状態で**両方空になった**。`RankingScope.resolve`が国と`localTabActive`を`Scope`レコードで返すようにして揃えた。
  - 実測(ローカル): 個人ランキング画面のHTMLが1,593KB→**828KB**、行数2000→1000。断片の取得は0.35〜0.48秒。Chromeでタブを切り替えて日本のランキングが取得・描画されることを確認した。

## フェーズ1.15: プレイヤー名検索に向けたタグ・名前の蓄積を開始(2026-09-18)

- ユーザー依頼で、名前検索の仕様案を`プレイヤー名検索（検討中）.md`にまとめ、まず蓄積だけ始めた(コミット`f463b58`)。DBは使わずテキストファイルに追記する。
  - プレイヤー情報・クラン情報・個人ランキングで公式APIから得たタグと名前を、`inbox/`に1時間ごとのTSVで追記する。書き込み処理は新設の`store`パッケージ(`PlayerSightingLog`)に置き、Service層から呼ぶ。
  - ランキングは毎回同じ1000人を返すため、最近書いた組はCaffeineで覚えて書かない。書き込みに失敗しても画面は表示する(ログに警告だけ出す)。
- 本番の保存先: `/var/lib/clash-royale-api/player-index/`(opc所有)を作成し、`/etc/clash-royale-api/env`に`PLAYER_INDEX_DIR`を1行追記した。デプロイ直後に1983件記録されたことを確認した。
- **並行作業での注意点**: 別セッションの作業途中の変更が作業ツリーに残っていたため、`git archive HEAD`でコミット済みの内容だけをscratchpadに取り出してビルドしたjarをデプロイした(作業ツリーのままビルドすると、未コミットの変更まで本番に出てしまうため)。
- テスト130件成功(`PlayerSightingLogTest`6件、対戦の参加者を記録するテスト1件を追加)。

## フェーズ1.16: カード詳細画面(2026-09-19)

- [x] **カード詳細画面(`/card/{id}`)を追加し、各画面のカード画像からリンクした**。
  - 公式APIにカードを1枚だけ取るエンドポイントは無いため、`/cards`の一覧(通常のカード`items`とタワーユニット`supportItems`)をキャッシュ`cards`に載せ、`CardService#byId`でIDから探す。存在しないIDは`CardNotFoundException`で404。
  - 公式APIが返すのは名前・画像(進化画像を含む)・レアリティ・エリクサー・`maxLevel`だけで、**説明文やHP・ダメージなどのステータスは無い**。画面にもその旨の注記を出している。
  - リンクのため、`BattleLogEntry.Card`・`CardView`・`CardPerformanceView`・`PlayerBattleStats.CardPerformance`にカードIDを追加。リンクは共通フラグメント`cardLink`(`aria-label`は「〇〇の詳細」、画像のaltは空)。
  - レアリティの訳語(ノーマル/レア/スーパーレア/ウルトラレア/チャンピオン)は、ファミ通・Wikipedia・攻略サイトの表記と一致することを確認した(Supercell公式サポートは本文を取得できず、「チャンピオン」のみ記事タイトルで確認)。
- **表示確認で見つけて直した不具合2点**:
  - 「最大レベル」にAPIの`maxLevel`をそのまま出していたため、ウルトラレアが「8」、チャンピオンが「6」になっていた。ゲーム内表記ではどのカードも上限16で情報が無いので、ユーザーの選択で**レベル範囲**(`CardLevel.inGame(1, maxLevel)`〜`inGame(maxLevel, maxLevel)`、例: ウルトラレア「9〜16」)に変えた。
  - `style.css`のスマホ向けメディアクエリで、`.deck-panel .deck-card .card-name {`の行の途中にリンク用のCSSブロックが丸ごと挟まっていた(置換のずれ)。スマホ幅でカード名の縮小がデッキ以外にも効き、ルールも二重になっていたため、元の1行に戻した。
- **確認**: ローカルで日英・進化あり/なし・タワーユニット・チャンピオン・存在しないID(404)・不正なID(400)を確認。プレイヤー情報画面と対戦詳細画面から張られたカードリンク20種類がすべて200で開けることを確認し、Chromeで実描画(リンクからの遷移、広い幅での画像2枚+ステータスの横並び、500px幅での縦積み)も確認した。
  - ハマりポイント: CSSOMでルールを数えるとき、最近のChromeは通常の`CSSStyleRule`も(CSSネスティング対応で)空の`cssRules`を持つ。「`cssRules`があれば展開」という書き方だと通常ルールが全部消え、「CSSが古いのでは」と誤認しかける。`selectorText`の有無で先に判定すること。
- [x] **本番デプロイ・動作確認済み**(2026-09-19、`7b88639`)。ナイト(進化あり、1〜16)・プリンセス(9〜16)・タワープリンセス(英語)・存在しないID(404)と、プレイヤー情報画面のカードリンクを確認した。
  - 最初のscpが`Connection closed`で失敗し、SSH・HTTPとも無応答になった(VMフリーズ3回目)。ユーザーにOCIコンソールから再起動してもらい、**アプリの起動完了(HTTP 200)を待ってから**転送し直したところ成功した。起動直後はload averageが3.7前後と高く、そこへ40MBの転送を重ねるのは避けた方がよい。

- [x] **カード詳細画面の戻るリンクを、開いた元の画面に戻るようにした**(ユーザー依頼)。
  - カードへのリンクに`?player=タグ&battle=対戦日時`を付け、詳細画面で「対戦詳細に戻る」「プレイヤー情報に戻る」を出し分ける。どちらも無ければ「トップへ戻る」。
  - **戻り先のURLそのもの(`?from=/player/...`)は受け取らない**。オープンリダイレクトの足がかりになるため。タグは`Tags.looksLikeTag`、対戦日時は`\d{8}T\d{6}\.\d{3}Z`で検査し、合わない値は捨てる(`CardOrigin`)。`Referer`や`history.back()`は、直接アクセス時に戻り先が不定になるので採らなかった。(同日、ユーザー依頼で全画面を`history.back()`方式に変更した。直接アクセス時のフォールバックとして`CardOrigin`は残っている。下記参照)
  - ハマりポイント: Thymeleafの`@{...(battle=${null})}`は、パラメータを省かずに**空の`battle=`を出す**。URLは`CardOrigin#cardPath`で`UriComponentsBuilder.queryParamIfPresent`を使って組み立て、テンプレートは`@{${origin.cardPath(id)}}`とした。
  - 各画面のフラグメント(`deckGrid`/`performanceGrid`/`supportCardRow`/`cardLink`)は引数に`origin`を取る。Controllerが`cardOrigin`をModelに詰める。
  - 本番デプロイ・動作確認済み(2026-09-19、`64b618b`)。4パターン(対戦詳細から・プレイヤー情報から・直接・不正なタグ)の戻り先を確認した。

- [x] **全画面の戻るリンクを「ひとつ前の画面に戻る」にした**(ユーザー依頼。方式はユーザーが選択)。
  - 経緯: ほとんどの画面の戻るリンクが「トップへ戻る」固定だった。サーバー側で`Referer`から戻り先を決める案は、戻るリンクを押すこと自体が新しい遷移になるため、戻った先で「さっきの画面に戻る」が出て**2画面の往復**になる。そのため採らなかった。
  - 実装(`fragments/layout.html`の`head`、各テンプレートは変更なし): 動作は`history.back()`、文言は`document.referrer`のパスから画面名を判定する(`action.backTo*`)。履歴が無いとき(`history.length < 2`や直接アクセス)は、サーバーが出した従来のリンクのまま。`CardOrigin`は当初フォールバックとして残したが、同日ユーザーの指示で撤去した(下記)。
  - **同じ画面の中の遷移は履歴を積まない**: パスが同じでクエリだけ違うリンク・GETフォーム(言語切替・並べ替え・国選択)は`location.replace`にした。積むと「戻る」が同じ画面の切替前に戻るため。置き換えた後は`referrer`が自分自身になるので、画面に入ってきたときの直前のURLを`sessionStorage`の`backFrom:<パス>`に覚えておく。
  - **言語切替後に戻ると旧言語で出る問題**: 戻った先はbfcache(ブラウザが戻る操作のためにページをメモリに残す仕組み)やHTTPキャッシュから復元されるため、途中で英語に切り替えても日本語のまま表示された。最後の言語を`sessionStorage`に覚え、`pageshow`(persisted)・`back_forward`の読み込みで食い違っていたら**`lang`パラメータを外して**開き直す。外さないと、`?lang=ja`付きの履歴URLが再び日本語を指定してしまう(実際に一度そうなった)。
  - **ハマりポイント**: `th:inline="javascript"`のスクリプト内では、`[/`がThymeleafのテキスト記法(閉じ要素)と解釈され、`[/^\/$/, 'top']`のような「配列の先頭が正規表現」で`TextParseException`になる。**`mvn test`は通るのに全画面が500になった**(テンプレートの描画はテスト対象外)。オブジェクト(`{pattern: /.../, screen: '...'}`)に書き換えて回避した。テンプレートを触ったら、curlで全画面のステータスを確かめること。
  - 確認: Chromeで「トップ→プレイヤー→クラン(並べ替え・英語へ切替)→戻る→戻る」「個人ランキング直接(国をUSに)→プレイヤー→対戦詳細→カード→戻る×3」をたどり、文言・戻り先・履歴の数・言語・国選択が正しいことを確認した。
  - 本番デプロイ・動作確認済み(2026-09-19、`e3c9e89`)。全画面のステータスと、本番のChromeで「トップ→プレイヤー→クラン(並べ替え)→戻る」を確認した。

- [x] **`CardOrigin`を撤去した**(ユーザー指示)。履歴方式で「対戦詳細に戻る」などが出るようになり、役割は直接アクセス時のフォールバックだけになったため。
  - カードへのリンクは`/card/{id}`だけに戻り、フラグメントの`origin`引数・Controllerの`cardOrigin`も削除した。カード詳細画面の履歴が無いときの戻るリンクは「トップへ戻る」。以前の`?player=`付きURLも、パラメータを無視して表示できる。
  - `action.backToBattle`などのメッセージは、戻るリンクのスクリプトが使うので残している。
  - 本番デプロイ・動作確認済み(2026-09-19、`e173597`)。

## フェーズ1.17: デッキの平均エリクサー・4枚サイクル・コピーボタン(2026-09-19)

RoyaleAPIのデッキページ(`/decks/popular`)と比べた改善提案のうち、ユーザーが1と2を採用した。

- [x] **プレイヤー情報画面の「使用中のデッキ」と、対戦詳細画面の各デッキに、平均エリクサー・4枚サイクル・「デッキをコピー」ボタンを付けた**。
  - 計算とリンクの組み立ては`domain/Deck`。ViewMapperが`DeckMetaView`を作り、共通フラグメント`deckMeta`で表示する。
  - エリクサーは対戦履歴のカードに含まれる`elixirCost`を使う(`BattleLogEntry.Card`に追加)。カード一覧APIを別に呼ぶ必要は無い。
  - **8枚そろっていない、またはエリクサーが無いカードを含むデッキでは、指標を出さない**。一部のカードだけで計算した値は、ゲーム内の数字と食い違うため。
  - **コピー用リンクの形式は、公式の文書が見つからなかった**。RoyaleAPIのコピーボタンのリンクをChromeで取り出して合わせた: `https://link.clashroyale.com/en?clashroyale://copyDeck?deck=ID;…;ID&tt=タワーユニットID&l=Royals`。`l=Royals`の意味は不明で、そのまま合わせている。カードの並び順がスロット順(先頭が進化・ヒーロー枠)になるので、対戦履歴の順を保つ。
  - ハマりポイント: RoyaleAPIのページはWebFetchでは403になる。Chromeで開けば読める。ただし`javascript_tool`の戻り値に含まれるURLのクエリ文字列は、ツールが`[BLOCKED]`に伏せる。記号を置き換えて取り出した。
- **確認**: `mvn test`は全件成功(`DeckTest`を追加)。ローカルのChromeで、プレイヤー情報画面と対戦詳細画面の実描画、スマホ幅での折り返しを確認した。相手デッキの平均4.4は、元データ(ネクロマンサー=Witchが5コスト)と一致した。
  - **未確認**: 実機のスマホで、ボタンからゲームが開いてデッキがコピーされるか。PCでは公式のリンクページが開くだけになる。
- [x] **本番デプロイ・動作確認済み**(2026-09-19、`354a89b`)。プレイヤー情報画面・対戦詳細画面にデッキの指標とコピーリンクが出ること、主要画面(トップ・ランキング2画面・カード詳細・英語表示)が200を返すことを確認した。
  - デプロイ前の確認で、VMがHTTP・SSHとも無応答だった(SSHは`Connection timed out during banner exchange`)。デプロイ作業を始める前から止まっていた。約10分待っても復帰しなかったため、ユーザーにOCIコンソールから再起動してもらった(VMフリーズ4回目。**デプロイと関係なく、アクセスが少ない時間帯にも止まる**ことが分かった)。
  - 再起動からアプリの応答(200)までは約3分かかった。転送はload averageが1.3前後に下がるのを待ってから行い、問題なく完了した。

## フェーズ1.18: VMのメモリ不足対策(2026-09-19)

A1.Flexへの乗り換えを再挑戦したが"Out of host capacity"で作成できず、ユーザー判断で現行VMのまま対策することにした。

- **調査結果**: VM.Standard.E2.1.Microは1GBのはずが、OSから見えるメモリ(`MemTotal`)は約500MBだった。起動オプションの`crashkernel=1G-64G:448M`で、kdump(カーネルクラッシュ時のダンプ採取)用に448MBが予約されていたため。さらに`dnf-makecache.timer`が定期的にパッケージ情報を更新しており、1回で約330MBを使う。2026-09-18 23:29にはこれがOOM Killerに止められていた。swapへの退避が一気に起きることがフリーズの有力な原因と考えている。Java自体のRSSは約115MBで、アプリ側は原因ではなかった。
- [x] `dnf-makecache.timer`を無効化した(`systemctl disable --now`)。パッケージの更新は手動の`dnf update`で行う。
- [x] kdumpを無効化し、`grubby --update-kernel=ALL --remove-args="crashkernel"`で全カーネルの起動オプションから削除して再起動した。`/etc/default/grub`・`/etc/kernel/cmdline`には指定が無いので、カーネルを更新しても予約は戻らない。
  - 戻し方: `grubby --update-kernel=ALL --args="crashkernel=1G-64G:448M,64G-:512M"`を実行し、kdumpを有効にしてから再起動する。
- **結果**: `MemTotal`が498MBから945MBになり、swapの使用量は314MBから0MBになった。再起動からHTTP 200までは約7分かかった。
- ハマりポイント: Claude Codeの自動承認モードでは、VMへの書き込みを伴うSSHコマンドは許可を得ても止められることがある。特に`reboot`はユーザーに`!`付きで実行してもらった。また、`!`で実行されるのはbashなので、`$env:USERPROFILE`は使えない(`~/.ssh/...`と書く)。

## フェーズ1.19: 使用中のデッキを実データに、連勝・ランク戦成績、カード一覧画面(2026-09-19)

`TODO.md`の機能面の改善候補B-5・追加案(連勝・ランク戦成績)と、ユーザー依頼のカード一覧画面(C-7に近いもの)。

- [x] **使用中のデッキを、直近の1vs1からの推定から、ゲーム内で今セットしているデッキ(`/players/{tag}`の`currentDeck`・`currentDeckSupportCards`)に置き換えた**。対戦していないプレイヤーでも出せるようになり、対戦履歴の取得に失敗しても表示できる。
  - カードは対戦履歴と同じ形なので`BattleLogEntry.Card`を共用した。`PlayerService.latestOneOnOne`と「使用した対戦」のリンク・注記は削除。
  - 並び順はゲーム内のスロット順のまま(コピー用リンクがこの順を使う)。
- [x] **プレイヤー情報画面に連勝・連敗(`currentWinLoseStreak`)と、ランク戦の今シーズン・最高シーズンの順位とレーティングを追加**。
  - 連勝は正、連敗は負の数。実データ3人分を対戦履歴と突き合わせて確認した。**ただし1人は対戦履歴の並びと一致しなかった**(ランク戦・クラン対戦が混ざっていた人)。どの対戦を数えているかは公表されていないので、画面に注記を出している。
  - ランク戦の成績は、上位の順位に入っていないプレイヤーでは`rank`がnull・`trophies`が0になり、`leagueNumber`しか意味のある値が無い。**リーグ番号に対応する公式のリーグ名は裏付けが取れていないため、出していない**。順位があるときだけ表示する。
- [x] **カード一覧画面(`/cards`)を追加**。ヘッダーのメニュー「カード」から開ける。
  - レアリティごと(ノーマル→チャンピオン)にエリクサーの軽い順で並べ、タワーユニットは最後にまとめる。
  - 名前での絞り込みはブラウザ内で行う(全127枚を一度に出しているため)。日本語名・英語名の両方で引ける。ひらがなはカタカナに寄せて比べ、NFKCで全角・半角の違いも吸収する(「ないと」でナイト・メガナイト・ゴールドナイトが出る)。
  - カード詳細画面を直接開いたときの戻り先を、トップページからカード一覧に変えた。
  - ハマりポイント: エリクサーが無いのはタワーユニットだけではなく、**鏡(Mirror)にも無い**。そのため「エリクサー無し=タワーユニット」とは判定できない。`getCards()`が`items`と`supportItems`を連結していたのをやめ、分けたまま返すようにした。
  - ハマりポイント: 絞り込みで1枚だけ残ると、`.deck-grid`の`auto-fit`が空いた列を詰め、1枚が横幅いっぱいに伸びた。一覧だけ`auto-fill`にした。また、CSSのファイル名のハッシュは起動時に決まるので、CSSを変えたらアプリの再起動が必要。
- **確認**: `mvn test`は全件成功(`WinLoseStreakTest`・`CardServiceTest`を追加し、`latestOneOnOne`のテストを削除)。ローカルのChromeで、カード一覧(絞り込みを含む)とプレイヤー情報画面の実描画を確認した。平均エリクサー2.1は、元データのデッキ(3,2,3,2,2,1,1,3)の平均と一致した。
- [x] **本番デプロイ・動作確認済み**(2026-09-19、`c16b506`)。カード一覧(127枚)・プレイヤー情報画面の連勝とランク戦の順位が出ること、主要画面(トップ・ランキング2画面・カード詳細・英語表示)が200を返すことを確認した。VMは応答しており、フリーズは起きなかった。

## 方針変更: 日本特化から全世界向けへ(2026-09-19)

- [x] 2026-09-18に決めた「日本のクラロワユーザーに特化する」方針を、ユーザーの判断で撤回した。今後は全世界のユーザーに向けて発信する。
  - きっかけ: 「イタリア人が来たら日本語で表示されるのか」という質問。実際は、対応していない言語の訪問者には英語を出す実装になっている(`SupportedLanguages.INTERNATIONAL`)。
  - RoyaleAPIとデータ量・基盤の規模で張り合わない判断は、基盤の制約によるものなので残した。
  - コード側で日本を前提にしている箇所(言語・国の既定値など)は`TODO.md`の「方針変更で見直しが必要な項目」に洗い出した。まだコードは変えていない。

## フェーズ1.20: スペイン語・ポルトガル語に対応(2026-09-19)

- [x] **表示言語にスペイン語(`es`)とポルトガル語(`pt`)を追加**。ダウンロード数上位のブラジル・メキシコなどを想定した。ヒンディー語も一度依頼されたが、ゲーム自体がヒンディー語に対応しておらず(ゲーム内の対応言語は14言語)公式の訳語が無いため、ユーザーの判断で取り下げた。
  - 言語切替リンク・hreflangは`SupportedLanguages.languageCodes()`からループで作るようにした。以前はテンプレートに`ja`/`en`を直書きし、「日本語以外なら英語が選択中」という2言語前提の判定になっていた。
  - ポルトガル語はゲーム内と同じブラジル向けの訳。pt-PTの訪問者にも、英語より読みやすいので`pt`を出す。
  - カード一覧の絞り込みは、アクセント記号を省いても当たるようにした(「valquiria」→Valquíria)。外すのはU+0300〜U+036Fの結合記号だけなので、日本語の濁点(U+3099)は影響を受けない。
- [x] **カード名127枚は公式の表記で裏付けた**。公式APIは`Accept-Language`を付けても英語名しか返さないことを確認した。
  - 110枚: ゲームのAPKから取り出されたテキストデータ(GitHubの`smlbiobot/cr-csv`の`texts.csv`、2023年8月時点)。同じ表の日本語列が既存の日本語辞書と一致することで、データの正しさも確かめた。
  - 2023年以降の新カード17枚: Supercell公式ブログのスペイン語版・ポルトガル語版(`supercell.com/en/games/clashroyale/es/blog/`、`/pt/blog/`)の全記事(各125件前後)から名前を拾った。
  - 2023年の名前がその後改名されていないかも、公式ブログの表記と突き合わせて確認した。ブログに出てこなかったのはNight Witch・Flying Machineの2枚だけで、この2枚はゲームデータの表記を使っている。
  - 役職・レアリティ・ゲームモードなどの用語もゲームデータと公式ブログに合わせた。例: スペイン語ではデッキは「mazo」、レアリティは「calidad」、レアは「Especial」。ランク戦はスペイン語で「Modo competitivo」、ポルトガル語で「Ranqueado」(どちらも公式ブログの2025年改名後の表記)。
  - **既知の制約**: カード名以外の画面の文言(説明文・注記など)はClaudeによる翻訳で、ネイティブのチェックは受けていない。
- テスト: `MessagesConsistencyTest`を、対応言語すべてについて英語とキーが揃っているかを見るように一般化した。英語以外の対応言語がすべてのカード名を持つことも確認するようにした。`mvn test`は全件成功。
- 確認: ローカルで全画面(トップ・カード一覧・カード詳細・ランキング2画面・プレイヤー・クラン・対戦詳細・クラン名検索・404)を4言語で開き、すべて期待どおりのステータスで返り、訳の抜け(`??キー??`)が無いことを確認した。Chromeで、ポルトガル語のプレイヤー情報画面の描画と、幅375pxで言語リンク4つが1行に収まり横スクロールが出ないことを確認した。
- [x] **本番デプロイ・動作確認済み**(2026-09-19、`f4e4bcd`)。Accept-Languageがpt-BR→ポルトガル語、es-MX→スペイン語、it-IT→英語、ja→日本語になること、西・葡で主要画面が200を返し、カード名が公式の表記で出ることを確認した。

## フェーズ1.21: 言語・国の既定値から日本の前提を外す(2026-09-19)

`TODO.md`の「方針変更で見直しが必要な項目」の残り2つ。

- [x] **Accept-Languageが無い・壊れているときの表示言語を、日本語から英語にした**。`SupportedLanguages.DEFAULT`(日本語)を廃止し、対応外の言語と同じ`INTERNATIONAL`(英語)に一本化した。検索エンジンのクローラーの多くはAccept-Languageを送らないため、以前は検索結果に日本語の画面が載りやすかった。
- [x] **国を推定できないときの最後の砦を、日本から「既定言語(英語)の代表国=米国」にした**。米国も国一覧に無ければ一覧の先頭にする。
  - 選択なし(空)にしなかった理由: 国が決まらないと国別タブと国の選択欄が丸ごと描かれず、利用者が国を選び直す手段が無くなるため。
  - 手前に「表示言語の代表的な国」(ja→JP、es→ES、pt→BRなど)があるので、実際にここまで来るのはまれ。
- テスト: `SupportedLanguagesTest`・`CountryPreferenceTest`の期待値を更新し、「国一覧の先頭」のケースを追加。`mvn test`は全件成功。

## フェーズ1.22: 平均カードレベル(2026-09-19)

ユーザー依頼。使用中のデッキと、対戦履歴の対戦相手に平均カードレベルを出す。

- [x] **デッキの指標(`DeckMetaView`)に平均カードレベルを追加**。使用中のデッキと対戦詳細画面の各デッキに出る(同じ`deckMeta`フラグメントを使っているため)。
- [x] **対戦履歴一覧の対戦相手の名前の横に「平均Lv.14.3」を表示**。2v2では相手2人それぞれに出す。相手用に`OpponentView`を新設し、味方(`PlayerLinkView`)には出していない。
  - 平均はゲーム内表記のレベル(`CardLevel.inGame`)で求める。APIの生値はレアリティごとに1から数え直すため、そのまま平均すると意味のない数字になる。
  - タワーユニットは含めない(レベルの体系がカードと別のため)。8枚そろわないデッキでは出さない(平均エリクサーと同じ扱い)。
  - 名前の区切り(` / `)のCSSを`a + a`から`.name-list > * + *`に変えた。相手は名前と平均レベルを1つの`span`で包んでいるため。
- **既知の見え方**: 2v2などレベルが固定されるモードでは、APIが固定後のレベルを返すため平均Lv.11.0のようにそろう(実データで確認)。ランク戦の上位プレイヤーは全員16.0だった。
- 確認: `mvn test`は全件成功(`DeckTest`・`ViewMapperTest`に追加)。ローカルのChromeで使用中のデッキ・対戦履歴の実描画を確認し、4言語で表記と小数点(西・葡は`16,0`)が正しいことを確認した。
- [x] **本番デプロイ・動作確認済み**(2026-09-19、`b5b8802`)。プレイヤー情報画面に平均レベルと対戦相手の平均Lv.が出ること、主要画面(トップ・カード一覧・ランキング2画面・英語表示)が200を返すことを確認した。VMのフリーズは起きなかった。
- [x] **2v2では対戦相手の平均Lv.を出さないように変更**(2026-09-19、ユーザー依頼「どうせ大体Lv11だし」、`7e61f23`)。上の「既知の見え方」のとおり、2v2ではレベルがそろうため比べる意味が薄い。相手が2人以上いる対戦を2v2とみなし、`ViewMapper.toOpponents`で平均レベルをnullにする(ゲームモード名は`TeamVsTeam`・`CasualDuel2v2`など種類が多いため、人数で判定)。対戦詳細画面のデッキの平均レベルはそのまま。
  - 確認: 別セッションの作業途中の変更で作業ツリーのテストがコンパイルできなかったため、scratchpadのworktreeに差分だけ当てて`mvn test`(175件成功)・ローカル起動・Chromeでの描画確認を行い、そこからビルドしたjarを本番にデプロイした。本番でも2v2の16行は平均Lv.なし、1v1の14行はありを確認した。

## フェーズ1.23: 名前検索用のタグ巡回と整理バッチ、名前検索(2026-09-19、本番投入済み)

ユーザー依頼「タグ収集のバッチ化を検討して」から着手。検討内容・実測値の詳細は`プレイヤー名検索（検討中）.md`の「タグ収集のバッチ化」。

- [x] **巡回(`crawler`パッケージ)**: クラン対戦の履歴を主力に、5回に1回プレイヤーの対戦履歴を回す2段構え(クランに入っていない人を拾うため。ユーザーが選択)。アプリと同じプロセスで、Springの定期実行(fixed delay)で1回ずつAPIを呼ぶ。`CRAWLER_ENABLED`で有効化(既定は無効)。
  - 待ち行列はファイル(`crawler/clans`・`crawler/players`)で持ち、再起動しても続きから再開する。一度回ったタグは`visited/`に記録し、新しいタグが尽きるまで回り直さない。
  - **ハマりポイント**: `limit=1`(直近1回のクラン対戦)だと、2周目以降のクラン(=1周目のクランの対戦相手)は同じ対戦が返って新規が0人になった。`limit=10`で1回あたり約525人が新規。既定を10にした。
  - ローカル実測(1秒2回・20分×3回): 429は0回、メモリは約190MB。
- [x] **整理バッチ(`PlayerIndexCompactor`)**: inboxを`by-tag/`(1024ファイル、タグ順)にまとめ、処理済みのinboxを消す。毎日18:30 UTC(日本時間3:30)。同じinboxを2度取り込んでも結果が同じなので、途中で落ちても次回そのままやり直せる。今の時刻のinboxは追記中の可能性があるので対象外。
  - ユーザーが案A(整理バッチを先に作る)を選択。巡回を`limit=10`で回すと重複込みで1日2〜5GB書くため、本番投入の前提になる。
  - 実測(ローカル、ヒープ256MB): 68万行→23.3万人を約2.9秒。人数は`sort -u`の結果と一致。
- 定期実行の有効化は`SchedulingConfig`に集約。スレッドは既定の1本なので、整理バッチの実行中は巡回が止まる(意図どおり)。
- [x] **利用規約の確認**(2026-09-19): APIデータの保存・巡回を禁じる条文は無かった。代わりに、ファンコンテンツポリシーで必須の**非公式表示がサイトに無い**ことが分かった。詳細は`プレイヤー名検索（検討中）.md`の「利用規約」。
- [x] **全画面のフッターに非公式表示を追加**(`dfccca1`)。西・葡はポリシーの公式訳の文言、日本語は公式訳が無い(英語版へ転送される)ため英語の定型文を訳した。PC幅・スマホ幅(375px)で表示を確認。
  - `style.css`に別セッションの作業途中の変更(`overflow-wrap`)があったため、自分の差分だけを`git apply --cached`でステージした。デプロイも`git archive HEAD`から作ったjarで行った。
- [x] **本番デプロイ・巡回を有効化**(2026-09-19): `/etc/clash-royale-api/env`に`CRAWLER_ENABLED=true`・`CRAWLER_INTERVAL=10s`を追記(変更前の控えは`env.bak-20260919`)。止めるときは`CRAWLER_ENABLED=false`にして`systemctl restart`。整理バッチは毎日18:30 UTC(日本時間3:30)に動く。
  - 10秒に1回なら1日約8,600回。`limit=10`で重複込み1日数百MB程度を見込む。様子を見て間隔を詰める。
  - 巡回開始直後(03:17〜03:24 UTC)の実測: 約40回の呼び出しで24,083行(重複除去後23,557人)、1回あたり約630行・1行約40バイト → 1日約200MBの見込み。429・一時停止は0回。available 326MB。再起動をまたいでも待ち行列は続きから再開した(本番で確認)。
  - 画面由来(`inbox/yyyyMMdd-HH.tsv`)と巡回由来(`inbox/crawl-yyyyMMdd-HH.tsv`)は整理バッチで区別せずby-tagにまとめる設計。収集元はby-tagに残らない(残したくなったら初回整理前に列を足す必要がある、とユーザーに説明済み。回答待ち)。
- [x] **整理バッチの臨時実行(2026-09-19)**: ユーザー依頼「整理バッチを臨時で動かしたい」。巡回分も対象に含めるため04:05 UTC(日本時間13:05)に実行した(今の時刻のinboxは対象外になるため、03時台の巡回分を含めるには04時台に動かす必要があった)。
  - 方法: `/etc/clash-royale-api/env`に`PLAYER_INDEX_COMPACT_CRON=0 5 4 * * *`を**一時的に追記**して03:31 UTCに再起動。外から整理バッチを呼ぶ口は無いため。
  - 結果: `player index compacted: 14 inbox files, 212794 lines, 159807 players in updated by-tag files (5589 ms)`。21.3万行から**15.98万人**、所要5.6秒。`by-tag/`は1024ファイル・8.1MB(1人あたり約53バイト)。処理済みのinboxは消え、04時以降の分だけが残っていた。available 322MB、ディスクは30GB中6.6GB使用で余裕あり。
  - 実行後、envから`PLAYER_INDEX_COMPACT_CRON`の行を消して再起動済み(06:00 UTC)。整理バッチの実行は既定の毎日18:30 UTCのみに戻った。巡回も再開し、inboxが伸び続けていることを確認。起動は約25秒、available 365MB。
- [x] **名前の索引(by-name)**(2026-09-19): ユーザー依頼「(名前の索引と検索画面を)対応して」。整理バッチ(`PlayerIndexCompactor`)の中で、by-tagで変わった行だけを差分(旧名の削除・新名の追加)としてby-name(4096ファイル、正規化名順)に反映する。正規化と索引の読み出しは`PlayerNameIndex`に集約。
  - by-nameが無い(初回)か、前回by-nameに反映し終える前に落ちた(印のファイル`by-name.rebuild`が残っている)場合は、by-tagから全件作り直す。by-tagを書き換えた後に落ちると、次回はby-tagに変化が無く差分が出ないため。
  - 正規化のルールを変えたときは、by-nameを消せば次の整理バッチで作り直される。
  - 実測(ローカル、本番のby-tag 159,807人をコピーして): 全件の作り直しは6.9秒。同名が多いのは「clasher」278人・「alex」197人。
- [x] **プレイヤー名検索結果画面**(`/player/search`、2026-09-19): プレイヤー検索フォームで名前も受け付ける。`#`付きはタグ。タグに使われる文字(`0289PYLQGRJCUV`)だけの入力は、公式APIでタグとして見つからなければ名前として探す。名前は正規化後の完全一致で、最後に確認した日時の新しい順に50人まで表示。
  - 設定`player-index.name-search-enabled`(環境変数`PLAYER_NAME_SEARCH_ENABLED`)で切り替える。既定は無効で、無効の間は入力をすべてタグとして扱う(これまでどおり)。フォームの文言も有効時だけ「プレイヤー名またはタグ」になる。
  - 確認: ローカルで有効にし、大文字小文字・全角スペース・装飾タグ付きの名前・全角英字の名前・日本語名・`#`付き/なしの実在タグ・該当なしを確認。結果画面と該当なし画面をChromeで目視し、幅320・375px(日本語・ポルトガル語)で横はみ出しが0であることを実測した。
- [x] **本番デプロイ**(2026-09-19 06:16 UTC、`497b0a7`)。名前検索は無効のまま(`/player/search?q=alex`はこれまでどおり`/player/ALEX`へ転送、フォームの文言も従来どおりであることを確認)。本番にはまだby-nameが無いため、**今夜18:30 UTCの整理バッチで初めて全件から作られる**(ログに`by-name files rebuilt`と出る)。
- [x] **by-nameの初回作成(臨時実行)と名前検索の有効化**(2026-09-19): ユーザー依頼「(by-nameを)臨時で動かして」「プレイヤー検索有効にして」。
  - `/etc/clash-royale-api/env`に`PLAYER_INDEX_COMPACT_CRON=0 27 06 * * *`(一時)と`PLAYER_NAME_SEARCH_ENABLED=true`を追記して再起動(変更前の控えは`env.bak-20260919-byname`)。
  - 結果: `3 inbox files, 489741 lines, 539100 players in updated by-tag files, 4096 by-name files rebuilt (29897 ms)`。by-tagが**53.9万人**になり、by-nameを全件から作成(35MB)。
  - その後cronの行を消して再起動。名前検索は有効のまま。本番で名前検索(「alex」662人・「Clasher」996人・日本語名)・該当なし・#付き/なしのタグの転送を確認し、結果画面をChromeで目視した。再起動直後の最初の検索は約1秒、以降は0.1〜0.7秒。
- [x] **名前検索を前方一致に対応**(2026-09-19): ユーザー依頼「前方一致検索にすることはできる？」→ 決定「完全一致の人は全員表示、前方一致の人は完全一致が50人未満なら50人になるまで表示」「完全一致が多いときは50人ずつページ送り」「表示しきれない人がいれば、検索結果が多すぎるので一部だけ表示している旨を出す」。
  - **ハッシュ分割のままでは前方一致ができない**(名前が1文字違うと別のファイルになる)。先頭2〜3文字で分ける案は、本番の索引(53.9万人)で偏りを測ると先頭2文字「ma」が3.1%・先頭3文字「the」が0.8%あり、1億件では1ファイル数十〜数百MBになるため見送った。
  - by-nameを**正規化名の順に並べ、1〜2万行ずつのファイルに区切り、各ファイルの先頭のキーを目次`index.tsv`に持つ**形にした(`PlayerNameIndexWriter`)。同じ文字列で始まる行は連続するので、目次を二分探索して位置を決め、順に読むだけで済む。完全一致は全員を読んで数え、最近確認した順の上位だけを残す(ページ送り用)。前方一致は必要な人数+1人で読むのをやめる。
  - 毎晩の反映は、差分を外部ソート(`ExternalSort`、5万行ずつ並べて併合)で並べ、影響のあるファイルだけ既存の行と突き合わせて**新しい名前のファイル**に書き、目次を差し替えてから古いファイルを消す(検索が書き換え途中を読まないように)。ファイルが2万行に達したら1万行ずつに分ける。
  - **起動の1分後に、by-nameが使えない状態(目次が無い・以前の形式・前回の反映途中で落ちた)なら作り直す**ようにした。形式を変えたデプロイの直後から、定時(日本時間3:30)まで名前検索が0件になるのを避けるため。以前の形式のファイル(0000〜4095.tsv)は作り直しの際に消える。
  - **ハマりポイント**: Thymeleafでは`th:replace`が`th:each`より先に処理されるため、同じ要素に書くと断片に繰り返しの変数が渡らない(`null`)。繰り返しは外側の`th:block`に書く。
  - **ハマりポイント**: Windowsで`python - <<EOF`の形で日本語を含むスクリプトを渡すと、置換対象が一致しないことがあった。日本語を含む編集スクリプトはファイルに書いてから実行する。
  - 確認(ローカル、本番のby-tag 53.9万人、ヒープ256MB): 全件の作成は2.4秒(53ファイル)。検索は0.02〜0.06秒(再起動直後の初回のみ約0.5秒)。「alex」は完全一致662人を14ページで最後の「651〜662人目」まで表示、「g」は完全一致1人+前方一致49人と「多すぎる」案内、「a」は完全一致0人で前方一致50人。Chromeで結果画面とページ送りを目視し、幅320・375px×4言語で横はみ出し0を実測。起動1分後の自動作り直しも、目次を消して以前の形式のファイルを置いた状態から確認した。
  - **本番デプロイ**(2026-09-19 15:57 JST、`89625c5`。別セッションの作業途中の変更を含めないよう`git archive HEAD`から作ったjarで)。起動1分後の15:58 JSTに索引が自動で作り直された(`53 by-name files rebuilt (16198 ms)`、27MB)。本番で「alex」662人のページ送り(範囲外の`page=99`は最後のページ)、「a」「g」「clash」の前方一致と案内、該当なし、タグの転送を確認。応答は0.2〜0.5秒(同時に計ったトップページは2.0秒、プレイヤー情報画面は1.4秒)。available 359MB。

## フェーズ1.24: 言語切替をドロップダウンにする(2026-09-19)

ユーザー指摘「言語の切り替えリンクがよろしくない。対応言語が増えたらどうなる」への対応。

- [x] **ヘッダーの言語切替を、全言語のリンクを横に並べる方式から、ドロップダウン(`details`/`summary`)に変えた**。閉じた状態では「🌐 現在の言語 ▾」だけを出すので、言語が増えてもヘッダーの幅は変わらない。一覧は重ねて表示し、高さの上限(`min(60vh, 22rem)`)を超えたらメニュー内でスクロールする。
  - 地球儀を付けたのは、今表示されている言語が読めない人でも「言語の切替」だと分かるようにするため。
  - `details`なのでスクリプトが無くても開閉できる。標準では外側を押しても閉じないため、外側のクリックとEscキーで閉じるスクリプトだけ足した。
  - 選択中の言語は`aria-current="true"`とチェックマークで示す(以前は`is-active`クラス)。
- **ハマりポイント**: PC幅ではボタン基準の右揃えで一覧を出すが、スマホ幅ではヘッダーの折り返し方次第で言語メニューが左端に来るため、右揃えだと一覧が画面の左外にはみ出した(幅375pxで`left: -37px`)。スマホ幅(600px以下)では、一覧をヘッダーの左右いっぱいに広げた。`top`を指定しない絶対配置にすると、縦位置はボタン直下の本来の位置のまま使える。
- **ハマりポイント**: PythonのheredocでCSSの`\1F310`・`\25BE`などを書き込んだところ、バックスラッシュのエスケープが解釈されて制御文字に化けた。CSSのUnicodeエスケープを含む編集は、Editツールか`chr(92)`で書く。
- 確認: ローカルのChromeで幅1280・700・375・320pxを実測し、どの幅でも一覧が画面内に収まり横スクロールが出ないこと、外側のクリックで閉じることを確認した。項目を20個に複製しても、一覧はスクロールで収まった。
- 別件で気付いた既存の問題: プレイヤー情報画面の対戦履歴で、長い名前の対戦相手(`.name-list`)が幅375pxで11pxほど横にはみ出す → フェーズ1.25で対応。

## フェーズ1.25: スマホ幅の横はみ出しを解消(2026-09-19)

ユーザー依頼「(対戦相手名のはみ出しを)直してください。別セッションが作業中なのでデプロイはしないで」。別セッションの作業完了後にデプロイした。

- [x] **対戦履歴の対戦相手名**: スマホ幅の表は各セルが「項目名 7.5rem | 値 1fr」のグリッドになる。区切りの無い長い名前(キリル文字の「ШТУРМУЕМДМИША」)は折り返せず、値の列から約60pxはみ出していた。スマホ幅の表のセルに`overflow-wrap: anywhere`を付けた(クランメンバー・ランキングの表も同じ構造なので共通で効く)。項目名(`td::before`)は単語の途中で切れないよう`normal`に戻した。
- [x] **確認中に見つけた別のはみ出し**: 幅320pxのポルトガル語で、使用中のデッキ(4列固定)が42pxはみ出していた。`repeat(4, 1fr)`の`1fr`は中身の最長の単語(「Esqueletos」など)より縮まないため。`minmax(0, 1fr)`にし、カード名は長い単語を途中で折り返す(`overflow-wrap: anywhere` + `hyphens: auto`)。スマホ幅では対戦詳細のデッキと同じく余白と文字を小さくした。
- [x] **幅320pxでの読みやすさ**: 項目名の列が7.5remのままだと値の列が84pxしか残らず、「Ranqueado」も途中で折り返した。480px以下では項目名の列を5.5remにした。
- 確認: ローカルのChromeで、プレイヤー情報(4言語)・対戦詳細・クラン・ランキング2画面・トップ・カード一覧を幅320・375・1280pxで開き、すべて横はみ出しが0になることを実測した。幅320pxの描画もスクリーンショットで目視確認した。
- [x] **本番デプロイ・動作確認済み**(2026-09-19、`ad56672`。別セッションのフッター追加などコミット済みの変更も含む)。主要画面が200を返し、配信中のCSSに修正が入っていること、本番のプレイヤー情報(pt・ja)・クラン(es)を幅320・375pxで開いて横はみ出しが0であることを確認した。

## フェーズ1.26: 整理バッチを毎時に変更(2026-09-19)

ユーザーの指摘「整理バッチ1日1回は少ない気がする」を受け、間隔を選択肢で確認し、ユーザーが「1時間ごと」「application.ymlの既定値で変える」を選択。

- [x] `player-index.compact-cron`の既定値を`0 30 18 * * *`(毎日18:30 UTC)から`0 5 * * * *`(毎時5分、UTC)に変更。見かけてから名前検索に出るまでの待ちが最大約1日から最大2時間ほどに縮む。
  - inboxは1時間ごとのファイルで、書き込み中の時間帯のファイルは整理の対象外。このため毎時0分ちょうどではなく少し後(5分)に動かし、直前の1時間分を確実に取り込む。1時間より細かくしても早くはならない。
  - 負荷: 臨時実行の実測で14時間分(約21万行)が5.6秒だったため、毎時でも問題ない見込み。実行中は巡回が止まる(同じスレッド)が数秒程度。
  - 本番の`/etc/clash-royale-api/env`には`PLAYER_INDEX_COMPACT_CRON`が無いこと(既定値が効くこと)を確認済み。
- [x] **本番デプロイ**(2026-09-19 07:17 UTC、`145749c`)。HTTP 200を確認。初回の毎時実行は08:05 UTC(日本時間17:05)。

## フェーズ1.27: 本番ログの問題の修正、堅牢性の既知課題、巡回間隔の短縮(2026-09-19)

ユーザー依頼「やり残し・次の作業の提案」で挙げた項目のうち、ドキュメントの食い違い・本番ログの問題2つ・巡回間隔・`TODO.md`の堅牢性7〜9の実施を依頼された。

- [x] **ドキュメントの食い違い**(`9480c4d`): `TODO.md`の名前検索の節(by-nameと検索画面が「未着手」のまま)、本ファイルのフェーズ1.23の見出し(「本番未投入」)、末尾の「名前検索は撤去済み」を実態に合わせた。
- [x] **巡回中の例外**(04:32 UTCに1回): `getRiverRaceLog`で、応答本文の読み取り中に接続が切れ(`EOFException`)、`RestClientException`(`HttpMessageNotReadableException`)がclient層の外に出ていた。`call()`は`RestClientResponseException`と`ResourceAccessException`しか変換していなかったため。`RestClientException`全般も`ApiUnavailableException`に変換した。巡回は一時停止の扱いになり、画面側では500ではなくエラー画面になる。
- [x] **ゲームモード名の訳の抜け**: `CW_Battle_1v1`・`Overtime_Ladder`・`7xElixir_Ladder`(どれも`type=riverRacePvP`)。`gameMode.name`はイベントごとに増えるため、対戦種別の辞書に`riverRacePvP`・`riverRaceDuel`・`riverRaceDuelColosseum`・`boatBattle`を足した。訳語はSupercell公式サポートポータルの「クラン対戦とは」の各言語版で裏取りした(日本語「クラン対戦」、英語「Clan Wars」、西「guerras de clanes」、葡「Guerras de Clãs」)。表記の大小は既存の`gamemode.ClanWarWarDay`(「Guerra de clanes」「Guerra de clãs」)に揃えた。
- [x] **堅牢性7**: 名前検索が無効のとき、または`#`付きの入力で、タグの形式(`Tags.looksLikeTag`)でなければ転送せず「見つからない」を出す。名前検索が有効なら`ab/c`は名前検索に回るので元から問題なく、`#ab/c`が該当していた。
- [x] **堅牢性8**: テンプレートの通常のHTMLコメント24か所(`layout.html`23、`cards.html`1)をパーサーレベルコメント(`<!--/* */-->`)にした。`error.html`・`battle-detail.html`は既に対応済みだったが、`head`フラグメント内のスクリプトの説明などが配信されていた。
- [x] **堅牢性9**: `config/SecurityHeadersFilter`で`X-Content-Type-Options: nosniff`・`X-Frame-Options: DENY`・`Content-Security-Policy: frame-ancestors 'none'`・`Referrer-Policy: strict-origin-when-cross-origin`を付ける。静的ファイルやエラー画面にも付けるため、インターセプタではなくフィルタにした。CSPを`script-src`まで絞らないのは、テンプレートにインラインのscriptがあるため。
- 確認: テスト182件成功(worktree・masterとも)。ローカル起動で`#ab/c`が「見つからない」、全画面でコメント0件、静的ファイルにもヘッダが付くこと、クラン対戦の対戦が「クラン対戦」と表示されWARNが出ないことをHTMLで確認した(見た目の変更は無いため、描画の目視はしていない)。
- [x] **本番デプロイ**(2026-09-19 07:34 UTC、`da8e8c4`)。本番でヘッダ・`#ab/c`・コメント0件・「クラン対戦」の表示を確認。
- [x] **巡回の間隔を10s→5s**: 同じ再起動で`/etc/clash-royale-api/env`の`CRAWLER_INTERVAL`を変更(変更前の控えは`env.bak-20260919-interval`)。100回を約8分40秒で処理(10秒間隔のときは約17分)、`crawler paused`は0回。主要画面は0.9〜1.4秒、WARN/ERRORは0件、available 351MB。次は様子を見て2sにする。

## フェーズ1.28: ドイツ語・フランス語・イタリア語・ロシア語に対応(2026-09-19)

`TODO.md`の「対応言語の追加」(巡回データの国別集計で上位だった4言語)をユーザー依頼で実装。西・葡のとき(フェーズ1.20)と同じ方法(ゲームデータの裏取り＋Claudeによる文言翻訳)を踏襲した。作業は`worktree-i18n-de-fr-it-ru`で実施。

- [x] **カード名127枚を裏取り**。西・葡のときに使った`smlbiobot/cr-csv`(2023年時点のダンプ、`assets/csv_client/texts.csv`)にFR/DE/IT/RU列があることを確認し、110枚をここから取得した。
  - **複数候補の除外に気付いた点**: 同じ英語名で複数行がヒットするカード(`Goblins`→`gobelins`/`Duendes`など、クラン対戦のデッキ名テンプレート由来の断片)があり、素朴に最初の1件を採用すると誤った訳を拾う。実際のカード名は`TID_SPELL_*`(タワー ユニットのみ`TID_CHARACTER_*`)のTIDに限られると分かったため、TIDの接頭辞で絞り込んでから採用した。既存の日本語辞書の値とJP列を突き合わせて、この絞り込みが正しいことも確認した。
  - 2023年以降の新カード17枚(西・葡のときと同じ枚数)は、`gh search code`で見つけた更新版データセット(`HastyarSeamand/csv_logic-for-CR`、2025年10月時点、8枚追加で判明)と、公式Supercellブログの各言語版(独・仏・伊・露)の該当記事から採った。ロシア語もブログの現地語版があることを確認した(`supercell.com/en/games/clashroyale/ru/blog/`)。
  - `Rune Giant`・`Minion Giant`・`Void`・`Vines`・`Dagger Duchess`・`Royal Chef`・`Spirit Empress`・`Ronin`の8枚は、上記データセットにも無いほど新しく(2024〜2026年)、公式ブログの季節アナウンス記事・バランス調整記事から1枚ずつ確認した。`Ronin`はどの言語でも「Ronin」のまま(ロシア語は「Ронин」と転写)。
- [x] **レアリティ・役職・デッキ・エリクサー・クラン・トロフィーなど主要な用語もゲームデータ(texts.csv)で裏取り**。「クラン対戦」関連の用語(戦闘日・準備日)や「トロフィー目標」(Ladder)、「ランク戦」(Ranked)も同様。
- [x] **画面の残りの文言はClaudeによる翻訳**(西・葡と同じ既知の制約。ネイティブのチェックは受けていない)。
- [x] **ロシア語の複数形**: `IcuMessageSource`の仕組みは既存のまま(ICU4Jの`MessageFormat`が`one`/`few`/`many`/`other`を解釈する)で、メッセージ側にロシア語の文法規則(1→one、2〜4→few、0・5以上・11〜14→many)に沿った3〜4パターンを書くだけで対応できた。コード変更は不要。
- [x] **`SupportedLanguages.SUPPORTED`に4言語を追加**。国名の割り当て(`CountryPreference`)・hreflang・言語切替ドロップダウン(フェーズ1.24で導入済み)・`MessagesConsistencyTest`はすべてこの一覧から自動生成/自動検証される設計だったため、追加のコード変更は不要だった。
- [x] **テストの直し**: `SupportedLanguagesTest`・`LabelResolverTest`・`CountryNamesTest`が「未対応言語」の具体例としてフランス語・ドイツ語を使っていたため、追加後も未対応な韓国語に差し替えた(仏・独は対応言語になったため、以前のアサーションが成立しなくなっていた)。
- 確認: `mvn test`182件成功。ローカル起動(ポート18090)で新4言語のトップ・カード一覧・ランキング2画面が200を返すこと、WARNログ(訳の抜け)が0件であることを確認した。Chromeで実描画も確認: ロシア語のトップページ・カード一覧、言語切替ドロップダウンに8言語すべてが正しい自国語表記で並ぶこと(改行や重なりなし)。
  - カード一覧画面でロシア語の長いカード名がハイフネーションで複数行になる(既存のCSS、`hyphens: auto`)ことを確認したが、はみ出しは無く既存の挙動どおり。
  - **未実施**: 実際のスマホ幅での確認。Chrome拡張のウィンドウリサイズがこの環境で効かず([[project_chrome_resize_workaround]]参照)、`window.innerWidth`が変わらなかった。フレームでの代替も自サイトの`frame-ancestors 'none'`(CSPヘッダ)によりブロックされた。ただし言語ドロップダウンはフェーズ1.24で20項目に複製してもスマホ幅で収まることを確認済みの設計であり、8言語への増加はその範囲内。次にこの環境でスマホ幅の実描画確認が必要になった際は、CSSOMでのメディアクエリ書き換えに加えて別の手段(実機・別ツール)を検討する。
- **他セッションとの重複確認**: 作業中に別セッション(clash-royale-api-47)から「独・仏・伊は完了確認できた」という趣旨のメッセージを受け取り、一時的に並行作業を疑ってデプロイを保留した。セッション間メッセージで確認したところ、別セッションは独自に実装していたのではなく、このセッションのworktreeの状態を見ていただけと判明(重複無し)。誤解を招いたが実害は無かった。
- [x] **本番デプロイ**(2026-09-19、コミット`6c1839b`のjarから)。`git worktree list`で他の作業中worktreeが無いことを確認してから実施。ホームページ・カード一覧(ロシア語で長いカード名のハイフネーションを含む)・`<title>`が独・仏・伊・露それぞれで正しく表示されることを本番で確認した。起動後のWARN/ERRORは0件、available 364MB。

## フェーズ1.29: クランランキングを上位1000件まで表示(2026-09-19)

`TODO.md`に記録していた「クランランキングを200位まで表示したい」を、ユーザーとの対話の中で1000位まで拡大する方針に変更して対応。作業は`worktree-clan-ranking-200`で実施。

- [x] **公式APIの上限を実測**: ローカルの`config/application-local.yml`のトークンを使い、`https://api.clashroyale.com/v1/locations/global/rankings/clans`に`limit=200`・`limit=1000`を直接投げて確認。どちらもHTTP 200で、`limit=1000`のとき実際に1000件返ることを確認した(個人ランキングの実測と同じ上限)。
- [x] **`RankingService.CLAN_RANKING_SIZE`を10→1000に変更**。
- [x] **`/ranking`画面に、個人ランキング画面と同じ遅延読み込み方式を適用**: 1000件×2タブを最初から描くとHTMLが大きくなるため、開いているタブだけ先に描画し、もう一方はタブ切替時に新設の`/ranking/table`(`RankingController`)から取得する構成にした。既存の`rankingTabs`フラグメント(`lazyUrl`引数)がそのまま使えたため、テンプレート側の変更は`ranking.html`の1行(`lazyUrl`に`'/ranking/table'`を指定)のみで済んだ。
- 確認: `mvn test`182件成功(worktree・merge後のmaster双方)。ローカル(ポート18090)でcurlにより、グローバルタブ1000件・国別タブが遅延読み込み(`data-lazy-url`)になっていること、`/ranking/table?country=US`が999件・国列なしで返ることを確認。Chromeでの実描画でも、グローバル1000件表示、USタブへの切替が即座に効くこと(#993〜#999まで実在)を確認した。
- **気づいた点(このタスクとは別件、対応はしていない)**:
  - `HomeController`の`PLAYER_RANKING_SIZE`は100だが、`CLAUDE.md`「個人ランキング: トップページに上位10人」および`TODO.md`「画面デザインの改善候補」の対応済み欄(「個人ランキングを10人にした」)は10人のまま。実際のコードは`94d1852`で100人になったあと10人へは戻っておらず、ドキュメント記述と食い違っている。`index.html`のランキング表示は`lazyUrl`無し(両タブ即描画)のため、実際には200行(100人×2タブ)を毎回描画している。「トップページを軽く保つ」方針([[project-oci-vm-spec]]参照)とは相容れないので、次にトップページを触るセッションで見直しの要否を確認するとよい。
  - ブラウザでの実描画確認中、1000行(カード表示で1行あたり複数div)を描画した直後にChrome拡張のスクリーンショット取得(CDP `Page.captureScreenshot`)が2回、30秒でタイムアウトした(数秒待つと復帰)。本番相当のcurlでの応答時間は0.6秒(初回)・0.27秒(キャッシュ後)と問題無く、Chrome拡張側の一時的な取りこぼしの可能性が高いが、極端に行数が多い画面でクライアント側の描画コストが上がる点は留意事項として残す。
- [x] **本番デプロイ**(2026-09-19、コミット`501a466`のjarから)。`git worktree list`で他の作業中worktreeが無いことを確認してから実施。本番の`/ranking`でグローバル1000件・国別タブの遅延読み込みを確認した。

## フェーズ1.30: プレイヤー情報画面にカードコレクションを追加(2026-09-19)

`TODO.md`の「機能面の改善候補」9番(優先度が高いと記録していた項目)。`/players/{tag}`の`cards`(所持カード全件のlevel/maxLevel/rarity)は取得済みだが未使用だった。作業は`worktree-card-collection`で実施。

- [x] **実データで挙動を確認**: 複数プレイヤーの`/players/{tag}`を直接叩き、`cards`配列がレアリティを問わずほぼ全カード(122〜123枚)を含むこと、`count`が0でも`level==maxLevel`のカードがあること(例: レベル61のプレイヤーで`Mega Knight`など5枚がcount0だが全て最大レベル)を確認した。これは「手持ちを使い切って最大レベルにした」場合であり「未所持」ではない。この発見により、「所持しているか」の判定に`count`を使うと、育成が進んだカードほど誤って「未所持」扱いになるという逆転現象が起きることが分かった。
- [x] **設計をシンプルにした**: 上記の理由から「所持率」ではなく「最大レベル率」を出す設計にした。これにより`CardService`のカード一覧(catalog)と突き合わせる必要が無くなり、`player.cards()`だけで完結する純粋な集計(`domain/CardCollection`)にできた。
- [x] `PlayerResponse`に`cards`(`OwnedCard`: id/name/level/maxLevel/count/rarity)を追加。
- [x] `domain/CardCollection`: レアリティ別に「最大レベル枚数/総枚数」を集計する純粋ロジック。レアリティの並びはゲーム内の順(コモン→チャンピオン)。
- [x] `ViewMapper#toCardCollection`・`web/view/CardCollectionView`でレアリティ名の解決とパーセント計算(四捨五入)を行い、`player.html`に`stat-grid`を使ったセクションを追加(既存のCSSをそのまま流用、新規CSS無し)。
- [x] 8言語(ja/en/es/pt/de/fr/it/ru)の文言を追加。「所持率ではなく最大レベル率である」という注記も全言語に入れた。
- 確認: `mvn test`186件成功(worktree・merge後のmaster双方、`MessagesConsistencyTest`で8言語のキー一致も確認)。ローカル(ポート18091)で実プレイヤー(`8V0Q02L9Y`、対戦履歴ありのプレイヤー)・`UJ2JR02V0`(対戦履歴の無いプレイヤーでも表示されること)にアクセスし、8言語すべてで見出し・注記・レアリティ別の内訳が正しく表示されることをcurlで確認した。
- [x] **本番デプロイ**(2026-09-19、コミット`3ffaaa8`のjarから)。デプロイ前後でエラー・警告ログ無し、`free -m`のavailable 562MB。本番の`/player/8V0Q02L9Y`でカードコレクションセクションが表示されることを確認した。

## フェーズ1.31: クランランキング上位20件にクラン対戦トロフィーを追加(2026-09-19)

`TODO.md`の画面デザインの改善候補14「クランランキングの上位が全部同点に見える」への対応。ユーザーから「何かいい指標ある?」と聞かれ、実データ調査の上で提案・実装した。

- [x] **指標の調査**: 上位クランはクランスコアが上限(140000)で並び同点になる。`/clans/{tag}`の詳細フィールドを実データで確認したところ、`clanWarTrophies`(クラン対戦トロフィー)は上限が無く上位クランでも1805〜2346のようにばらけることを確認。`requiredTrophies`(参加条件)は上位が軒並み14000で使えなかった。
- [x] **実装**: `PlayerResponse`と同様に`ClanResponse`に`clanWarTrophies`を追加。クランごとに個別API呼び出しが必要なため上位20件に限定し(`RankingService.WAR_TROPHIES_RANK_LIMIT`)、仮想スレッドで並行取得。8言語の文言を追加。
- [x] **性能問題が発生 → 同日中に修正**: 本番デプロイ直後、`/ranking`の応答が初回11.8秒、以降も2〜4秒かかる劣化が発生。調査の結果、個々の`getClan`呼び出し自体は2分キャッシュが効いていた(単体の`/clan/{tag}`は0.3秒)が、20件を並行取得する処理自体(仮想スレッドExecutorの生成)を**リクエストのたびに毎回実行**しており、低スペックVM(1/8 OCPU)ではその起動コストだけで数秒かかっていた。`warTrophiesOfTopClans`の戻り値(Map)自体を`locationId`をキーに`@Cacheable`でキャッシュすることで解消した。
  - 教訓: 個々のAPI呼び出しがキャッシュされていても、それらを束ねる並行処理のオーケストレーション自体のコストは別物として考慮する必要がある。低スペックVMでは特に、スレッド(仮想スレッドでも)の起動コストが無視できない。
  - 修正後の実測(本番): 初回3.7秒、その後JVMのウォームアップに伴い徐々に短縮し、10リクエスト程度で0.75〜1.0秒に収束(warTrophies機能を持たない`/ranking/players`でも同じ収束カーブが見られたため、残りの差はこの機能固有ではなくJVM再起動直後のJITウォームアップと判断)。
- 確認: `mvn test`189件成功。本番でエラー・警告ログ無し、クラン対戦トロフィーの値(2346・2080・1805など)が正しく表示されることを確認。

## フェーズ1.32: トップページの個人ランキングを100人に変更(2026-09-20)

ユーザー依頼「トップページのランキングを100人にして」。これは`CLAUDE.md`の「トップページはできるだけ軽くする」方針(2026-09-19に大前提の基本方針として表明)と、それに基づき10人に絞った過去の判断([[project-oci-vm-spec]]、`TODO.md`のフェーズ1.29の記録)に反するため、方針変更の認識でよいか選択肢で確認したうえで着手した。作業は`worktree-home-ranking-100`で実施。

- [x] `HomeController.PLAYER_RANKING_SIZE`を10→100に変更。理由が古くなった実測コメントも更新した。
- 確認: `mvn test`189件成功(worktree・merge後のmaster双方)。
- [x] **本番デプロイ**(2026-09-20、`d12be80`)。HTTP 200、トップページの`<tr>`数が202(100人×2タブ分)であること、デプロイ前後でWARN/ERROR無しを確認。available 408MB。
- **申し送り**: `index.html`のランキング表示は`/ranking`・`/ranking/players`と異なり`lazyUrl`無し(両タブを常に即描画)のため、以前100人にしたときと同様にページの高さが増える(過去の実測でPC幅約5,600px)。今回はユーザーが方針変更として承知のうえで依頼したため対応していないが、表示が重い・長いと感じた場合は遅延読み込み方式(`/ranking`等と同じ`lazyUrl`)の適用を検討する。

## フェーズ1.33: カードコレクションの注記を分かりやすい文言に直す(2026-09-20)

ユーザー指摘「プレイヤー情報画面のこの文言が意味不明」(カードコレクションの注記)。文言のみの変更で、集計ロジック(`CardCollection`)は変えていない。作業は`worktree-card-collection-notice`で実施。

- **原因**: 日本語版が「『最大レベルまで上げて手持ちが0枚になったカード』も『未所持のカード』も同じように見えるため」と、公式APIの`count`(手持ちの予備枚数)列の挙動をいきなり持ち出していた。読者は`count`という前提を知らないため話が追えず、かつ結論(所持率ではなく最大レベルの割合)が文末に来ていた。フェーズ1.19-1.30の追加時(`e325f97`)に8言語同時に書かれたが、日本語だけが冗長に書かれていた。
- [x] **8言語すべての`cardCollection.notice`を差し替え**。理由を「未所持のカードを正確に判別できない」に集約し、結論を先に読ませる構成にした。他7言語も「両者を確実に区別できない」の"両者"が曖昧(実際は「未所持」と「使い切って最大レベル」の区別)だったため、同じ趣旨に揃えた。
- 確認: `mvn test`189件成功(worktree・merge後のmaster双方)。ローカルで実プレイヤー(`200GJLL208`)にアクセスし、8言語すべての注記の描画を確認。特にアポストロフィを含むen/fr/itで、ICU MessageFormatのエスケープ(`''`)が正しく単一の`'`に解決されること(`can't`・`L'API`・`qu'un`)を実描画で確認した。
- [x] **本番デプロイ**(2026-09-20、`4967154`)。HTTP 200、日本語の注記が新しい文言で表示されることをChromeの実描画で確認した。

## フェーズ1.34: デザインレビュー指摘の修正(2026-09-20)

ユーザー依頼「デザイン観点でレビューして」。Chromeでローカル起動して主要画面(トップページ・プレイヤー情報・対戦履歴・対戦詳細・カード一覧・クラン情報・クランランキング)をデスクトップ幅とモバイル幅(390px、[[project-chrome-resize-workaround]]の手法でダークモードも同時確認)で実描画し、6件指摘。うち3件をCSS・テンプレート修正、1件はアイコン変更、2件は見送りとした。作業は`worktree-design-review-fixes`で実施。

- [x] **対戦履歴の勝敗バッジが縦に2行折り返す**(`.badge`に`white-space: nowrap`が無く、狭いテーブル列で「負け」が「負\nけ」になっていた)を修正。
- [x] **ランキング表の見出し(「クランスコア」等)が列幅不足で折り返す**(`thead th`に`white-space: nowrap`を追加、はみ出しは既存の`.table-scroll`の横スクロールに委ねる)を修正。
- [x] **カード名が2行になるカードだけ、下のエリクサー/レベル表示の位置がずれる**(`.deck-card`をflex columnにし`.card-sub`に`margin-top: auto`を付けて下端に揃える)を修正。カード一覧・使用中のデッキ・対戦詳細・得意/苦手カードの全箇所に共通で効く。
- [x] **プレイヤー情報画面の「勝利数」アイコンを✅→⚔️に変更**(✅は「達成チェック」のニュアンスが強く違和感があったため)。
- **誤診断だったため見送り**: 対戦相手名が「■■■■■■■」と表示されるケースを当初「フォントのtofu(文字化け)」と誤診断し、絵文字フォールバックをbodyのfont-familyに追加する修正を試みたが、既存の⭐🏆等の絵文字がモノクロ化する副作用が出たため撤回した。DevToolsでコードポイントを確認したところ実体はU+2B1B(BLACK LARGE SQUARE)を7個並べたプレイヤーの自称ニックネームであり、正しくレンダリングされていた。表示上の不具合ではないため対応不要と判断。
  - 教訓: 見た目が「文字化け」に見えても、実際のUnicodeコードポイントを確認するまでは原因(フォント側かデータ側か)を決め付けない。
- **見送り(低優先度、意図的に対応せず)**: 7枚デッキ+タワーユニット表示で4列グリッドの最終行に空白ができる点は、グリッドの自然な挙動であり視覚的な破綻ではないため据え置き。
- 確認: `mvn test`189件成功(worktree・merge後のmaster双方)。Chromeの実描画でデスクトップ幅・モバイル幅(390px)の両方について、修正前後の対戦履歴・カード一覧・ランキング表を比較して改善を確認した。
- [x] **本番デプロイ**(2026-09-20、`e3b20dc`)。HTTP 200、本番の`/player/VL8C9VQ8Y`で「負け」バッジが1行で表示されることを確認した。

## フェーズ1.35: カード一覧のエリクサー表示を水滴バッジに変更(2026-09-20)

ユーザー指摘「カード一覧のエリクサー数の表示がダサくない?」。カード名の下に小さな灰色文字で「1エリクサー」と出していたが、同じ語が全カードに並んで単調・情報密度が低く、名前の折り返しと相まってごちゃついて見えていた。選択肢を提示し、ユーザーの選択で「ゲーム内と同じ水滴バッジをカード画像の左上に重ねる」案を採用。作業は`worktree-elixir-badge`で実施。

- [x] `fragments/layout.html`に`elixirBadge(cost)`フラグメントを追加(インラインSVGの水滴+数字)。単位付きの「3エリクサー」は`role="img"`+`aria-label`で読み上げ用に残したので、`cards.elixir`のメッセージキーは全言語そのまま使っている。
- [x] `cards.html`はカード画像を`.card-thumb`(画像と同じ56px幅・`position: relative`)で包み、その中にバッジを絶対配置。`card-sub`の行は削除した。
- [x] 色は`--color-elixir`をライト`#a32fb5`/ダーク`#b23dc4`で新設。ゲーム内の明るい紫だと白文字とのコントラスト比が4.05:1で4.5:1に届かないため暗くした(現在は約5.8:1)。水滴の縁は`stroke: var(--color-bg)`で描き、下のカード絵柄に紛れないようにしている。バッジは`pointer-events: none`で画像リンクを塞がない。
- 確認: `mvn test`189件成功(worktree・merge後のmaster双方)。Chromeの実描画でライト/ダーク(`[[project-chrome-resize-workaround]]`の手法)・狭い横幅の3通りを確認した。
- [x] **本番デプロイ**(2026-09-20、`062626f`)。HTTP 200、本番の`/cards`でバッジ表示を確認した。

## フェーズ1.36: エリクサーバッジの調整とエリクサーでの絞り込み(2026-09-20)

ユーザー依頼「水滴バッジのデザインはどう思う?」。本番の`/cards`をライト/ダーク両方で実描画して評価し、改善点3件+提案1件を挙げ、ユーザーの指示で全4件を実施した。作業は`worktree-elixir-badge-tune`で実施。

- [x] **バッジを拡大**(20×24→22×26px、数字0.68rem→0.75rem)。実測10.88pxは56pxのカード画像に対して小さく、実機で読みづらいと判断した。
- [x] **水滴の縁の色を`--color-elixir-outline`として独立**させた。従来は`stroke: var(--color-bg)`の直指定で、カード一覧のタイル背景(`--color-bg`と同値)では合っていたが、白い面(`--color-surface`。プレイヤー情報画面のデッキなど)にフラグメントを流用すると縁だけ浮く構造だった。既定値は`var(--color-bg)`(カスタムプロパティは使用時に解決されるので、ダークモード用の再定義は不要)。
- [x] **鏡(Mirror)にゲーム内と同じ`?`のバッジを出す**。従来は`elixirCost`が`null`のカードをバッジごと消しており、タワーユニットと同じ扱いで「表示漏れ」に見えていた。判定は`ViewMapper`に置き、`ElixirBadgeView(text, label)`を新設してテンプレートから分岐を追い出した。タワーユニット(`supportItems`)は引き続きバッジ無し。読み上げ用に`cards.elixir.variable`を8言語へ追加。
- [x] **エリクサーでの絞り込みを追加**(`/cards`の名前入力欄の下に1〜9のチップ、複数選択可、名前の絞り込みとAND)。選択肢は固定値ではなく`CardService.elixirCosts()`がカード一覧から作るため、将来コストの上限が変わっても追従する。押下状態は`aria-pressed`で持ち、CSSも`[aria-pressed="true"]`で塗り分けている。
- [x] **既存バグの修正**: `.deck-card[hidden] { display: none; }`を追加。フェーズ1.34(`0d3c3cc`)で`.deck-card`に`display: flex`を入れた際、ブラウザ標準の`[hidden] { display: none }`より詳細度が高くなり、**カード名での絞り込みが効かなくなっていた**(`hidden`属性は付くが見た目が変わらない)。今回エリクサー絞り込みを実装して初めて気づいた。
  - 教訓: 要素に`display`を指定すると`[hidden]`が無効化される。JSで`hidden`を使って出し分けている箇所に`display`を足すときは、併せて`[hidden]`の打ち消しを書く。
- 確認: `mvn test`191件成功(worktree・merge後のmaster双方。`CardServiceTest`に選択肢の生成、`ViewMapperTest`に鏡/タワーユニットのバッジ判定を追加)。ローカル起動しChromeの実描画で、ライト/ダーク・デスクトップ幅/390px幅、絞り込みの動作、鏡の`?`バッジを確認した。
- [x] **本番デプロイ**(2026-09-20、`e33ab84`)。HTTP 200、本番の`/cards`で5コスト絞り込みの動作を確認した。

## フェーズ1.37: お気に入り機能 1回目(Cookie保存・登録解除・☆ボタン)(2026-09-20)

`お気に入り（検討中）.md`(2026-09-19時点の検討メモ)に沿って着手。未決定だった3点をユーザーに確認: ☆ボタンはSVGアイコン、お気に入り画面のプレイヤー列にランク戦順位を出す、Cookieの説明はプライバシーポリシー等へ今回は追加しない(お気に入り画面の注記のみ)。進め方は検討メモの「3回に分ける」の1回目のみをこのセッションで実施(トップページのお気に入りカード・お気に入り画面`/favorites`は次回以降)。作業は`worktree-favorites`で実施。

- [x] `domain.Favorites`(タグと名前の並び、登録・上限10件・重複・解除・名前書き換え。HTTP非依存)。
- [x] `web.FavoriteCookies`(Cookie `fav_players`/`fav_clans`の読み書き。値は`1.`+Base64URL(パディングなし)、中身は「タグ(#なし) TAB 名前」を改行区切り。壊れた値・知らないバージョン・タグ形式でない行・11件以上・50文字超の名前に寛容)。属性は`Path=/`・`Max-Age=365日`・`HttpOnly`・`SameSite=Lax`。
- [x] `web.FavoriteController`(このアプリ初のPOST)。`POST /favorites/{players|clans}/{tag}`・`.../remove`。登録名はフォームから受けず`PlayerService#findPlayer`/`ClanService#findClan`から取り直す(タグの実在確認を兼ねる)。`Accept: application/json`(fetch)には`{"favorite":true/false}`または上限時409`{"error":"limit"}`を返し、それ以外(JS無し)は303で対象画面へ戻す(戻り先はタグから組み立て、Refererを使わないためオープンリダイレクトにならない)。
- [x] プレイヤー情報画面・クラン情報画面の見出しに☆ボタン(`aria-pressed`+ラベル付き、JSのfetchで画面遷移せず切替、上限時はボタン近くに文言表示)。画面を開いたとき登録済みなら保存名を最新化(`FavoriteCookies#refreshName`)。
- 決めていないことのうち残り2点(お気に入り画面のプレイヤー列にランク戦順位を出すか、Cookie説明をどこに載せるか)は上記で確定済み。`お気に入り（検討中）.md`は次回(トップページ・お気に入り画面)着手時に更新する。
- 確認: `mvn test`199件成功(worktree・merge後のmaster双方)。ローカル起動(ポート8081。8080は別セッションが使用中だったため)しChromeの実描画で、プレイヤー・クラン情報画面の登録/解除(fetch、画面遷移なし)、リロード後も状態維持、ライト/ダーク、モバイル幅、上限時の文言表示、JS無し相当(curlでAcceptヘッダーを変えたPOST)の303リダイレクトを確認した。
- [x] **本番デプロイ**(2026-09-20、`1ed7943`)。他セッション(`worktree-elixir-badge-tune`)が空いたことを確認してから実施。HTTP 200、`/player/{tag}`のレスポンスに☆ボタンが含まれることを確認、起動後のWARN/ERRORは0件、available 366MB。

## フェーズ1.38: お気に入り機能 2回目(トップページのお気に入りカード)(2026-09-20)

ユーザー依頼「2回目に進んで」。`お気に入り（検討中）.md`の「トップページ」節に沿って実施。作業は`worktree-favorites-top`で実施。

- [x] `HomeController`に`FavoriteCookies`を注入し、Cookieから名前だけを読んで`favoritePlayers`/`favoriteClans`をモデルに追加(公式APIは呼ばない)。
- [x] `index.html`に「お気に入り」カードを追加(検索フォームの下・個人ランキングの上)。プレイヤー・クランを行で分け、名前をチップ状のリンクで並べる。両方0件のときはカードごと出さない。
- [x] **bfcache対応**: ☆の切替成功時に`sessionStorage`へ変更時刻(`favoritesChangedAt`)を書き、`[data-favorites-tracked]`を付けた画面(現状トップページの`<main>`)は表示時点の値を覚えておき、`pageshow`で`persisted`かつ食い違っていれば`location.reload()`する(言語切替の仕組みと同じ考え方)。0件→1件の切り替わり(カードの出現)もこの仕組みでカバーされる。
- お気に入り画面(`/favorites`、3回目)への「お気に入り画面へ」リンクは、そのページ自体が無いためまだ置いていない(3回目で追加)。
- 確認: `mvn test`199件成功(worktree・merge後のmaster双方)。ローカル起動(ポート8082。8080/8081は他セッションが使用中だったため)しChromeの実描画で、お気に入り0件時にカードが出ないこと、1件登録後に出ること、クラン情報画面で☆を押した後にアプリの戻るリンク(`history.back()`)でトップページに戻ると新しい状態(bfcacheの読み直し)が反映されること、ライト/ダーク・モバイル幅を確認した。
- [x] **本番デプロイ**(2026-09-20、`1ed7943`。フェーズ1.37の未デプロイ分と合わせて実施)。他セッションが空いたことを確認してから実施。

## フェーズ1.39: お気に入り機能 3回目(お気に入り画面)(2026-09-20)

ユーザー依頼「3回目を進めて」。`お気に入り（検討中）.md`の「お気に入り画面」「お気に入り画面の取得処理」節に沿って実施。作業は`worktree-favorites-screen`で実施。

- [x] `FavoriteFetch<T>`(domain、sealed interface)。取得できた(`Found`)/見つからない(`NotFound`)/取得できない(`Unavailable`)の3通り。プレイヤー・クランで型引数だけ変えて共用。
- [x] `FavoriteService`(service)。`PlayerService#findPlayer`/`ClanService#findClan`を、仮想スレッド+`Semaphore(4)`で並行に呼ぶ(公式APIのレート制限値が未確認なため、一度に20本投げない)。1件の失敗(`ResourceNotFoundException`→`NotFound`、それ以外の`ClashRoyaleApiException`→`Unavailable`)が他の件に影響しない。
- [x] `FavoriteRowStatus`・`FavoritePlayerView`・`FavoriteClanView`(web/view)と`ViewMapper`の変換メソッド。`found()`/`notFound()`をレコードのメソッドとして持たせ、テンプレート側の分岐をenum文字列比較にしないようにした。
- [x] `FavoriteController`に`GET /favorites`を追加。取得できた最新の名前でCookieを書き直す(`refreshNames`)。POST(登録・解除)は`back`パラメータ(`back=favorites`)を追加し、お気に入り画面からの「外す」はそこへ303リダイレクトする(戻り先はタグ・固定値から組み立てるため、オープンリダイレクトにはならない)。
- [x] `favorites.html`新設。プレイヤー・クランの表(名前・タグ・トロフィー/クランスコア・ランク戦レーティング/メンバー数(n/50)・外すボタン)。見つからない/取得できない行は`inactive-badge`(既存クラス流用)で注記。0件のときは案内文+検索フォーム。表の下にCookieの説明。
  - 「外す」ボタンはJSのfetchではなく通常のフォーム送信(POST→303→`/favorites`再取得)。一覧の増減(特に0件になったときの空表示への切り替え)をサーバー側にまかせられ、JS側の状態管理が要らないため(☆ボタンのトグルとは異なり、行の見た目を保ったまま即時反映する要件が無い)。
  - ヘッダーのメニューに「お気に入り」を追加(5項目)。戻るリンクの画面判定に`/favorites`を追加し、文言「お気に入りに戻る」を新設。`[data-favorites-tracked]`を`favorites.html`にも付け、bfcache対応をトップページと共通化。
  - `CLAUDE.md`の「画面の一部だけ失敗」の例外一覧にお気に入り画面を追記。`画面一覧.md`にお気に入り画面の節を追加し、ヘッダー・画面遷移の記述も現状(5項目・8言語)に更新。
- 確認: `mvn test`208件成功(`FavoriteServiceTest`・`ViewMapperTest`への追加分含む。worktree・merge後のmaster双方)。ローカル起動(ポート8080。他セッションが使用していなかったため)しChromeの実描画で、登録済み(見出し・列・「外す」ボタン)、見つからない行(`curl`でCookieに存在しないタグを仕込んで確認)、ライト/ダーク、モバイル幅(ヘッダー5項目の折り返し含む)、「外す」ボタンで表が更新されること、戻るリンクの文言(「«お気に入りに戻る」)を確認した。
  - Chrome拡張が途中で切断され、ユーザーに再起動を依頼して再接続後に実描画を再開した。
- [x] **本番デプロイ**(2026-09-20、`7690aa0`)。他セッションが空いたことを確認してから実施。HTTP 200、`/favorites`が200・日本語の案内文を含むことを確認、起動後のWARN/ERRORは0件、available 367MB。
- 追加対応: 2回目で保留していたトップページの「お気に入り画面へ」リンクを、お気に入り画面ができたため追加(`worktree-favorites-link`)。`mvn test`確認・実描画(トップページ→お気に入り画面への遷移)確認済み。masterへマージ・push(`20c2ce9`)まで完了。**本番デプロイは他セッションが稼働中(busy)だったため見送り**、次回空いたタイミングで実施する。

これでお気に入り機能(検討中メモの3回すべて)の実装が完了した。`お気に入り（検討中）.md`は本項目の実装が一区切りついたため、今後は同ファイルより本ログと`TODO.md`を参照する。

## 未着手・今後の検討事項

- **お気に入り機能のトップページ「お気に入り画面へ」リンク追加分の本番デプロイ**(2026-09-20、フェーズ1.39の追加対応。`20c2ce9`はpush済み・未デプロイ)
- クイズ機能など追加サービスの詳細仕様(フェーズ2、着手時に`CLAUDE.md`の「提供機能」セクションを更新)
- DB導入のタイミングと方式(プレイヤー名検索はDBを使わず、ファイルの索引で実現した。フェーズ1.23参照)
- CI/CD自動化
- ページビュー向上施策(詳細は`TODO.md`の「ページビュー向上に関するTODO」を参照)

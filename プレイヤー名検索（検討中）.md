# プレイヤー名検索（検討中）

2026-09-18にユーザーから依頼。まだ先の話になる見込みのため、`TODO.md`から本ファイルに移した(未着手)。

プレイヤー名とタグの対応を当方で蓄積し、名前でもプレイヤーを検索できるようにしたい。**条件: DBは導入せず、テキストファイルで保持する。** 想定は1〜2億件(数十GB)で、1つのファイルにまとめると検索が遅くなるため、分割して持つ。以下は2026-09-18時点の案で、未決定事項は末尾にまとめた。

## 現状(2026-09-19更新)

- **by-tag・by-nameの整理バッチと検索画面は実装済み**(`進捗ログ.md`のフェーズ1.23)。検索画面は2026-09-19にユーザーの判断で本番で有効化した(`PLAYER_NAME_SEARCH_ENABLED=true`)。削除依頼の窓口は後で作成する。以下の2026-09-18時点の記述のうち、「未着手」とあるものはこの行が優先。

## 現状(2026-09-18)

- **蓄積(inboxへの追記)だけ先に始めた**(コミット`f463b58`)。by-tag/by-nameへの整理バッチと検索画面は未着手。
- 集め方はユーザーの閲覧に便乗する方式。公式APIの応答から、次の3か所でタグと名前を記録している。
  - プレイヤー情報画面(本人と、対戦履歴に出てくる全参加者)
  - クラン情報画面(メンバー全員)
  - 個人ランキング(トップページ・個人ランキング画面。表示件数によらず取得した1000人分)
- ランキングは画面を開くたびに同じ1000人が返るため、最近書いた(タグ, 名前)の組をメモリで覚えて書かない(最大5万件・1日)。名前が変わった場合は別の組として記録される。
- 本番の保存先は`/var/lib/clash-royale-api/player-index/inbox/`(1時間ごとのファイル`yyyyMMdd-HH.tsv`、UTC)。デプロイ直後の記録は1983件(ランキング2種類とプレイヤー・クラン各1件の表示分)。
- 閲覧に便乗する方式なので、増え方はアクセス数次第。1〜2億件に届かせるには、別途巡回が必要になる見込み(下の「データの集め方」)。

## 検索方法の範囲

| 検索方法 | 実現性 | 構成 |
|---|---|---|
| 完全一致(正規化後) | ◎ | ハッシュ分割(推奨案) |
| 前方一致 | ○ | 正規化名の順に並べたファイル+各ファイルの先頭キーの索引(案B) |
| 部分一致 | × | n-gram索引が必要で容量が数倍〜十倍以上になる。見送り |

- 完全一致から始める。前方一致へ移る場合も、by-nameを作り直すだけでよい(正本のby-tagはそのまま使える)。

## 正規化(保存時と検索時で必ず同じ関数を通す)

1. `GameText.stripFormatting`でゲーム内の装飾タグを除去
2. NFKC正規化(全角・半角などの表記ゆれを吸収)
3. `toLowerCase(Locale.ROOT)`
4. 前後の空白を削除し、連続する空白を1つにする

- 正規化のルールを変えると全件の作り直しになるため、最初に決めてテストで固定する。

## ファイル構成(`/var/lib/clash-royale-api/`)

```
inbox/                 発見したプレイヤーの追記専用ログ(1時間ごとに切り替え)
  20260918-10.tsv      tag \t name \t 確認日時
by-tag/                タグ→最新の名前(正本)
  0000.tsv … 1023.tsv  tag \t name \t 最終確認日時(タグ順)
by-name/               正規化名→タグ(検索用の索引)
  0000.tsv … 4095.tsv  正規化名 \t tag \t 表示名 \t 最終確認日時(正規化名順)
```

- 検索は、正規化名のハッシュ値から1ファイルを特定して読むだけ。1ファイルは約3.7万件・2〜4MBなので、全体が数十GBでも数ms〜数十msで返せる。
- ファイル数は2のべき乗の固定値にする(変えると全件の振り分け直しになるため)。ハッシュ計算は1クラスに閉じ込める(`String.hashCode`は計算方法が仕様で固定されているので使える)。
- 容量の見積もり: by-tag・by-nameそれぞれ約7〜8GB。inboxと作り直し用の作業領域を含めて20〜30GBを見込む。

## 更新(1日1回のバッチ)

1. inboxのログを、タグのハッシュ値ごとに振り分ける
2. by-tagのファイルを1つずつ読み込み、名前の変化を反映する。名前が変わったタグは「旧名を削除/新名を追加」の差分として出力する
3. その差分を名前のハッシュ値ごとに振り分け、by-nameのファイルを1つずつ書き換える
4. 書き換えは一時ファイルに書いてから`rename`で置き換える(検索が書きかけのファイルを読まないようにするため)
5. 処理済みのinboxのログを削除する

- 常にファイル1つ分ずつ処理するので、全体を並べ替える処理が不要。メモリ1GBでも処理できる。
- 名前は変更できるため、by-tagで「そのタグの今の名前」を持ち、旧名の索引を消す。by-tagが無いと、旧名で検索しても引っかかり続ける。

## 画面の仕様

- 入力がタグの形式(先頭が`#`、または`0289PYLQGRJCUV`だけで構成)ならタグとして扱い、それ以外は名前検索にする(クラン検索と同じ考え方)。
- 名前は重複するので、結果は候補の一覧にする(クラン名検索の結果画面と同じ形)。最終確認日時の新しい順に並べる。
- 「当サイトが過去に確認した名前であり、現在の名前とは異なる場合がある」旨を注記する。プレイヤー情報画面に移れば、公式APIから最新の名前を取得して表示する。

## 着手前に検討・決定が必要なこと

- **データの集め方(最大の難所)**: 公式APIにはプレイヤーを一覧で取得する機能が無い。対戦履歴の巡回(1回の呼び出しで最大30人前後)か、クランのメンバー一覧の巡回(1回で最大50人、こちらの方が効率的)で集める。どちらも1億人を集めるには数百万回以上の呼び出しが要る。仮に毎秒5回でも100万回で約2.3日かかり、しかも集めるほど新しく見つかる割合が下がる。呼び出し回数の上限は未確認なので、試しに巡回して1日に何人増えるかを確かめるのが先決。
- **VMのスペック**: 現行VM(メモリ1GB)には巡回とバッチまで載せられない見込み。`TODO.md`の「VMの乗り換え」を先に行う。無料枠のブロックボリュームは合計200GBまでで、今の起動ディスクは50GB弱なので、ディスクの拡張か追加が必要。
- **バックアップ**: データが消えると集め直しに数週間以上かかる。無料枠のObject Storageは20GBまでなので、全データを置くには足りない可能性がある。
- **利用規約**: 他人のプレイヤー名を大量に蓄積して検索させることが、Supercellの利用規約やファンコンテンツポリシーに反しないかを確認する。
  - **2026-09-19に確認**(法的な判断ではなく、条文を読んだ範囲での整理):
    - 公式APIに独自の規約は無い。developer.clashroyale.comの「Getting Started」は、API Terms and Conditionsとしてファンコンテンツポリシーを参照しており、フッターの「Terms of Service」はSupercell全体の利用規約に飛ぶ。
    - **APIデータの保存・巡回を禁じる条文は見当たらない**。呼び出し回数はAPI側の制限(超過すると呼び出しが失敗する)で管理される前提。
    - 利用規約で関係しそうなのは「Collect or post anyone's private information, including personally identifiable information … through the Service」。タグとゲーム内の名前はゲーム内で誰でも見られる公開情報なので、個人を特定できる私的な情報には当たらないと考えるが、名前に本名を使う人もいる。検索画面を公開する前に、**削除依頼の窓口**(自分の名前を検索結果から外してほしい人向け)を用意するのが無難。
    - ファンコンテンツポリシーの必須事項として、**非公式である旨の表示**(“This material is unofficial and is not endorsed by Supercell. For more information see Supercell's Fan Content Policy: www.supercell.com/fan-content-policy.” またはほぼ同等の文)が要る。**2026-09-19時点でサイトに表示が無い**(名前検索とは別に、サイト全体として対応が必要)。
    - 収益化は広告・寄付・コーチングのみ可(現状は該当なし)。
- **ユーザーに決めてもらうこと**: 検索方法の範囲(完全一致で始めてよいか)、データの集め方(クランのメンバー一覧を中心にするか)、VMの乗り換えを先に行うか。
- 参考: この案は実質的にDBの索引を自前で作るもの。同時に書き込まれたときの整合、途中で落ちたときの復旧、定期的なファイルの再構成は自前で面倒を見ることになる(DBを入れない方針は変えない前提で、判断材料として記録)。

## タグ収集のバッチ化(2026-09-19に検討、未決定)

ユーザーから「タグ収集のバッチ化を検討して」と依頼。閲覧に便乗する今の方式では足りないため、自分から巡回して集めるバッチ(以下、巡回バッチ)を足す案。上の「更新(1日1回のバッチ)」は集めたものを整理する別のバッチで、巡回バッチはinboxに同じ形式で書くだけにし、両者を切り離す。

### 現状の実測(本番、2026-09-18 10時〜09-19 01時 UTC)

- inboxは34,247行だが、**タグの重複を除くと6,521人**。約15時間で数千人のペースで、1〜2億件には桁が全く足りない。
- 重複が多いのは、デプロイで再起動するたびに「最近書いた組」のメモリが消え、ランキングの1000人を書き直すため。整理バッチで吸収できるので問題ではない。
- VMのメモリ: 945MB中、使用可能342MB。アプリのJVMは常駐約245MB。ディスクの空きは23GB。

### 公式APIで試した結果(ローカルのキーで2026-09-19に確認)

| 取得元 | 1回で得られる人数 | 次の巡回先 | 結果 |
|---|---|---|---|
| クランのメンバー一覧(`/clans/{tag}`) | 最大50人 | 無し | 既存 |
| クラン対戦の履歴(`/clans/{tag}/riverracelog?limit=1`) | **約125人**(5クラン分の参加者) | 対戦相手の4クラン | 1回あたりの人数が最も多い |
| クランランキング(`/locations/{id}/rankings/clans`) | クラン最大1000件 | クランのタグ | 日本で999件 |
| クラン検索(`/clans?locationId=..&minMembers=..&maxMembers=..`) | クラン最大640件 | クランのタグ | 名前なしでも国・人数で絞れば使える |

- クラン検索は**1回の結果が640件で打ち切られ**、続きのカーソルも返らない(`limit=100`なら`after`が返るが、合計640件で止まる)。国×人数(2〜50)で細かく分けて何度も呼べば、多くのクランを列挙できる見込み。網羅できるかは未確認。
- `minMembers=1`や`minScore`単独は`badRequest`。名前は3文字以上が必要。

### 推奨案: アプリと同じプロセスで動かす(`@Scheduled`)

- **別プロセス(別のJVM)にしない**。2つ目のJVMは最低でも150〜250MB使うため、使用可能342MBの今のVMではメモリ不足でフリーズする恐れが高い(既に何度か起きている)。
- **Spring Batchは使わない**。強み(ジョブの再開管理、チャンク単位のトランザクション)は、DBと終わりのある入力を前提にしたもの。巡回は「クランのタグを順に処理し続ける」終わりの無いキューなので、恩恵が小さい。
- 同じプロセスなら、画面からの呼び出しと巡回で**API呼び出しの間隔を1か所で管理できる**(巡回のせいで利用者が429になるのを防ぎやすい)。
- VMを乗り換えたら、同じjarを巡回専用の設定(`crawler.enabled=true`、Web側はオフ)で別に起動する形へ移せるよう、巡回は1つのパッケージに閉じ込めて`@ConditionalOnProperty`で切り替えられるようにしておく。

### 実装上の注意点

- **キャッシュを通さない**: `ClashRoyaleApiClient`の各メソッドは`@Cacheable`付き。巡回からそのまま呼ぶと、Caffeineに巡回結果が溜まってメモリを圧迫し、利用者の分を追い出す。巡回用にキャッシュしない呼び出し口を用意する。
- **重複除去のメモリを使わない**: `PlayerSightingLog`の「最近書いた組」(最大5万件)が巡回で埋め尽くされ、役に立たなくなる。巡回は別ファイル(例: `inbox/crawl-yyyyMMdd-HH.tsv`)に重複を気にせず書き、重複除去は整理バッチに任せる。
- **巡回済みのクランをメモリで持たない**: クランは数百万件規模になり得るため、HashSetだと数十〜百MB以上になる。「今回の周回で回るクランのタグ一覧」をファイルにしておき、何行目まで処理したかをファイルに記録する。再起動(デプロイ)しても続きから再開できる。周回が終わったら、新たに見つけたクランを加えて重複を除き、次の周回のファイルを作る。
- **呼び出し間隔**: 1本のスレッドで一定間隔(最初は2秒に1回程度)。429が返ったら間隔を広げて一時停止する。間隔と有効/無効は`/etc/clash-royale-api/env`で変えられるようにし、ビルドし直さずに止められるようにする。
- **見積もり**: 毎秒1回でも1日8.6万回。クラン対戦の履歴中心なら1回あたり数十〜百人強なので、重複前で1日数百万件、inboxは1日百数十MB。整理バッチが無いとディスクを食うので、整理バッチも近いうちに必要になる。

### 進め方の案

1. **まずローカルで試す**(本番に影響しない): ローカルのキーで数千回ほど巡回し、1回あたりの新規人数の推移と、429が出る間隔を実測する。呼び出し回数の上限が未確認という課題もここで確かめられる。
2. 結果が良ければ、本番で低い頻度から動かし、1週間ほどメモリ・ディスク・画面の応答を見る。
3. inboxが増えてきたら、整理バッチ(by-tag/by-name)に着手する。

- 利用規約の確認(上記)は、本番で大量に巡回する前に済ませる。

### 試作とローカルでの実測(2026-09-19)

ユーザーの判断で「ローカルで試作・実測」に着手。さらに、クランに入っていない人も拾うため**2段構え**(クラン対戦の履歴を主力に、5回に1回をプレイヤーの対戦履歴)にした。どちらも1秒2回・ヒープ256MBで、各20分前後回した。

- 実装: `crawler`パッケージ(`Crawler`・`TagQueue`・`CrawlerConfig`)。`crawler.enabled=true`のときだけ、Springの定期実行(fixed delay)で1回ずつAPIを呼ぶ。巡回用のAPI呼び出し(`getRiverRaceLog`・`getBattleLogUncached`)はキャッシュを通さない。集めた分は`inbox/crawl-yyyyMMdd-HH.tsv`に重複を除かず書く。既定は無効なので、本番には影響しない。
- **429は3回とも0回**。1秒2回でも制限に掛からなかった(上限は依然不明)。メモリは常駐で約190MB。
- **クランに入っていない人**: プレイヤーの対戦履歴の相手の約31%がクラン無し(15人分の標本)。対戦履歴の巡回で拾った人の約87%が新規だった。
- **周回の切り替えで新規が0%に落ちた(2回発生)**:
  1. 周回の切り替えで、回ったばかりのクランを最初から回り直していた。→ 一度回ったタグを`visited/`に記録し、次の周回は未訪問のものだけにした(尽きたら全件を回り直す)。
  2. それでも2周目で新規が4%→0%に落ちた。2周目のクランは1周目のクランの**対戦相手**なので、`limit=1`(直近1回のクラン対戦)では**同じ対戦**が返り、参加者が全員既知になる。
- 2周目のクラン30件で、`limit`ごとの新規人数を測った:

| limit | 1回あたりの新規人数 | 1回あたりの記録行数(重複込み) |
|---|---|---|
| 1 | 0 | 245 |
| 3 | 270 | 907 |
| 5 | 270 | 1,447 |
| 10 | 525 | 2,777 |

- **2026-09-19追記: 対応済み**。ユーザーが「整理バッチを先に作る」を選択し、`PlayerIndexCompactor`(inbox→by-tag、毎日18:30 UTC)を実装した。`races-per-clan`の既定も10にした。詳細は`進捗ログ.md`のフェーズ1.23。
- **新たな課題: ディスク使用量**。`limit=10`だと1回の応答の大半が既知の人で、重複を除かずに書くと1日あたり(2秒間隔で4.3万回)**約2〜5GB**になり、今のVMの空き(23GB)を数日〜十日で使い切る。整理バッチで重複を除くか、書く前に除く仕組みが本番投入の前提になる。

# Clash Royale API 画面一覧

ユーザーとClaude Codeとのコミュニケーションで画面を指す際は、以下の名称で統一する。
(2026-09-14にユーザーと合意。新しい画面を追加した場合は、このファイルに追記する)

## 画面

| 画面名 | URL | 概要 |
|---|---|---|
| トップページ | `/` | 検索フォームと個人ランキング(上位10人) |
| 個人ランキング画面 | `/ranking/players` | 個人ランキング上位1000人 |
| クランランキング画面 | `/ranking` | クランランキング上位1000クラン |
| プレイヤー情報画面 | `/player/{tag}` | プレイヤーの情報と対戦履歴 |
| クラン情報画面 | `/clan/{tag}` | クランの情報とメンバー一覧 |
| クラン名検索結果画面 | `/clan/search?q=...` | クラン名で探した候補一覧 |
| プレイヤー名検索結果画面 | `/player/search?q=...` | プレイヤー名で探した候補一覧(設定`PLAYER_NAME_SEARCH_ENABLED`で有効にしたときだけ出る。本番は2026-09-19から有効) |
| 対戦詳細画面 | `/player/{tag}/battles/{battleTime}` | 対戦1件の両者のデッキ |
| カード一覧画面 | `/cards` | 全カードをレアリティ別に一覧。名前で絞り込み |
| カード詳細画面 | `/card/{id}` | カード1枚の画像・レアリティ・エリクサー・レベル範囲 |
| お気に入り画面 | `/favorites` | お気に入り登録したプレイヤー・クランの一覧と解除 |
| エラー画面 | (各画面でエラー発生時) | 原因別のエラーメッセージ |

## 各画面の表示内容

### トップページ

- プレイヤー検索フォーム
- クラン検索フォーム(タグ・クラン名の両対応)
- 個人ランキング上位10人(順位・プレイヤー・レーティング)。国・地域別の切替タブあり(国が決まれば国別タブを初期表示)
- 個人ランキング画面・クランランキング画面へのリンク

### 個人ランキング画面

- ランク戦(旧称パス・オブ・レジェンド。2025年6月に公式が改名)の現在シーズンの上位1000人(順位・プレイヤー・レベル・レーティング・クラン)。1000人は公式APIが返せる上限
- 国・地域別の切替タブあり

### クランランキング画面

- クランスコア上位1000クラン(順位・クラン・国・地域・クランスコア・メンバー数。国・地域の列はグローバルのタブだけに出る)。1000クランは公式APIが返せる上限(2026-09-19に1000件まで拡大。経緯は`進捗ログ.md`のフェーズ1.29)
- 国・地域別の切替タブあり

### プレイヤー情報画面

- 基本情報(トロフィー・最高トロフィー・レベル・勝敗数・3クラウン勝利数・所属クラン)
- 基本情報には、連勝・連敗(0のときは出さない。対戦履歴と一致しないことがある旨の注記付き)と、ランク戦の今シーズン・最高シーズンの順位とレーティング(順位があるときだけ)も出す
- 使用中のデッキ(ゲーム内で今セットしているデッキ8枚とタワーユニット。公式APIが返さなかった場合は案内文を出す)
  - デッキの下に平均エリクサー・4枚サイクル・平均カードレベルと「デッキをコピー」ボタン(ゲームを開いてデッキをコピーする。8枚そろわない・エリクサー不明のカードを含むデッキでは出さない)
- 戦績サマリー、得意/苦手カード(5回以上対戦したカードが無い場合は、表示せず案内文を出す)
- 直近の対戦履歴一覧(日時・モード・結果・クラウン・対戦相手・「詳細」ボタン)。2v2では相手2人と味方も表示。対戦相手の名前の横に、その対戦で使ったデッキの平均カードレベル

### クラン情報画面

- クランスコア・メンバー数
- メンバー一覧(役職・トロフィー・寄付数・最終アクセス)。列見出しでソート(`?sortBy=...&sortDir=asc/desc#members`、並べ替え後も一覧の位置に戻る)
- 最終アクセスが7日以上前のメンバーは警告色で表示

### クラン名検索結果画面

- クラン名の部分一致で見つかった候補一覧(クランスコア上位20件)。見出しは「「検索語」の検索結果」
- 該当なしの場合は、案内文と、入力値を残したクラン検索フォーム

### プレイヤー名検索結果画面

- 当サイトが確認したことのあるプレイヤーを名前で探した候補一覧。見出しは「「検索語」の検索結果」。1ページ最大50人
  - 「名前が一致したプレイヤー」: 完全一致した人の全員。最後に確認した日時の新しい順で、50人を超えるときは「前の50人」「次の50人」でページ送りする(`?page=N`)
  - 「「検索語」で始まる名前」: 完全一致が50人未満のときだけ、残りの枠を前方一致の人(名前順)で埋める
  - 前方一致の人を表示しきれないときは「検索結果が多すぎるため、一部だけ表示しています。名前を続けて入力すると絞り込めます」と案内する
- 一致の判定では、装飾タグ・大文字小文字・全角半角・空白の数の違いを区別しない。検索語は1文字から受け付ける
- 各候補に名前・タグ・最終確認日時と「見る」ボタン。名前は最後に確認した時点のもので、現在の名前と異なる場合がある旨を注記する
- 該当なしの場合は、案内文(完全一致であること、見つかるのは当サイトが確認した人だけであること)と、入力値を残したプレイヤー検索フォーム

### 対戦詳細画面

- 両者の名前・勝敗・クラウン数(2v2では見ているプレイヤー本人を太字)
- 使用デッキ8枚とタワーユニット。見出しは「自分のデッキ」「味方のデッキ」(2v2)「相手のデッキ」
- 各デッキの下に平均エリクサー・4枚サイクル・平均カードレベルと「デッキをコピー」ボタン(プレイヤー情報画面の使用中のデッキと同じ)

### カード一覧画面

- 全カードをレアリティごと(ノーマル・レア・スーパーレア・ウルトラレア・チャンピオン)に、エリクサーの軽い順で並べる。タワーユニットは最後にまとめる
- 各カードは画像(カード詳細画面へのリンク)・名前・エリクサー
- カード名での絞り込み(表示言語の名前・英語名のどちらでも。日本語はひらがなでも、スペイン語・ポルトガル語はアクセント記号なしでも可)。ブラウザ内で即時に絞り込み、該当が無ければ案内文を出す

### カード詳細画面

- カード名(英語以外の表示言語では英語名も併記)
- カード画像。進化のあるカードは通常と進化の2枚を「通常」「進化」のラベル付きで並べる
- レアリティ、エリクサーコスト(タワーユニットには無いので出さない)、レベル範囲(ゲーム内表記。例: ウルトラレアは「9〜16」)
- 公式APIは説明文やHP・ダメージなどのステータスを返さない旨の注記
- 存在しないカードIDは「見つからない」系のエラー画面(404)

### お気に入り画面

- お気に入り登録したプレイヤー・クランを、それぞれ表で表示(登録が新しい順)。各行に「外す」ボタン
  - プレイヤー: 名前・タグ・トロフィー・ランク戦のレーティング(あれば)・所属クラン
  - クラン: 名前・タグ・クランスコア・メンバー数(n/50)
- 1件ずつ取得できなくてもエラー画面にはせず、行ごとに「見つかりませんでした」(404、自動では外さない)・「最新の情報を取得できませんでした」(それ以外)を出す
- 1件も無いときは登録方法の案内と、プレイヤー検索・クラン検索のフォームを表示する
- 表の下に、お気に入りはこのブラウザ(Cookie)に保存される旨の注記
- プレイヤー情報画面・クラン情報画面の見出し横の☆ボタンでも登録・解除できる(登録名は公式APIから取り直す。タグごとにプレイヤー・クラン各10件まで)

### エラー画面

- 原因別のメッセージ: プレイヤー・クランが見つからない、対戦が履歴から消えた、公式APIに接続できない・拒否された、アクセス集中、不正なリクエスト、存在しないURL、その他
- 「見つからない」系(存在しないタグ・対戦・URL)は、エラーの赤ではなく中立的な案内と、プレイヤー検索・クラン検索のフォームを表示する

## 画面をまたぐ共通の仕様

- **ヘッダー**: 全画面共通。アプリ名(トップページへのリンク)、メニュー(検索・個人ランキング・クランランキング・カード・お気に入り)、言語メニュー(「🌐 現在の言語 ▾」を押すと8言語の一覧が開くドロップダウン。`?lang=ja` / `en` / `es` / `pt` / `de` / `fr` / `it` / `ru`、Cookieに保存)。
- **戻るリンク**: トップページ以外の画面にある。**ひとつ前の画面に戻る**(ブラウザの履歴で戻る)。文言は直前の画面に合わせて「クラン情報に戻る」「個人ランキングに戻る」などに変わる(画面一覧にない画面からは「戻る」)。
  - 直接開いた・新しいタブで開いたなど、戻る履歴が無いときは従来のリンクになる: 対戦詳細画面は元のプレイヤー情報画面、それ以外は「トップへ戻る」。
  - 言語切替・クランメンバーの並べ替え・国選択は、同じ画面のままなので履歴を積まない(ブラウザの戻るボタンでも、切替前の状態には戻らず前の画面に戻る)。
- **国・地域別の切替タブ**: トップページ・個人ランキング画面・クランランキング画面で共通の部品。
  - 「グローバル」と「選択中の国」の両方を最初に読み込んでおき、タブの切替では通信しない。
  - 対象国の決まり方: `?country=JP`で指定 > Cookieに保存した前回の選択 > ブラウザのAccept-Language > 表示言語の代表的な国(ja→日本、es→スペイン、pt→ブラジル、en→米国) > 米国 > 国一覧の先頭。
  - 国選択フォームで国を選ぶとCookieに保存され、3画面で共有される。一覧に無い国を`?country=`で指定した場合は、選んだものとみなさず、Cookieにも保存しない。
  - 最初に開くタブ: トップページは国が決まれば国別、個人ランキング画面・クランランキング画面はグローバル。どの画面も、国を選び直した直後は国別を開く。

## 画面遷移

- 全画面 → トップページ・個人ランキング画面・クランランキング画面・カード一覧画面・お気に入り画面(ヘッダーのメニュー)
- トップページ → お気に入り画面(お気に入りが1件以上あるときのカードから)
- お気に入り画面 → プレイヤー情報画面・クラン情報画面(名前のリンク)
- トップページ → プレイヤー情報画面(プレイヤー検索、または個人ランキングのプレイヤー名)/ プレイヤー名検索結果画面(名前検索が有効で、タグとして見つからなかった場合)
- プレイヤー名検索結果画面 → プレイヤー情報画面
- トップページ → クラン情報画面(クラン検索でタグが見つかった場合)/ クラン名検索結果画面(それ以外)
- トップページ → 個人ランキング画面・クランランキング画面(リンク)。個人ランキング画面とクランランキング画面は相互にリンクしている
- 個人ランキング画面 → プレイヤー情報画面・クラン情報画面(名前のリンク)
- クランランキング画面・クラン名検索結果画面 → クラン情報画面
- プレイヤー情報画面 → クラン情報画面(所属クラン)、対戦相手・味方のプレイヤー情報画面、対戦詳細画面(「詳細」ボタン)
- クラン情報画面 → メンバーのプレイヤー情報画面
- 対戦詳細画面 → 参加者全員のプレイヤー情報画面
- プレイヤー情報画面(得意/苦手カード・使用中のデッキ)・対戦詳細画面 → カード詳細画面(カード画像のリンク。タワーユニットも含む)

## 補足

- URL中の`{tag}`は先頭の`#`を除いたタグ(例: `/player/VRQUQ0QL`)。
- `/player/search?q=...`は、名前検索が無効(既定)なら画面を持たず、入力を正規化して`/player/{tag}`へリダイレクトするだけ。
  - 名前検索が有効なら: `#`付きはタグとしてそのままリダイレクト。タグに使われる文字(`0289PYLQGRJCUV`)だけの入力は、そのタグのプレイヤーが実在すればリダイレクトし、いなければ名前として探す。それ以外は名前として探し、プレイヤー名検索結果画面を出す。
- `/clan/search?q=...`は、入力がタグの形式でそのタグのクランが実在すれば、クラン名検索結果画面を出さずに`/clan/{tag}`へリダイレクトする。
- `/player/search`・`/clan/search`で検索語が空の場合は、トップページへリダイレクトする。

## 更新履歴

- 2026-09-17: URLを実装に合わせて更新し、クラン名検索結果画面・エラー画面を追記
- 2026-09-17: クランランキング画面を追加
- 2026-09-17: 個人ランキング画面を追加。同日、トップページに上位100人のタブ付きランキングを載せ、個人ランキング画面は200人に拡大
- 2026-09-17: 画面デザインのレビュー指摘を反映。トップページの個人ランキングを10人に、ヘッダーにメニューを追加、戻るリンクを「トップへ戻る」に、見つからないときに検索フォームを表示、対戦履歴に「詳細」ボタンと2v2の味方表示を追加
- 2026-09-17: 実装と突き合わせて全面的に見直し。画面をまたぐ共通の仕様(ヘッダー・戻るリンク・国・地域別の切替タブ)と画面遷移の節を追加し、各画面の表示項目・エラーの種類を実装に合わせて補った
- 2026-09-17: 再度実装と突き合わせ、クランランキング画面の「国・地域」列がグローバルのタブだけに出ること、国別タブの初期表示と一覧に無い国の扱いを追記。パス・オブ・レジェンドの公式の改名(ランク戦)を反映
- 2026-09-19: カード詳細画面を追加。同日、戻るリンクを開いた元の画面に戻るよう変更し、さらに全画面の戻るリンクをひとつ前の画面に戻る方式にした(カード詳細専用の仕組みは撤去)
- 2026-09-19: プレイヤー情報画面の使用中のデッキと対戦詳細画面に、平均エリクサー・4枚サイクル・デッキのコピーボタンを追加
- 2026-09-19: デッキの指標に平均カードレベルを追加。対戦履歴一覧の対戦相手にも平均カードレベルを表示
- 2026-09-19: プレイヤー名検索結果画面を追加し、同日本番で有効にした。同日、前方一致と完全一致のページ送りに対応
- 2026-09-20: お気に入り画面を追加。ヘッダーのメニューと言語メニューの説明を現状(お気に入り追加・8言語)に更新

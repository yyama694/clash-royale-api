# TODO

## 全体コードレビュー(2026-09-15、2回目)の対応結果

差分ではなくコードベース全体を対象にレビューを実施し、指摘事項をすべて対応済み(コミット `79a67ed` ほか)。
対応内容の詳細は`進捗ログ.md`および当該コミットメッセージを参照。

**対応済み**
- Service層(`PlayerService`/`ClanService`)と`domain`パッケージを新設し、Controllerから業務ロジックを分離
- ViewModel(`web/view`)+`ViewMapper`導入により、テンプレートから`T(...)`によるstaticメソッド呼び出し(7箇所)と勝敗判定を排除
- ラベル3クラス(`CardNameLabels`/`GameModeLabels`/`RoleLabels`)を廃止し`messages*.properties`に集約。固定文言含め全面多言語化(`?lang=ja`/`?lang=en`)
- URLをパス形式(`/player/{tag}`、`/clan/{tag}`)へ変更。検索は`/player/search?q=`、`/clan/search?q=`に分離
- 対戦詳細をindex参照からbattleTime参照に変更(黙って別の対戦が表示される問題を解消)
- 得意カード/苦手カードの重複表示を修正。Wilson score intervalで試行回数を加味した順位付けに変更
- 2v2で相手2人目のデッキが集計から漏れていた問題を修正
- client層で独自例外に変換し、`@ControllerAdvice`でエラー画面を集約(旧TODO 3・5・6が同時に解決)
- Spring Boot 3.3.4 → 4.1.1(3.3系・3.5系ともOSSサポート終了のため)
- APIキーをjarから排除(ローカルは`./config/`、本番は環境変数`CLASHROYALE_API_TOKEN`)
- `@NotBlank`によるfail-fast、デフォルトprofileから`local`を除去
- `RestClient.Builder`のDIとタイムアウト設定(connect 3s / read 8s)
- Caffeineによる2分TTLキャッシュ(レート制限・低スペックVM対策。実測で11倍高速化)
- テンプレートのフラグメント化、`lang`属性、フォームの`required`、ダークモード対応、`deck-grid`のauto-fit化
- テストを純粋ロジック+Mockitoの範囲で再構成(36件。MockMvc/MockRestServiceServerは引き続き不使用)

**今回対応しなかったもの(継続TODO)**
- (2026-09-17、ユーザー判断で見送り)可観測性(Actuator未導入、ヘルスチェック・死活監視なし)。2026-09-15にVM無応答が2回発生しており優先度は高い
- ログのローテーション設定
- JVMのメモリ上限指定(`-Xmx`等)。VMフリーズ対策として検討の余地あり(今回はOOMの痕跡が無かったため見送り)
- HTTPS未対応(下記「ページビュー向上に関するTODO」の1番で対応)

## ソースコードレビュー(2026-09-17、3回目、一部対応済み)

コミット`a21f7a9`の時点で、Java・テンプレート・設定を全件読んだ。画面デザインと多言語化の観点は下の各節にあるため、それ以外(性能・堅牢性・構造・セキュリティ)に絞った。実測は、HEADをスクラッチ領域に展開してポート18081で起動し、curlで行った。

### 優先度高

**1. トップページの表示速度が、公式APIの応答に引きずられる**(2026-09-17、ユーザー判断で見送り)
- トップページは、個人ランキングを出すために公式APIを最大3回、順番に呼ぶ(国一覧、グローバル、選択国)。
- 実測: キャッシュが空のとき1.40秒、キャッシュが効いているとき0.03秒。キャッシュの有効期間は2分なので、アクセスの少ない時間帯はほとんどの訪問が遅い方になる。
- 公式APIが止まったときは、失敗してもエラー画面にはならないが、タイムアウトまで待つ。設定上、接続できなければ約9秒(3秒×3回)、接続後に応答が無ければ最大24秒(8秒×3回)。その間、トップページの検索フォームも表示されない。
- 失敗はキャッシュされないので、障害中はアクセスのたびに3回ずつ呼び直す(そのたびにWARNログも出る)。
- 対応案:
  - 3回の呼び出しを並列にする
  - ランキングの取得に、トップページ用の短いタイムアウトを設ける
  - 一定時間の失敗をキャッシュする(ネガティブキャッシュ)
  - 定期ジョブでランキングを先に取得しておき、リクエスト時にはAPIを呼ばない(個人ランキング・クランランキングは全訪問者で同じ内容なので相性がよい)

**2. キャッシュが切れた瞬間に、同じAPIを同時に何回も呼ぶ**(2026-09-17、ユーザー判断で見送り)
- `@Cacheable`は既定で`sync = false`のため、キャッシュが空のときに同時に来たリクエストは、それぞれが公式APIを呼ぶ。
- トップページは全訪問者が同じキー(`global`など)を使うので、アクセスが集中したときに重なりやすい。レート制限と低スペックVMの負荷の両面で不利。
- ランキング・国一覧のように全員で共有するキャッシュは、`@Cacheable(sync = true)`にする。

### 構造・効率

**3. 同じランキングを件数違いで別々に取得している**(2026-09-17に対応済み、`4cc710d`)
- キャッシュのキーが`(locationId, limit)`のため、トップページ(100人)と個人ランキング画面(200人)で、同じデータを公式APIから2回取得する。
- 200人分を1回取得してトップページ側で先頭100人に切り出せば、呼び出し回数とキャッシュの量を減らせる。
- 対応: `RankingService#topPlayers`が常に`MAX_PLAYER_RANKING_SIZE`(200)で取得し、画面の件数に切り出すようにした。詳細は`進捗ログ.md`。

**4. 国一覧のキャッシュが2分で切れる**
- `ClashRoyaleApiClient#getLocations`のコメントには「内容はほぼ変化しない」とある。それでも全キャッシュ共通の設定(`expireAfterWrite=2m`)のため、2分ごとに取り直している。
- 国一覧だけ有効期間を長くする(例: 1日)。Caffeineのキャッシュごとに設定を分ける必要がある。

**5. `LocationService#byCountryCode`が本番コードから使われていない**
- 呼び出し元はテストだけ。同じ処理が`CountryPreference#find`にもある(国コードの正規化と検索)。
- どちらかに寄せる。`RankingScope`を作ったので、国を決める処理は`web`側にまとまっている。

**6. `RankingScope`にテストが無い**(2026-09-17に対応済み、`9dd2134`)
- 国を決めてModelに詰める処理を、3つのControllerから切り出した部品。「国を明示的に選んだときだけCookieに保存する」「その直後だけ国別タブを開く」という分岐がある。
- `CountryPreference`のテストと同じく、Mockitoの範囲で書ける。

### 堅牢性・セキュリティ

**7. プレイヤー検索に`/`を含めると、Tomcat既定のエラーページになる**
- 実測: `/player/search?q=ab/c`は`/player/AB%2FC`にリダイレクトされる。Tomcatは既定でエンコードされた`/`を含むパスを拒否するため、HTTP 400の素のエラーページ(英語・サイトのデザイン無し)が返る。
- タグに`/`は含まれないので、`Tags`で「タグとして使えない文字を含む入力」を検出し、リダイレクトする前に「見つからない」の画面を出す。

**8. 内部向けのHTMLコメントが、利用者に配信される**
- `error.html`のコメント(「errorKey は GlobalExceptionHandler が原因別に設定する…」)と、`battle-detail.html`のコメントが、そのままレスポンスに含まれる。実測でエラー画面のHTMLに含まれていることを確認した。
- 内部のクラス名が見えるだけで実害は小さいが、Thymeleafのパーサーレベルコメント(`<!--/* ... */-->`)にすれば配信されない。

**9. セキュリティ関連のレスポンスヘッダが無い**
- 実測: `X-Content-Type-Options`・`X-Frame-Options`(または`Content-Security-Policy`の`frame-ancestors`)がどれも付いていない。Spring Securityを入れていないため。
- ログインや入力を伴う操作が無いので、クリックジャッキングなどの実害は小さい。HTTPS化(「ページビュー向上に関するTODO」の1)のときに、リバースプロキシかフィルタで一緒に付ける。

## 静的リソース(CSS/JS)のキャッシュ対策(2026-09-16に判明、2026-09-17に対応済み)

**対応結果(2026-09-17)**: ユーザー判断で対応案1(コンテンツハッシュ付きURL)を採用した。`application.yml`に`spring.web.resources.chain.strategy.content.enabled: true`と`cache.cachecontrol.max-age: 365d`を追加。ローカルで、CSSのURLが`/css/style-<ハッシュ>.css`になり、`Cache-Control: max-age=31536000`が返ることを確認した。以下は判明時の記録。

**問題**: CSSを変更してデプロイしても、ブラウザが古いCSSをキャッシュから使い続けて変更が反映されない。2026-09-16の対戦詳細画面のタワーユニット表示変更で実際に発生(詳細と実測値は`進捗ログ.md`参照)。Spring Bootの静的リソース配信がデフォルトで`Cache-Control`も`ETag`も返さないため、ブラウザがヒューリスティックキャッシュを適用するのが原因。CSS/JSを変更するたびに毎回起きる構造的な問題。

**対応案(ユーザー未決定)**
1. **コンテンツハッシュ付きURL(推奨)**: `spring.web.resources.chain.strategy.content.enabled=true`を設定する。テンプレート側は`fragments/layout.html`ですでに`th:href="@{/css/style.css}"`(Thymeleaf経由)になっているため、設定の追加だけで効く。中身が変わるとURL自体が変わるためキャッシュ事故が原理的に起きない。
2. **常に再検証**: `spring.web.resources.cache.cachecontrol.no-cache=true`。設定1行で済むが毎回サーバーへ問い合わせが発生する(低スペックVMには不利)。
3. 手動でクエリ`?v=2`を付与。更新忘れが起きるため非推奨。

**暫定回避**: 表示が変わらない場合は`Ctrl + Shift + R`(スーパーリロード)で確認する。デプロイ後の動作確認時も同様。

## ページビュー向上に関するTODO(2026-09-14、別セッション経由でユーザーから記録依頼)

着手時期は未定。土台整備を先に済ませてから集客施策に進む想定。

**土台として先に整えるべきもの**
1. 独自ドメイン取得 + HTTPS化(SNS等でリンク共有しても怪しく見えないようにするため。旧「ドメイン取得・HTTPS化」項目と統合)
2. OGP/メタタグ設定(X/Discordでリンクをシェアしたときにタイトル・説明・画像が表示されるように)
3. 低スペックVM(Always Free枠)でのアクセス急増に対する耐性確認(過去にVMフリーズが発生した実績あり。[[project-oci-vm-spec]]参照)

**集客・再訪施策**
4. 差別化ポイント(日本語での戦績・カード名表示)を訴求する形にサイト説明文・タイトルを調整
5. X(旧Twitter)・Discordなど日本語クラロワコミュニティへの直接告知(検索流入だけに頼らない)
6. 再訪動機になるコンテンツ(クイズ機能など、既存の「フェーズ2」構想と紐付け)
7. アクセス解析の導入(施策効果測定のため、本格的な集客施策より先に仕込んでおくと良い)

## 画面デザインの改善候補(2026-09-16に洗い出し、2026-09-17に実描画で再レビュー、2026-09-17にほぼ対応済み)

### 対応結果(2026-09-17)

ユーザーの指示で、下の指摘をまとめて修正した。仕様の判断が要る2件はユーザーに確認した。
- 優先度高の1: トップページを10人に絞る
- 追加分の24: 5回以上対戦したカードが無ければ、得意/苦手カードを出さない

確認は、ローカルでの実描画(PC幅・360〜375px幅・ダークモード・キーボード操作)で行った。経緯とハマりポイントは`進捗ログ.md`のフェーズ1.11を参照。

| 番号 | 内容 | 状態 |
|---|---|---|
| 1 | トップページが長すぎる | 対応済み。個人ランキングを10人にした(高さはPC幅で5,574px→1,615px) |
| 2 | ダークモードのボタン・ヘッダーのコントラスト | 対応済み。色の変数を「白文字を載せる背景」と「文字色」に分けた(ボタン6.67:1) |
| 3 | 所属クランのリンク色 | 対応済み |
| 4 | 画像の読み込みでガタつく | 対応済み。`width`/`height`属性(285×420)を指定 |
| 5 | 「検索に戻る」の文言 | 対応済み。「トップへ戻る」に変更(パンくずは入れていない) |
| 6 | ヘッダーにメニューが無い | 対応済み。検索・個人ランキング・クランランキングを追加 |
| 7 | 「もっと見る」の使い回し | 対応済み。クランランキング画面は「個人ランキングを見る」 |
| 8 | 見つからないときの行き止まり | 対応済み。中立色の案内+検索フォーム(クラン検索は入力値を残す) |
| 9 | 検索語がタグと同じ見た目 | 対応済み。見出しを「「償い」の検索結果」にした |
| 10 | 日時にリンクが付いている | 対応済み。行末に「詳細」ボタンを置き、日時はただの文字にした(書式は多言語化の候補5) |
| 11 | ソートできる見出しが分からない | 対応済み。未ソート列に⇅ |
| 12 | 数値列が左揃え | 対応済み。右揃え+等幅数字(スマホのカード表示では左揃え) |
| 13 | 得意/苦手の強弱 | 対応済み。勝ち色/負け色の下線と勝率の色 |
| 14 | クランランキング上位が同点 | 未対応。公式APIのデータ由来で、画面側で出せる別の指標を調べていないため |
| 15 | 長期間アクセスの無いメンバー | 対応済み。7日以上を警告色 |
| 16 | 個人ランキングのカード表示が冗長 | 対応済み。1行目に「#順位 名前」、クラン未所属の行は非表示 |
| 17 | 統計がスマホで縦1列 | 対応済み。スマホ幅だけ最小幅140pxにして2列 |
| 18 | カード名が3〜4行に折り返す | 対応済み。スマホ幅でカード名の文字を小さくした |
| 19 | 2v2で味方にも「自分のデッキ」 | 対応済み。本人を先頭に並べ替え、「味方のデッキ」を追加。対戦カードでも本人を太字 |
| 20 | 2v2の対戦履歴で相手が1人 | 対応済み。相手2人と味方を表示 |
| 21 | 2v2で左右の2段目がずれる | 対応済み。左右を同じ行に配置(実測で上端が一致) |
| 22 | 長い国名でタブが折り返す | 対応済み。折り返さず横スクロール |
| 23 | 名前の色指定タグ | 対応済み(`web/DisplayNames`)。多言語化担当セッションの`domain/GameText`と役割が重なるため、そちらに寄せる予定 |
| 24 | 1戦だと得意/苦手が矛盾 | 対応済み。全カードへのフォールバックを廃止し、案内文を表示 |
| 25 | スマホでソートすると一覧が画面外 | 対応済み。ソートのリンクに`#members` |
| 26 | ソート用チップの押せる範囲 | 対応済み。チップ全体(72×36px)を押せる。言語切替・メニューも高さ35px前後に拡大 |
| アクセシビリティ | ライトモードのコントラスト、絵文字の読み上げ、フォーカスリング、タブの矢印キー、タブのスクリプトの範囲 | 対応済み。勝ちバッジ4.83:1、表見出し5.42:1 |
| その他 | 空状態が素っ気ない / 得意苦手のHTML重複 / `--color-primary-dark`の名前 | 対応済み |
| その他 | ページごとに本文の幅が変わる | **見送り**。全画面を1000pxに揃えて試したが、対戦詳細でカード名(「ゴブリンドリル」など)が折り返したため、対戦詳細だけ1100pxのままにした |
| モバイル(既存) | 表のカード化で表のセマンティクスが失われる / クランメンバー一覧が1人5行 | 未対応 |

以下は指摘時の記録。

2026-09-16にコードから洗い出した候補を、2026-09-17のレビューで実際の描画と照合し、新しい指摘を加えた。対戦詳細の2カラム化とモバイル対応は着手済み(`進捗ログ.md`のフェーズ1.8)。

**確認方法(2026-09-17)**
- 本番VMが応答しなかったため、コミットをスクラッチ領域に展開し、ローカルのポート18080で起動して確認した。トップページ以外は`4977f5d`、トップページは`94d1852`の状態。
- ChromeでPC幅(1568px)を確認した。ダークモードとモバイル幅(390px)は、CSSOMでメディアクエリの条件を書き換えて確認した。
- コントラスト比はWCAGの計算式で算出した。

**同じ種類の文言・書式の問題は、上の多言語化の候補と重なる**。対戦日時が`20260915T222949.000Z`のまま表示される件は候補5、数値に桁区切りが無い件は候補8、`※`の直書きは候補7を参照。

### 優先度高(実害がある、または目立つ)

**1. トップページが縦に長すぎる**(`94d1852`以降。2026-09-17、ユーザー判断で見送り)
- 個人ランキングを100人表示にしたため、ページの高さはPC幅で約5,600px、モバイル幅で約12,500pxになった。
- その下にあるクランランキングへのリンクは、PCで約5,300px、モバイルで約12,300pxの位置にあり、ほぼ見つけられない。
- トップページの主な機能である検索との比重が逆転している。
- 対応案: トップページは10人前後に絞り、続きは「もっと見る」で個人ランキング画面へ誘導する。100人の表示を維持するなら、クランランキングへのリンクを個人ランキングより上に移す。

**2. ダークモードで主ボタンとヘッダーの文字が読みにくい**(既存の指摘を実測で更新)

| 箇所 | 配色 | コントラスト比 |
|---|---|---|
| 主ボタン(検索・表示) | 白 on `#7b95ff` | 2.77:1 |
| 主ボタンのホバー時 | 白 on `#a8bcff` | 1.86:1 |
| ヘッダーのグラデーション | 白 on `#7b95ff → #a8bcff` | 2.77 → 1.86:1 |

- 通常文字の基準(4.5:1)も、大きい文字の基準(3:1)も下回る。
- ヘッダーの言語切替リンクは`opacity: 0.7`のため、さらに読みにくい。
- 原因: ダーク時の`--color-primary`/`--color-primary-dark`が明るい色で、それを「白文字を載せる背景」にも使っている。
- 文字色用とボタン背景用で、変数を分ける必要がある。変数名と実体のずれ(下の「その他」)も同じ原因。

**3. プレイヤー情報画面の「所属クラン」リンクにスタイルが当たっていない**
- `.stat-grid .value a`の定義が無いため、ブラウザ既定の色になる(実測: ライトでは`#0000EE`、ダークでは紫。どちらも下線付き)。サイト内の他のリンク(テーマ色・下線なし)と見た目が揃っていない。

**4. カード画像の読み込み中にレイアウトが崩れる**(既存の指摘を実描画で確認)
- 対戦詳細で、画像が未読み込みのカードは高さが縮み、8枚のグリッドの行の高さがばらつく。読み込み完了とともにガタつく。
- 原因: `loading="lazy"`で、画像に`width`/`height`属性も`aspect-ratio`も無い。

### 情報設計・導線

5. **「検索に戻る」の文言が実際の遷移と合っていない**。ランキング画面・プレイヤー情報画面・クラン情報画面の戻るリンクは、どれもトップページへ飛ぶだけ。ランキングから来た場合や、プレイヤー画面からクラン画面に来た場合は、「検索」に戻るわけではない。「トップへ戻る」にするか、パンくずにする(パンくずが無い件は既存の指摘)。
6. **ヘッダーにナビゲーションが無い**。ランキング画面へはトップページからしか行けず、プレイヤー画面などからは一度トップに戻る必要がある。ヘッダーに「検索 / 個人ランキング / クランランキング」を置く案がある。
7. **ボタンの文言の使い回し**。クランランキング画面の下のボタンが「個人ランキングを**もっと**見る」になっているが、この画面では個人ランキングを1件も見せていない。トップページ用の文言(`playerRanking.link`)を流用しているのが原因。
8. **見つからなかったときの画面が行き止まり**。存在しないタグ、またはクラン名で該当なしのとき、エラー用の赤い枠が1行出るだけで、画面の残りは空白になる。
   - 入力した値が表示されず、再検索のフォームも無い。
   - 「見つからない」は利用者の入力ミスであってシステムエラーではないので、赤い`.error-box`より、中立的な表示に再検索フォームを添える方が自然。
9. **クラン名検索結果画面で、検索語がタグと同じ見た目になる**。検索語「償い」が、クランタグと同じ`.tag-pill`で表示されるため、タグのように見える。「「償い」の検索結果」のように、文章で見せる方が分かりやすい。
10. **対戦履歴で、リンクが読みにくい日時に付いている**。対戦詳細へのリンクが、日時の列(`20260915T222949.000Z`)に付いている。日時の書式を直すまでは、行の末尾に「詳細」リンクを置くか、行全体をクリック可能にする案がある。
11. **ソートできる列見出しが、ソートできるように見えない**。クランメンバー一覧の見出しは、未ソートの状態では通常の見出しと同じ見た目(ホバーで初めて下線が出る)。未ソート列にも`⇅`などの控えめな印を付ける。
12. **数値列が左揃え**。トロフィー・寄付数・クランスコア・レーティングなど、比較する数値が左揃えのため、桁の違いが見比べにくい。右揃え+`font-variant-numeric: tabular-nums`にする(桁区切りは多言語化の候補8)。
13. **得意/苦手カードの強弱が伝わらない**(既存の指摘を実描画で確認)。見た目がまったく同じカードが2段並ぶだけで、見出しを読まないと区別できない。勝率も小さい灰色の文字だけ。枠や勝率の色を勝ち・負け色に寄せる、勝率をバーで表示する、などの案がある。
14. **クランランキングの上位が全部同じ値に見える**(公式APIのデータ由来)。2026-09-17時点で、グローバル上位10件はすべてクランスコア140000・メンバー50人で、表の数値列が何も伝えていない。表示側で工夫するなら、同点を示す表記か、別の指標(必要トロフィーなど。APIで取れるか要確認)を並べる。
15. **最終アクセスが長いメンバーが目立たない**。「26日前」も「今日」と同じ見た目。リーダーが整理対象を探す用途を考えると、一定日数以上(例: 7日以上)を薄い警告色にすると実用的。

### モバイル

16. **個人ランキングのカード表示が冗長**(個人ランキング画面で確認)。1人あたり5行(順位/プレイヤー/レベル/レーティング/クラン)で、200人表示だと非常に長い。
    - 順位は「順位」ラベル付きの1行ではなく、カードの左上に大きく出す方がランキングらしい。
    - クラン未所属の人は「クラン」ラベルだけが空の値で表示される。値が無ければ行ごと出さない。
17. **プレイヤー情報画面の統計が縦に7個並ぶ**。390px幅だと`.stat-grid`の`minmax(160px, 1fr)`が2列に収まらず、1列になる(カードの余白を引くと内側が約323pxで、2列に必要な332pxに足りない)。`minmax(140px, 1fr)`程度にすれば2列になる。
18. **対戦詳細でカード名が3〜4行に折り返す**。4列固定のため、1枚の幅が約60pxしかない。「エレクトロスピリット」「ロイヤルジャイアント」が3行になり、カードの高さが揃わない。モバイルだけカード名の文字を小さくする、または`line-clamp`で2行に制限する。
- (既存)クランメンバー一覧はモバイルで1人5行になり長い。
- (既存)表を`display: block`でカード化しているため、表のセマンティクスが失われる。

### アクセシビリティ(既存の指摘、未対応のまま)
- **ライトモードでも基準をわずかに下回る箇所がある**(2026-09-17に算出)。
  - 勝ちバッジ(`#1e8e5a` on `#e5f6ee`): 3.70:1
  - 表の見出し(`#6b7280` on `#eef1f8`): 4.28:1
  - どちらも小さい文字なので、基準は4.5:1。
- 装飾用の絵文字(stat-gridの⭐🏆🥇✅❌👑🛡️👥、ヘッダーの👑)に`aria-hidden`が無く、読み上げられる。
- フォーカスリングを自前で定義しているのは`.search-form input`だけ。2回目のレビューで、検索ボタンなどにはブラウザ既定のリング(1px前後の黒っぽい線)が出ることを確認した。ただし、入力欄(テーマ色2px)と見た目が揃っていない。青いヘッダーの上では、黒いリングはほぼ見えないはず(ヘッダー上は未実測)。ボタン・リンク・タブ・ソート用ヘッダも`:focus-visible`で共通化する。
- ランキングのタブが矢印キーで移動できない(ARIAのタブパターンでは、左右キーでの移動と`tabindex`の切り替えが推奨)。
- タブ切り替えのスクリプトが`document`全体の`.tabs [role="tab"]`を対象にしており、IDも`tab-global`などの固定値。1画面に`rankingTabs`を2つ置くと互いに干渉する(今はどの画面も1つだけなので実害なし)。

### その他(既存)
- 空状態(`.empty-state`)が文字色を薄くするだけで素っ気ない。
- `player.html`の得意/苦手カードが、`deckGrid`フラグメントを使わず同じHTMLを手書きしている。`card-level`クラスに勝率を入れている(クラス名と中身の不一致)。
- ダークモードの`--color-primary-dark`が、実際には明るい色(上の優先度高の2と同じ原因)。
- ページごとに本文の幅が変わる(対戦詳細だけ1100px、他は860px)。画面を移動すると「戻る」リンクや見出しの左端が横にずれる。

### 追加分(2026-09-17、2回目のレビュー)

1回目と同じコード(`94d1852`)を、1回目で見ていない観点で確認した。確認した観点は、2v2の対戦、英語表示、タブレット幅(700px)、モバイル幅(360px)、キーボード操作、存在しないURL。確認方法は1回目と同じ(ローカルのポート18080)。

**19. 2v2の対戦詳細で、味方のデッキにも「自分のデッキ」と付く**(優先度高: 誤解を招く)
- 実例: `/player/8V0Q02L9Y/battles/20260916T222733.000Z`。見ているのはダイナマイト四国の画面。
- 見出しは「自分のデッキ(langonileandro)」「自分のデッキ(ダイナマイト四国)」の2つになり、味方も「自分」と表示される。
- さらに、公式APIの`team`の順序は、見ているプレイヤーが先頭とは限らない。この例では**味方の方が先頭**に来るため、他人のデッキが一番上に「自分のデッキ」として出る。上部の対戦カードでも、見ているプレイヤーは2番目に並ぶ。
- 対応案: 見ているプレイヤーのタグで`team`を並べ替えて先頭にする。見出しは「自分のデッキ」「味方のデッキ」「相手のデッキ」に分ける。対戦カードでも本人を強調する。
- 補足: 勝敗やクラウン数はチーム単位なので、並べ替えても集計(`PlayerBattleStats`)には影響しない。

**20. 2v2の対戦履歴で、対戦相手が1人しか出ない**
- プレイヤー情報画面の対戦履歴は、「対戦相手」列に`opponent[0]`だけを出している。2v2では相手のもう1人と味方が画面から分からない。
- 実例: 8V0Q02L9Yの対戦履歴は、画面で確認した先頭4件がすべて2v2(`TeamVsTeam`)だった。2v2を主に遊ぶプレイヤーでは、一覧の大半で情報が欠ける。
- 対応案: 2v2では相手を2人とも出す(「A / B」)。味方も「(味方: C)」のように補足する。モード名の`TeamVsTeam`が訳されていない件は、多言語化の候補1。

**21. 2v2の対戦詳細で、左右のデッキの2段目の位置が揃わない**
- 左右の列は独立して縦に積んでいる(`.decks-side`)。そのため、カード名が3行に折り返す方(例: 「アイススピリット」)だけ1段目が高くなる。
- 実測: 2段目の上端が左921px・右937pxと、16pxずれる。
- 「自分・味方」と「相手」を行単位で見比べにくい。行ごとにgridで揃えるか、カード名の行数を固定する(モバイルの18と同じ対処)。

**22. モバイルで、国名が長いと「グローバル」タブが2行に折り返す**
- 実測: 360px幅で国別タブが「中華人民共和国香港特別行政区」のとき、「グロー/バル」と2行になった。`.tabs`はflexで、タブに`white-space: nowrap`も`flex-shrink: 0`も無いため。
- 国名自体が長い件は、多言語化の追加分(`getDisplayCountry`の件)。仮に国名を短くしても、英語の長い国名などで再発しうる。タブ側で、折り返さない+横スクロールを許す指定を入れておくのが確実。

**23. 名前に含まれるゲーム内の色指定タグが、対戦詳細にもそのまま出る**
- 実例: 対戦カードとデッキ見出しに「<c6>Ale :D」、対戦履歴の相手名にも同じ表記が出た。
- 多言語化の追加分では、個人ランキングの`<c2>`として記録済み。表示箇所は、ランキングだけでなく**プレイヤー名を出す全画面**(対戦履歴・対戦詳細・クランメンバー一覧・ランキング)に及ぶ。そのため、ViewMapperで一括して取り除くのがよい。

**優先度高の8の補足**: 存在しないURL(`/nonexistent-page`)でも、同じ赤い1行+空白の画面になることを確認した。文言は「検索画面からやり直してください」だが、フォームは無く、リンクは画面上部の小さな「検索に戻る」だけ。

**問題が見つからなかった観点**
- タブレット幅(700px): デッキは縦積みに切り替わり、はみ出しも無い。
- 英語表示: レイアウト崩れは無し。文言の問題(`1 crowns`、`Your deck(名前)`の空白抜けなど)は、多言語化の候補6・7に記録済み。
- キーボード操作: 入力欄→検索ボタンの順にフォーカスが移る。

### 追加分(2026-09-17、3回目のレビュー)

画面に関わるコードは2回目と同じ(`94d1852`以降、テンプレート・CSSに変更なし)。3回目は次の観点で確認した。
- 対戦数が少ない・対戦履歴が無いプレイヤー: クラン「償い」のメンバー49人を順に確認した
- ダークモードのランキング・クラン検索・エラー画面
- モバイルでのソート操作とタップ領域
- 応答時間

**24. 対戦が1戦しかないと、得意カードと苦手カードの両方に「100% (1/1)」が並ぶ**(優先度高: 表示が矛盾する)
- 実例: `/player/8GYQVLPU`(対戦履歴1件、1勝0敗)。得意カード3枚・苦手カード3枚が、すべて「100% (1/1)」。**勝率100%のカードが「苦手」として出る**。
- 原因: 使用回数5回以上のカードが1枚も無いと、`PlayerBattleStats`は全カードを対象に順位付けする(フォールバック)。対戦1件では、相手デッキ8枚がすべて同じ勝敗になる。そのため、得意と苦手を分ける根拠が無いまま3枚ずつ並べてしまう。
- 同じメンバー49人のうち、対戦履歴が1〜9件のプレイヤーが6人いた。同じ状態になりうるのは珍しくない。
- 対応案:
  - 対戦数が一定未満(例: 5戦未満)なら、カード分析を出さずに「対戦数が少ないため分析できません」と表示する。
  - あるいは、得意は勝率が50%を超えるカード、苦手は50%未満のカードに限る。
  - 画面の注記(「たまたまの全勝・全敗で上位に来ないよう…」)とも矛盾しているので、どちらかに揃える。
- 補足: 「5回」はユーザー指定の仕様値(`進捗ログ.md`参照)。変えるのは閾値ではなく、フォールバック時の見せ方。対応方針はユーザー判断。

**25. スマホでクランメンバー一覧をソートすると、毎回ページ先頭に戻り、一覧が画面外に出る**
- ソートはリンクによるページ遷移なので、表示し直すと先頭に戻る。
- 実測: 375px幅で、表の上端は822px。クランの説明文・統計・注記2つの下にあるためで、スマホの画面の高さでは一覧が見えない。ソートのたびにスクロールし直すことになる。
- 対応案: ソート用リンクに`#members`のようなアンカーを付けて、一覧の位置に戻す。トップページの国選択では`/#player-ranking`で同じ対処をしている。

**26. スマホのソート用チップは、押せる範囲が見た目より小さい**
- 実測: チップの見た目は48×28pxだが、リンクは中の文字部分の26×17pxだけ(余白は`th`側に付いている)。チップの端を押しても反応しない。
- 言語切替リンクも高さ25pxで、推奨サイズ(44px前後)より小さい。
- 対応案: 余白を`a`側に移して`display: block`にし、チップ全体を押せるようにする。

**問題が見つからなかった観点**
- ダークモード: ランキングのタブ・国の選択欄、クラン名検索結果、エラー枠は読める。主ボタンとヘッダーのコントラスト不足は、優先度高の2で記録済み。
- 対戦履歴が0件のプレイヤー: 戦績サマリーごと非表示になり、対戦履歴欄に「対戦履歴がありません」と出る。破綻は無い(見た目が素っ気ない件は「その他」に記録済み)。
- 応答時間: 送信中の表示が無いことは問題にならなかった。キャッシュが無い状態でもローカルで0.3〜0.5秒、本番でプレイヤー情報0.56秒・トップページ0.62秒だった。ただしトップページのHTMLは約98KB(個人ランキング100行×グローバル・国別の2つ分)で、優先度高の1(トップページの長さ)と同じ原因。

## 多言語化(i18n)の改善候補(2026-09-17に洗い出し、2026-09-18に全21件を対応)

### 対応結果(2026-09-18)

1〜7・9・11・13〜15・21は、別セッションが作業途中(ビルド不可、`wip/i18n`に退避)で止めていたものを
引き継いで完了させた。残っていた8・10・12・16〜20と「残っている判断事項」も同日に対応し、この節は全件完了した。
経緯は`進捗ログ.md`のフェーズ1.12・1.13を参照。

| 番号 | 内容 | 状態 |
|---|---|---|
| 1 | ゲームモード名の訳が実データの65%で欠けている | 対応済み。`gameMode.name`の辞書 → `type`の辞書 → 「その他」の二段構え。英語側の訳も追加。訳語は公式情報で裏取りし、`Ladder`は「トロフィー目標」、旧パス・オブ・レジェンドは「ランク戦」にした |
| 2 | カード名の訳の抜け6枚と綴り違い | 対応済み。5枚を追加し`card.Vine`→`card.Vines`を修正。辞書に無いキーは`LabelResolver`がWARNログを出す |
| 3 | 不正な`?lang=`でHTTP 500 | 対応済み。`setIgnoreInvalidLocale(true)` |
| 4 | 対応していない言語の扱い | 対応済み。`SupportedLanguages`でAccept-Languageのq値順に対応言語を選ぶ |
| 5 | 対戦日時がUTCの生値 | 対応済み。`<time datetime>`+ブラウザの`Intl.DateTimeFormat`で閲覧者の時刻に変換 |
| 6 | 英語の単数・複数(`1 crowns`) | 対応済み。ICU MessageFormatを`MessageSource`に組み込み、テンプレートの単複分岐を廃止 |
| 7 | 日本語の記号(`※`)の直書き | 対応済み。`notice`フラグメント+`notice.format`に集約 |
| 8 | 数値の桁区切り | 対応済み。トロフィー・クランスコア・レーティング・寄付数を`#numbers.formatInteger`でロケール別の区切りに揃えた |
| 9 | 言語選択のCookieが消える | 対応済み。365日(国選択と同じ) |
| 10 | エラー画面の言語切替リンクが`/error?lang=`になる | 対応済み。`RequestDispatcher.ERROR_REQUEST_URI`で転送前のURLを使う |
| 11 | 日英のキー一致がテストで担保されていない | 対応済み(`MessagesConsistencyTest`) |
| 12 | `Vary`ヘッダと`hreflang` | 対応済み。`Vary`は先行対応済みで、今回`<link rel="alternate" hreflang>`(ja/en/x-default)を追加。絶対URLが要るため`siteBaseUrl`を全画面に渡す |
| 13 | 日本語の国名の並び順 | 対応済み。漢字で始まる国に読み仮名(`country.reading.*`)を持たせ、日本が158番目(以前はほぼ末尾)になった |
| 14 | 国名が正式名称で長すぎる | 対応済み。ICUの短い表記(例: 香港)を使う |
| 15 | 国ではない地域名が翻訳されない | 対応済み。`CountryNames`でCLDRの地域コードに対応付ける |
| 16 | 右から左に書く言語の名前(`<bdi>`) | 対応済み。表やリンクの中の名前は`<bdi>`、`<title>`やデッキ見出しのように要素を置けない箇所はメッセージ側にUnicodeの分離文字(FSI/PDI)を持たせた |
| 17 | 言語切替リンクに`lang`/`hreflang`が無い | 対応済み |
| 18 | メッセージの件数のハードコード | 対応済み。`playerRanking.lead`/`playerRanking.notice`/`ranking.notice`の件数を`{0}`にし、Controllerの定数から渡す(`RankingService.CLAN_RANKING_SIZE`を公開) |
| 19 | `ranking.notice`の説明が古い | 対応済み。「クランのみ掲載」を削除。あわせて注記の「パス・オブ・レジェンド」を現行名の「ランク戦」に統一した |
| 20 | 国を推定できないときの既定が日本 | 対応済み。ユーザー判断で「表示言語の代表的な国を既定にする」を採用(ja→JP、en→US)。それも国一覧に無ければ従来どおり日本 |
| 21 | 名前のゲーム内装飾タグ | 対応済み(`domain/GameText`。旧`web/DisplayNames`を統合) |

**判断事項だった件(2026-09-18に決着)**: `type`が`trail`の中にラダー・2v2・チャレンジが混在するため、
`Challenge_AllCards_EventDeck_NoSet`(実データで50/270戦)が「通常バトル」と大まかに表示されていた。
ユーザー判断で前方一致を採用し、`gamemodeprefix.*`の辞書(最初の`_`までを引く)を名前の完全一致と`type`の間に挟んだ。
`Challenge_*`は「チャレンジ」と表示される。

**実測(2026-09-18、ローカルのポート18080)**: 桁区切り(`140,000`・`12,265`)、hreflang3件、
エラー画面の言語リンクが`/nonexistent?x=1&lang=ja`、Accept-Language無しで`?lang=en`のとき国別タブが「United States」、
デッキ見出しが`自分のデッキ(名前)`、クラン名検索の見出しで末尾の絵文字が閉じ引用符の外に回り込まないこと(Chromeの実描画)を確認した。

以下は洗い出し時の記録。


ユーザーの依頼で洗い出した候補。コードの読み取りに加えて、次の方法で裏付けを取った(数値はこの日時点のもの)。

- 公式APIの実データ: `/cards`、日本上位クランのメンバーなど9人分の対戦履歴(計270戦)
- ローカル起動での実測: コミット`ce63eaf`の状態をポート18080で起動し、curlで確認

### 実害が大きいもの(実データ・実測で確認済み)

**1. ゲームモード名の訳が実データの65%で欠けており、内部IDがそのまま表示される**
- 270戦の内訳:

  | gameMode.name | type | 件数 | 訳 |
  |---|---|---|---|
  | `Ladder` | `trail` | 95 | あり |
  | `Ranked1v1_NewArena2` | `pathOfLegend` | 79 | なし |
  | `Challenge_AllCards_EventDeck_NoSet` | `trail` | 50 | なし |
  | `TeamVsTeam` | `trail` | 30 | なし |
  | `PickMode` | `trail` | 16 | なし |

- `gameMode.name`はイベントやアリーナごとに接尾辞付きのIDが増えるため(`Ranked1v1_NewArena2`など)、完全一致の辞書では追いつかない。`type`の方が分類として安定しているので、「`type`で大分類を表示し、既知のIDだけ詳しい名前にする」二段構えを検討する。
- 英語側(`messages.properties`)にはゲームモードの定義が1件も無い。そのため英語UIでは、訳を用意したモードでも`PathOfLegend`のような内部IDが出る。
- 日本語ラベルに「ランク戦 (Ladder)」のように内部IDを併記しているのは、旧`GameModeLabels`時代の「日本語 (英語名)」形式の名残。
- `Ladder`が「ランク戦」、`Ranked1v1`が「ランク戦(1vs1)」と、別モードがほぼ同じ名前になっている。実データでは`Ladder`は`type=trail`(トロフィーロード側)。訳語は公式情報で裏取りしてから直す。

**2. カード名の訳の抜け6枚と、キーの綴り違い1件**
- 公式API `/cards`(通常123枚+タワーユニット4枚)と`messages_ja.properties`を突き合わせた。
- 訳が無いカードと、270戦中の出現回数:

  | カード | 出現回数 |
  |---|---|
  | `Spirit Empress` | 23 |
  | `Vines` | 21 |
  | `Ronin` | 16 |
  | `Boss Bandit` | 9 |
  | `Goblin Curse` | 8 |
  | `Goblin Demolisher` | 2 |

  日本語UIでも、これらは英語名のまま表示される。
- `Vines`は、辞書側のキーが`card.Vine`(末尾のsが無い)になっている。訳を用意してあるのに効いていない。
- 新カードの追加に気付く仕組みが無い。`LabelResolver`が辞書に無いキーに当たったらWARNログを出す、などで検知できるようにしたい。

**3. 不正な`?lang=`でHTTP 500になる**
- 実測: `?lang=a.b`で500。
- 原因: `LocaleChangeInterceptor`の`ignoreInvalidLocale`は既定で`false`。そのため、`StringUtils.parseLocale`が投げる`IllegalArgumentException`が、そのまま500になる(spring-webmvc 7.0.9のバイトコードで確認)。
- さらに、エラー画面を表示するための転送先(`/error`)でもインターセプタが同じ例外を投げる。そのためアプリのエラー画面は出ず、**Tomcat既定の素のエラーページ**(`HTTP Status 500 – Internal Server Error`、英語固定・サイトのデザイン無し)が返る(2026-09-17に実測、ログにも例外が2回出る)。
- URLを書き換えるだけで500を出せるので、ログや監視のノイズになる。`WebConfig`で`setIgnoreInvalidLocale(true)`にする。

**4. 対応していない言語の扱い**

| リクエスト | `<html lang>` | 本文 | 問題 |
|---|---|---|---|
| `Accept-Language: fr` | `fr` | 英語 | 属性と本文の言語が食い違う |
| `Accept-Language: fr,ja;q=0.9` | `fr` | 英語 | 第2候補の日本語が読めるのに英語になる |
| `Accept-Language: zh-TW,ja;q=0.8` | `zh` | 英語 | 同上 |
| `?lang=fr` | `fr` | 英語 | 対応していない`fr`をCookieに保存してしまう |

- 原因: `request.getLocale()`は最優先の1言語しか見ない。
- 対応案: 対応言語(ja/en)のリストを持ち、Accept-Languageのq値の順に、最初に一致した対応言語を選ぶ(`Locale.lookup`、または`AcceptHeaderLocaleResolver#setSupportedLocales`と同じ考え方)。`?lang=`も対応言語以外は無視する。これで`<html lang>`も実際の表示言語と一致する。
- 国別ランキングの`CountryPreference`も、Accept-Languageを自前でq値の順に解釈している。言語の判定も同じ考え方に揃えられる。

### 表示の自然さ

**5. 対戦日時が`20260917T030143.000Z`のまま、UTCで表示される**
- 対象: `player.html`の対戦履歴一覧、`battle-detail.html`の見出しの下。読みにくいうえに、利用者のタイムゾーンと合っていない。
- ロケールに応じた書式で表示する(例: 日本語`2026/09/17 12:01`、英語`Sep 17, 2026, 3:01 AM`)。
- タイムゾーンはAccept-Languageからは決められない。「言語がjaならAsia/Tokyo」と割り切るか、ブラウザ側のJS(`Intl.DateTimeFormat`)で変換するかを決める必要がある。URLのキーとしての`battleTime`は今のまま使う。
- 関連1: 最終アクセスの「今日/1日前」は、UTC基準の24時間単位の経過なので、利用者の暦日とずれることがある。
- 関連2: 寄付数リセットの注記「毎週日曜0時(GMT)」は、日本語なら「日曜9時(日本時間)」の方が親切。

**6. 英語の単数・複数(`1 crowns`など)**
- 単数でも複数形のまま出るメッセージ:
  - `battle.crowns={0} crowns`(対戦詳細で「1 crowns」になる)
  - `stats.draws=({0} draws)`
  - `stats.summary=... in the last {0} battles`
  - `clanSearch.members={0} members`
- `clan.lastSeen.day`/`days`はテンプレートの`th:switch`で分岐している。英語の「1とそれ以外」という単複ルールを前提にしたロジックがテンプレートにある状態で、複数形が3種類以上ある言語(ロシア語など)を足すと破綻する。
- ICU4Jの`MessageFormat`(`{0, plural, one{# crown} other{# crowns}}`)を`MessageSource`に組み込めば、単複を言語ごとのメッセージ側で表現でき、テンプレートの分岐も消せる。依存ライブラリが1つ増える。Java標準の`ChoiceFormat`は、`#`の衝突で過去にハマっている(`進捗ログ.md`参照)。

**7. 日本語の記号がテンプレートに直書きされている**
- 注記の先頭の`※`(`player.html`に3箇所、`clan.html`に2箇所、`ranking.html`に1箇所)が、英語UIにも出る。
- `battle-detail.html`のデッキ見出しの全角括弧が、英語UIでも使われる(`Your deck(Name)`)。
- 記号や括弧もメッセージ側に含める(例: `battle.deck.team=自分のデッキ({0})` / `Your deck ({0})`)。

**8. 数値の桁区切り**
- トロフィーやクランスコアは、テンプレートで数値をそのまま出しており、`85000`のように区切りが無い。
- 一方、メッセージの`{0}`は`MessageFormat`が区切りを入れるため、画面内で表記が混ざりうる。
- `#numbers.formatInteger(x, 1, 'DEFAULT')`などで、ロケールに応じた区切りに揃える。

### 運用・構造

**9. 言語選択のCookieが、ブラウザを閉じると消える**
- 実測: `Set-Cookie: lang=en; Path=/; SameSite=Lax`で、Max-Ageが無い(`CookieLocaleResolver`の既定)。
- `setCookieMaxAge`で有効期限を延ばす。国選択のCookieは365日にしているので、それに揃える。

**10. エラー画面の言語切替リンクの行き先が`/error?lang=...`になる**
- 実測: 存在しないURLなど、サーブレットコンテナのエラー処理を経由した場合に起きる。`GlobalModelAttributes`が`request.getRequestURI()`を使っており、エラー処理の中では`/error`になるため。
- 元のURLが入っているリクエスト属性`RequestDispatcher.ERROR_REQUEST_URI`を使う。

**11. 日英のメッセージキーの一致がテストで担保されていない**
- 2026-09-17の確認では、`card.*`/`gamemode.*`以外のキーは日英で一致していた。ただ、キーを追加したときの片方の漏れは検知できない。
- 2つの`.properties`を読んでキーの集合を比べるテストは、純粋ロジックのテストの範囲で書ける。

**12. 検索エンジンやキャッシュへの言語の伝え方**(上の「ページビュー向上に関するTODO」と関連)
- 同じURLでも、CookieやAccept-Languageによって返す言語が変わる。それなのに`Vary`ヘッダが無い(`Content-Language`は付いている)。将来CDNやリバースプロキシを挟むと、別の言語のページがキャッシュから返る恐れがある。
- 検索エンジンのクローラーは基本的にAccept-Languageを送らないため、英語ページはインデックスされにくい。`?lang=en`付きのURLを`<link rel="alternate" hreflang>`で示すと、英語圏からの流入を狙える。

### 追加分(2026-09-17、2回目のレビュー)

1回目のレビュー(コミット`ce63eaf`時点)以降に、クランランキングの国別対応と個人ランキング画面が追加された。そこでコミット`94d1852`の時点で改めてレビューした。

- 上の1〜12は、すべて未対応のまま残っている(下の「実測結果」で再確認済み)。
- 8(数値の桁区切り)は、新しく追加された個人ランキングのレーティング(`eloRating`)とクランランキングのスコアにも当てはまる。
- 以下は新しく見つかった指摘。公式API(`/locations`、グローバルの個人ランキング上位200人、グローバルのクランランキング上位10件)と、JDK 21のロケールデータで裏付けを取った。

**13. 国名の日本語の並び順が読み順になっていない**(実害あり)
- `ViewMapper#toCountryOptions`は`Collator.getInstance(locale)`で並べている。JDKの日本語Collatorは漢字の読みを知らないため、カタカナの国名が五十音順に並んだ後ろに、漢字の国名がUnicode順でまとめて並ぶ。
- 実測: 254か国のうち、末尾が「…/ 韓国 / 合衆国領有小離島 / 西サハラ / 赤道ギニア / 台湾 / 中央アフリカ共和国 / … / 中国 / 東ティモール / 南アフリカ / 南スーダン / 南極 / **日本** / 仏領ギアナ / … / 北朝鮮」。主な利用者の国である日本が、プルダウンのほぼ最後にある。
- 対応案:
  - 日本などよく使う国を先頭に固定する(簡単)
  - 読み仮名を持つ
  - ICU4JのCollatorを使う。ICU4Jでも漢字の読み順にはならないため、効果は要検証。6のICU4J導入と合わせて検討する
- あわせて、並べ替えに使うロケールは`displayLocale(locale)`に揃える。今は国名を英語で表示していても、Accept-Languageが`fr`ならフランス語の照合順で並ぶ。

**14. 一部の国名がCLDRの正式名称で長すぎる、または呼び方に配慮が要る**
- `Locale#getDisplayCountry`は、CLDRの短い表記(short/variant)を使わない。そのため次の名前が、国選択のプルダウンと**国別タブの見出し**にそのまま出る:

  | コード | 日本語 | 英語 |
  |---|---|---|
  | `HK` | 中華人民共和国香港特別行政区 | Hong Kong SAR China |
  | `MO` | 中華人民共和国マカオ特別行政区 | Macao SAR China |
  | `PS` | パレスチナ自治区 | Palestinian Territories |
  | `MM` | ミャンマー (ビルマ) | Myanmar (Burma) |

- タブの見出しとしては長すぎるうえ、香港などは呼び方に政治的な配慮が要る。少数の例外だけ、`messages*.properties`に`country.HK=香港`のような上書きを持つ(辞書にあればそちらを使い、無ければCLDRを使う)。
- 参考: 公式APIの英語名は`CG`=「Congo (DRC)」、`CD`=「Congo (Republic)」と取り違えている(本来は逆)。CLDRの訳を優先する今の実装では表に出ないが、英語名にフォールバックするときに誤った名前になる。

**15. 国ではない地域名が翻訳されない**
- `ViewMapper#locationName`は、`countryCode`が無い場所だと公式APIの英語名をそのまま返す。
- 公式APIの`/locations`には、国以外に`Europe`/`North America`/`South America`/`Asia`/`Oceania`/`Africa`/`International`/`Unknown`がある。クランの所在地がこれらだと、日本語UIのクランランキングの「国・地域」列に英語が出る。
- 確認した時点のグローバル上位10クランはすべて国だったため、現時点で実際には出ていない。この8件だけなら、`location.<id>`のキーで辞書に持てる。

**16. 右から左に書く言語(アラビア語など)の名前で、周りの表示が崩れる**
- 実データ: グローバル個人ランキング上位200人のうち7件が、アラビア文字のプレイヤー名またはクラン名だった(例: `مقهى الحظ ☕️`)。
- 表のセルのように名前だけが入る場所は問題ない。問題は、名前を他の文字列と同じ行に並べている場所:
  - `battle-detail.html`のデッキ見出し(`自分のデッキ(名前)`)
  - `<title>`(`名前 | アプリ名`)
  - `clan-search.html`の「クラン名+タグ」
- Chromeでの実測:
  - アラビア文字だけの名前(`ابـۦـو شـجــاع`)では、括弧の位置は崩れなかった。
  - **名前の末尾に絵文字や数字がある場合に崩れる**。デッキ見出しに実データの名前`مقهى الحظ ☕️`・`سحر العيون♥`・`لغة العيون❤️`を差し込んだところ、名前の一部である末尾の絵文字が名前の外側(閉じ括弧の直前、右端)に回り込んだ。`<bdi>`で囲むと、名前の中の正しい位置(左端)に表示された。`علي 2`でも、数字の位置が`<bdi>`の有無で変わった。
- 利用者が付けた名前を出す箇所は`<bdi>`で囲むか、`dir="auto"`を付けて、周りの文字列から隔離する。

**17. 言語切替リンクに`lang`/`hreflang`が無い**
- `fragments/layout.html`の「日本語」「English」リンクには、ページの言語と違う言語の文字列が入る。`lang`属性が無いため、スクリーンリーダーが英語UIで「日本語」を英語の読み方で読もうとする。
- `<a lang="ja" hreflang="ja">`のように指定する。12の`<link rel="alternate" hreflang>`と一緒に対応するとよい。

**18. メッセージの件数がハードコードされていて、コード側の定数とずれうる**
- 件数を文言に直接書いているメッセージ:
  - `playerRanking.lead`の「上位100人」
  - `playerRanking.notice`の「上位200人」
  - `ranking.notice`の「上位10件」
- 実際の件数は`HomeController`/`RankingController`/`RankingService`の定数で決まる。定数を変えると、日本語と英語の2ファイル×各メッセージを直す必要があり、片方だけ直し忘れやすい。
- `{0}`の引数にして、定数から渡す。6の単数・複数対応とも関係する。

**19. `ranking.notice`の説明が、個人ランキングの追加後に古くなっている**
- 日本語「プレイヤーのトロフィーランキングは公式API側が現在データを返さないため、クランのみ掲載しています」、英語も同じ趣旨。
- 今はパス・オブ・レジェンドの個人ランキング画面があるので、「クランのみ掲載」は事実と合わない。文言の直し漏れで、日英両方に同じ誤りがある。個人ランキング側の`playerRanking.notice`に同じ説明があるので、クランランキング側からは削除してよい。

**20. 国を推定できないときの既定が、言語に関係なく日本**
- `CountryPreference`は、Accept-Languageに地域が付いていないと既定のJPになる(`en`だけ、`fr`だけ、など)。英語UIの訪問者にも、国別タブに「Japan」が出る。
- 主な利用者が日本という判断は妥当。英語UIのときだけ、国別タブを出さずにグローバルだけにする、という選択肢もある(ユーザー判断)。

**21. 名前に含まれるゲーム内の装飾タグが、そのまま文字として出る**(実測で見つけた、多言語化とは別の問題)
- 実データ: グローバル個人ランキングに`XD <c2>ZEYAD`という名前があった。`<c2>`はゲーム内で文字色を変えるタグとみられる。
- `th:text`がエスケープするのでXSSにはならない。ただ、画面には`<c2>`が文字として出る。表示用には、`<c1>`〜`</c>`のようなタグを取り除くのがよい(タグの仕様は要調査)。

### 実測結果(2026-09-17、コミット`94d1852`)

ローカルでポート18080に起動し、curlとChromeで確認した。対戦履歴は、グローバル個人ランキング上位12人の対戦詳細(各3戦)と、`#UJ2JR02V0`の対戦履歴30戦を使った。

| # | 結果 | 実測内容 |
|---|---|---|
| 1 | 再現 | 日本語UIで30戦中26戦が`Ranked1v1_NewArena2`、ほかに`TeamVsTeam`・`Showdown_Friendly`が内部IDのまま。英語UIは既知の`Friendly`も内部IDのまま |
| 2 | 再現 | 対戦詳細36件で`Ronin`×4、`Vines`×2、`Spirit Empress`×1が日本語UIでも英語のまま。辞書のキーは今も`card.Vine` |
| 3 | 再現 | `?lang=a.b`でHTTP 500、Tomcat既定のエラーページ。`?lang=fr`・`?lang=xx_YY`は200だが、`Set-Cookie: lang=fr`を保存する |
| 4 | 再現 | `fr`→`<html lang="fr">`で英語表示、`fr,ja;q=0.9`→`fr`で英語、`zh-TW,ja;q=0.8`→`zh`で英語 |
| 5 | 再現 | 対戦履歴・対戦詳細とも`20260917T083125.000Z` |
| 6 | 再現 | 対戦詳細で`1 crowns`。クランの最終アクセスは`1 day ago`/`3 days ago`(テンプレート側の分岐で対応) |
| 7 | 再現 | 英語UIでも注記の先頭が`※`(プレイヤー3件、クラン2件、ランキング各1件)。英語UIのデッキ見出しが`Your deck(名前)`(全角括弧) |
| 8 | 再現 | トロフィー`14000`、クランスコア`140000`、レーティング`2640`など、すべて桁区切り無し |
| 9 | 再現 | `Set-Cookie: lang=en; Path=/; SameSite=Lax`(Max-Age無し) |
| 10 | 再現 | `/nonexistent?x=1`の404画面で、言語切替リンクが`/error?x=1&lang=en` |
| 11 | 未実施 | テストの有無の問題なので実測の対象外(`card.*`/`gamemode.*`以外のキーは、今回も日英で一致) |
| 12 | 再現 | `Content-Language: ja`のみで`Vary`無し。`hreflang`は0件 |
| 13 | 再現 | 日本語UIのプルダウン254件中、「日本」は246番目 |
| 14 | 再現 | `?country=HK`で、タブ見出しとプルダウンの両方が「中華人民共和国香港特別行政区」。PCの幅でもタブの中で最も長い |
| 15 | 再現せず | 日本語UIのクランランキング「国・地域」列は、10件すべて国名(日本語)だった |
| 16 | 条件付きで再現 | 上の16を参照(末尾に絵文字や数字がある名前で崩れる) |
| 17 | 再現 | `<a href="/?lang=ja">日本語`に`lang`/`hreflang`が無い |
| 18 | 現時点ではずれていない | トップページ100行、個人ランキング画面200行で文言と一致(香港は該当者が105人のため105行)。定数を変えたときのリスクとして残す |
| 19 | 再現 | クランランキング画面の注記に「クランのみ掲載しています」が残っている |
| 20 | 再現 | `Accept-Language: en`・`fr`で国別タブが「Japan」。`en-US,en;q=0.9`は「United States」 |

## jarのサイズ(2026-09-18にユーザーから起票依頼、未着手)

多言語化でICU4Jを入れたため、jarが24.8MB→40.0MBに増えた(`進捗ログ.md`のフェーズ1.12)。ICU4J本体は約15MBで、大半は全言語分のロケールデータ。

- **影響**: デプロイのたびにscpで40MBを転送する時間。起動時間とメモリ(メモリ1GBのVMのフリーズとの関係は未確認)。利用者のブラウザが受け取るデータには影響しない。
- **ICU4Jを使っている箇所**:
  - `IcuMessageSource`: 英語の単数・複数(`{0, plural, one{# crown} other{# crowns}}`)。Java標準の`ChoiceFormat`でも書けるが、`#`の衝突で過去にハマっている
  - `CountryNames`の`Collator`: 国名の五十音順。Java標準の`Collator`でもおおむね代替できる見込み(要検証)
  - `CountryNames`の`LocaleDisplayNames`: 国名の短い表記(例: 香港)。Java標準には短い表記の指定が無いため、置き換えるなら該当する国だけ自前の辞書で補う
  - `CountryPreference`: `ULocale`(Accept-Languageからの国の推定)
- **対応案(未決定)**:
  1. 現状維持(40MBを許容する)
  2. ICU4Jを外してJava標準に置き換える。上記3箇所の代替と、既存テスト(`IcuMessageSourceTest`など)の書き換えが必要
- VMの乗り換え(下記)で転送・メモリの問題が薄れるなら、優先度は下がる。

## VMの乗り換え(2026-09-18にユーザーから依頼、未着手)

現行VM(`132.226.7.203`、VM.Standard.E2.1.Micro)は低スペック(1/8 OCPU、メモリ1GB)で、デプロイ・再起動の前後にSSH/HTTPとも無応答になることがある(2026-09-15に2回発生)。もっとリソースの充実したVMへ乗り換えたい。**条件: 無料(Always Free)の範囲内に収めること。**

- **第一候補**: Ampere A1.Flex(ARM)。Always Free枠で合計4 OCPU・メモリ24GBまで使える。2026-09-13の構築時は"Out of host capacity"で作成できなかったため、時間を置いて再挑戦する。
  - アプリはjarなのでCPUアーキテクチャに依存しない。VM側にaarch64版のJava 21を入れればよい。
  - 空き容量が出るまで再試行する方法や、アカウントを従量課金(Pay As You Go)に切り替えると確保しやすいという情報もある。ただし従量課金への切り替えは課金が発生し得る操作なので、実施する場合は事前確認する。
- **乗り換え時の注意点**
  - パブリックIPが変わるため、Clash Royale APIキーの作り直しが必要(Allowed IP Addressesは作成後に編集できない)。予約済みパブリックIPを使えば、次回以降の乗り換えでIPを引き継げるか要確認。
  - 新VMでfirewalldとOCIセキュリティリストの両方で8080番を開放する。systemdのサービス定義と`/etc/clash-royale-api/env`(root:root 600)も移す。
  - 新VMでの動作確認が済んでから旧VMを削除する(OCIインフラの作成・削除なので、どちらも事前確認する)。Always Freeの上限を超えないよう、A1とE2.1.Microの同時稼働が枠内に収まるかも確認する。
  - 乗り換え後は`CLAUDE.md`のインフラ構成・デプロイ手順、メモリーのVMスペックに関する記述を更新する。

## プレイヤー名検索(2026-09-18にユーザーから依頼、検討中・未着手)

まだ先の話になる見込み。仕様案と未決定事項は`プレイヤー名検索（検討中）.md`を参照。

## サービスの方向性(2026-09-18にユーザーが決定)

**日本のクラロワユーザーに特化したサービスにする。** RoyaleAPI(royaleapi.com)と同じ土俵(デッキ統計・カード統計など)では戦わない。

### 大手と正面から戦わない理由

- RoyaleAPIのデッキ統計の価値は、膨大な対戦を常時収集・集計していることにある。無料枠のVM1台では、データ量でも基盤でも追いつけない。
- RoyaleAPIは2017年頃から運営されていて知名度が高く、プロ選手や大会にも使われている。機能の幅も広い(カード統計、プレイヤーの履歴、クラン戦の分析、大会情報、デッキ作成、Discordボットなど)。

### 勝ち筋(狭い場所で一番になる)

1. **日本語ユーザーにとっての使いやすさ**: 自然な日本語、スマホでの見やすさ、日本の利用者がよく使う動線に特化する。
2. **自分のための分析**: 得意・苦手カードや使用中のデッキのように、「自分はどうすれば勝てるか」に絞った分析。全体の統計とは切り口が違い、今ある機能の延長線上にある。
3. **遊べるコンテンツ**: フェーズ2のクイズ機能。統計サイトは調べ物のために来る場所だが、遊びは繰り返し来る理由になる。大手と直接競合しない。
4. **日本のクラン運営向け**: 非アクティブなメンバーの警告のような、リーダーが毎日使う管理機能。一度使い始めた人が定着しやすい。

### 公開に向けた優先順位

1. **独自ドメインとHTTPS**: 今のURL(`http://132.226.7.203:8080`)は、ブラウザに「保護されていない通信」と表示され、検索エンジンにもまず載らない。競合と比べる以前の前提条件。
2. **VMの乗り換え**(上の「VMの乗り換え」): 利用者が増えると、今のスペックでは持たない。
3. **他に無い機能を1つ**: クイズか、自分向けの分析。

- このサイトの今の一番の価値は、Spring Bootを実際に運用して学べていること。「RoyaleAPIに勝つ」ではなく、「日本語でクラロワをする人が、ある目的のためにはこちらを使う」状態を目標にする。

## 機能面の改善候補(2026-09-19に洗い出し、未着手)

上の「サービスの方向性」に沿って洗い出した。公式APIの未使用データは、2026-09-19に実データで取得できることを確認済み(⚠は着手時に要裏付け)。

### 公式APIで取れるのに使っていないデータ(2026-09-19に確認)

| データ | 取得元 | 確認結果 |
|---|---|---|
| 所持カード全123枚のレベル・枚数 | `/players/{tag}`の`cards` | 取得できた(`level`・`maxLevel`・`count`・`elixirCost`を含む) |
| ゲーム内で今セットしているデッキ | `/players/{tag}`の`currentDeck`・`currentDeckSupportCards` | 取得できた |
| 連勝・連敗数、バッジ、実績、ランク戦のシーズン成績など | `/players/{tag}` | 取得できた(`currentWinLoseStreak`・`badges`・`achievements`・`currentPathOfLegendSeasonResult`など) |
| クラン対戦の参加状況 | `/clans/{tag}/currentriverrace` | 日本1位のクランで54人分取得できた(`decksUsed`・`decksUsedToday`・`fame`など)。クラン対戦に参加していないクランは404(`notFound`)になる |
| 過去のクラン対戦 | `/clans/{tag}/riverracelog` | 200が返ることは確認。試したクランは0件だったので、中身は未確認 |
| 宝箱の出現順 | `/players/{tag}/upcomingchests` | 取得できた(`Gold Crate`・`Magical Chest`など) |

### A. すぐできて効果が大きいもの(小規模)

1. **デッキをゲームにコピーするボタン**(使用中のデッキ・対戦詳細)。強いプレイヤーのデッキをそのまま使えるようにする。⚠公式のデッキ共有リンクの正確な形式は未確認。
2. **デッキの平均エリクサーコスト**(同上)。カード詳細用にキャッシュしている`/cards`から計算でき、APIの呼び出しは増えない。
3. **自分のタグを覚えておく**(トップページに「自分の戦績」への近道)。一番よく見られるのは自分の戦績なので、再訪の動機になる。Cookieか`localStorage`で実現でき、DBは不要。

### B. 方向性の「勝ち筋」に直結するもの(中規模)

4. **クラン対戦の参加状況**(クラン情報画面)。勝ち筋4「日本のクラン運営向け」。「今日まだ攻撃していないメンバー」を一覧にする。既存の「最終アクセス7日以上前の警告」とあわせて、リーダーが毎日使う画面になる。`riverracelog`で「毎週貢献の少ないメンバー」も出せる見込み。**公開に向けた優先順位の「他に無い機能を1つ」の第一候補**。
5. **使用中のデッキを実データ(`currentDeck`)に置き換える**(既存機能の改善)。今は直近の1vs1の対戦から推定しているため、対戦していないと出せない。
6. **カードの育成状況**(プレイヤー情報画面)。勝ち筋2「自分のための分析」。使用中のデッキでレベルが低いカードや、あと何枚でレベルアップできるかを出す。⚠レベルアップに必要な枚数の表は公式情報で裏付けが必要。レベルはAPIの値をそのまま出さず、`CardLevel`でゲーム内表記に直すこと。

### C. 日本語ユーザー向け・再訪向け

7. **カード一覧画面(日英名の対照つき)**。攻略情報は英語名が多いため、英語名から日本語名を引ける一覧は実用的。カード詳細画面への入口にもなる。
8. **クイズ(フェーズ2)の最初の一歩**。`/cards`の画像・レアリティ・エリクサーだけで、「画像からカード名を当てる」「エリクサーコストを当てる」などが作れる。DBなしで始められる。

### 進め方の案

- まずA-1〜3で既存画面の使い勝手を上げ、次にB-4を1つ目の「他に無い機能」にする。
- 独自ドメイン・HTTPS化とOGP設定(「ページビュー向上に関するTODO」)は機能ではないが公開の前提条件なので、機能追加とは別に進める。

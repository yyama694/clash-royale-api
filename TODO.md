# TODO

対応済みの項目・経緯は`進捗ログ.md`を参照。ここには未着手・保留中の項目のみ残す。

## 運用・堅牢性

- **可観測性が無い**(2026-09-17、ユーザー判断で見送り。優先度は高め)。Actuator未導入、ヘルスチェック・死活監視なし。2026-09-15にVM無応答が2回発生している。**2026-09-20にも実際にダウンが発生**(AIクローラーの高頻度クロールで`PlayerSightingLog`の書き込みロックが詰まりTomcatのスレッドプールが枯渇、サイト全体が502になった。手動でVMに入って気づき再起動で復旧。根本対策は`進捗ログ.md`フェーズ1.49参照)。死活監視(例: 外形監視で定期的にトップページを叩き、応答なし・異常ステータスで通知)が無いと、次回同様の障害はユーザーが偶然気づくまで気づけない
  - **対応方針決定(2026-09-20)**: UptimeRobotをメール通知で導入する(SMS通知は有料プランが必要なため、まずはメールのみで様子見)。ユーザー自身のアカウント登録が必要なため未着手。手順: (1) https://uptimerobot.com/ でアカウント作成(`yyama694@gmail.com`想定) (2) HTTP(s)モニター追加、URL`https://clashroyale-api.duckdns.org/`、間隔5分(無料枠) (3) Alert Contactsにメール通知先を割り当て (4) テスト通知で疎通確認
  - あわせて、AIクローラー(GPTBot・ClaudeBot等)の`/player/{tag}`への高頻度クロールを`robots.txt`で抑制する案も未着手(フェーズ1.49の保留事項)
- **ボット・スキャナー対策が必要**(2026-09-20、アクセス状況精査中に判明)。Chromeを偽装した巡回ボット(`158.69.55.82`・`158.69.117.45`。全ページを機械的に巡回、現状のUser-Agentベースのbot除外ロジックをすり抜ける)や、SSRF・脆弱性を狙うスキャナー(`204.76.203.31`。`/.git/config`・`/eureka/apps`・クラウドのメタデータエンドポイント`169.254.169.254`等への到達を試行)が本番に来ている。実アクセス統計を歪めるだけでなく、フェーズ1.49のダウンの引き金にもなった。対応案は未検討(例: fail2ban等でのIP遮断、Apache側でのレート制限、WAF導入)。上記のAIクローラー向けrobots.txt対応とは別軸の課題
- **ログのローテーション設定が無い**
- **JVMのメモリ上限指定(`-Xmx`等)が無い**。VMフリーズ対策として検討の余地あり(今のところOOMの痕跡は無い)
## パフォーマンス(2026-09-17レビュー由来)

1. **トップページの表示速度が公式APIの応答に引きずられる**(2026-09-17、ユーザー判断で見送り)。個人ランキング取得で公式APIを最大3回順番に呼ぶため、キャッシュが空だと1.4秒、API障害時は最大24秒待つ。対応案: 並列化/短いタイムアウト/ネガティブキャッシュ/定期ジョブでの先取得。
2. **キャッシュが切れた瞬間に同じAPIを同時に何度も呼ぶ**(2026-09-17、ユーザー判断で見送り)。全訪問者が同じキー(`global`等)を使う共有キャッシュは`@Cacheable(sync = true)`にする。
3. **国一覧のキャッシュが2分で切れる**。内容はほぼ変化しないため、キャッシュごとに有効期間を分けて長くする(例: 1日)。
4. **`LocationService#byCountryCode`が本番コードから未使用**。テストからしか呼ばれていない。同じ処理が`CountryPreference#find`にもあるため、どちらかに寄せる。

## 画面デザイン(2026-09-16〜17洗い出し分の残り)

- **モバイルの表カード化で表のセマンティクスが失われる**/**クランメンバー一覧が1人5行で長い**(未対応)
- **アクセシビリティ**(未対応のまま): 装飾用絵文字(⭐🏆🥇✅❌👑🛡️👥)に`aria-hidden`が無い/フォーカスリングが要素ごとに不統一/ランキングのタブが矢印キーで移動できない/タブ切り替えスクリプトが`document`全体の`.tabs [role="tab"]`を対象にしており、1画面に複数のタブ群を置くと干渉する(現状は実害なし)

## jarのサイズ(2026-09-18起票、未着手)

多言語化でICU4Jを入れたため jar が24.8MB→40.0MBに増えた。ICU4J本体は約15MBで、大半は全言語分のロケールデータ。

- 影響: デプロイのたびのscp転送時間、起動時間、メモリ
- 対応案(未決定): (1)現状維持 (2)ICU4Jを外しJava標準に置き換え(`IcuMessageSource`の複数形/`CountryNames`のCollator・短縮表記/`CountryPreference`の`ULocale`、計3〜4箇所の代替とテスト書き換えが必要)
- 2026-09-20のVM乗り換え(1 OCPU・約5.5GBメモリに増強)で転送・メモリ面の切迫度は下がったため、優先度は低い

## VMの乗り換え後の残作業(2026-09-20、`進捗ログ.md`フェーズ1.40)

新VM(`161.33.136.175`、VM.Standard.A1.Flex/ARM、Oracle Linux 9.8)への切り替えが完了。2026-09-20に固定IP(予約済みパブリックIP)を取得し、一時IP`158.179.182.210`から`161.33.136.175`に変わった。残っているのは以下。

- **旧VM(`132.226.7.203`)の削除**(2026-09-20時点でユーザー判断により翌日以降に先送り。理由: 移行漏れが無いか様子を見るため。OCIインフラ削除のため実施前に要確認)
- [ ] APIキーの作り直し(2026-09-20に固定IP取得に伴い実施。新キーの許可リストは本番新IP・ローカル開発機の2つ。旧VMはもう使わないため含めていない)・本番/ローカル双方への反映
- DNS/リンク先の切り替え検討(現状は本番URLをIP直打ちで案内しているため、独自ドメイン導入時にあわせて見直す)

## プレイヤー名検索の残タスク

- **巡回間隔を詰める**: 2026-09-19に10s→5s、2026-09-20に5s→3sにした(`/etc/clash-royale-api/env`の`CRAWLER_INTERVAL`)。429(`crawler paused`)が増えていないか、画面の応答が遅くならないかを様子見中
- **バックアップの検討**: データが消えると集め直しに時間がかかる。無料枠のObject Storage(20GB)に`by-tag/`を置けるか

## サービスの方向性(2026-09-19確定、参考情報)

全世界のクラロワユーザーに向けて発信する方針。RoyaleAPIとはデータ量・基盤の規模で正面から戦わない。

**勝ち筋**: 1) どの言語でも使いやすいこと 2) 自分のための分析 3) 遊べるコンテンツ(クイズ) 4) クラン運営向け機能

**公開に向けた優先順位**: 1) ~~独自ドメイン+HTTPS~~(2026-09-20完了) 2) ~~VMの乗り換え~~(2026-09-20完了) 3) 他に無い機能を1つ(クイズか自分向け分析)

**今後の対応言語の候補**(2026-09-19の巡回データ集計より): アラビア語(3.0%、右から左に書く言語のためdir="rtl"対応が別途必要)、トルコ語(2.3%)、韓国語(1.9%)。ペルシャ語(2.9%)はゲーム自体が対応言語か要確認。訪問者のAccept-Language集計(需要の直接測定)は未実施。

## ページビュー向上に関するTODO(2026-09-14記録、未着手)

着手時期は未定。土台整備を先に済ませてから集客施策に進む想定。

**土台**
1. ~~独自ドメイン取得 + HTTPS化~~(2026-09-20完了)
2. ~~OGP/メタタグ設定~~(2026-09-20完了。og:title/description/image・twitter:card。画像はユーザー提供のキャラクター画像を630x630のJPEGに変換)
   - 付随して`sitemap.xml`(全カード詳細ページ含む)・`robots.txt`を新設。Search Console登録はまだ(ユーザーのGoogleアカウントでの作業が必要、次回セッションで着手)
   - **副産物で見つけた既存バグを修正**: Apacheのリバースプロキシ設定(HTTPS用vhost)で`X-Forwarded-Proto`が`"http"`のまま(HTTP用設定のコピー漏れ)だったため、hreflang・OGP・sitemapの絶対URLが常に`http://`になっていた。Apache設定修正+Spring側に`server.forward-headers-strategy: framework`を追加して解消
3. 低スペックVMでのアクセス急増耐性の確認

**集客・再訪施策**
4. 差別化ポイント(自分向け分析・公式表記のカード名・スマホでの見やすさ)を訴求するサイト説明文・タイトルの調整
5. 各言語圏のクラロワコミュニティへの直接告知(宣伝ルールを事前確認)
6. 再訪動機になるコンテンツ(クイズ機能など、下記機能面の改善候補と紐付け)
7. アクセス解析の導入(施策効果測定のため、本格的な集客施策より先に仕込む)
8. **Google Search Console登録**(2026-09-20起票、次のステップ)。ユーザーのGoogleアカウントでプロパティ登録が必要。HTML確認ファイル方式(`/googleXXXX.html`を静的リソースに置く)を想定。登録後にsitemap.xmlを送信する

## 機能面の改善候補(2026-09-19洗い出し、残り)

- **B-4 クラン対戦の参加状況**(クラン情報画面、勝ち筋4)。「今日まだ攻撃していないメンバー」一覧。`riverracelog`で「毎週貢献の少ないメンバー」も出せる見込み。**公開優先順位「他に無い機能を1つ」の第一候補**
- **B-6 カードの育成状況**(プレイヤー情報画面、勝ち筋2)。使用中デッキでレベルが低いカード、あと何枚でレベルアップできるかを表示。⚠レベルアップ必要枚数は公式情報で裏付けが必要。レベル表記は`CardLevel`でゲーム内表記に直すこと
- **D-10 実績・バッジの見える化**(プレイヤー情報画面、勝ち筋2)。`achievements`・`badges`は取得済みだが未使用
- **D-11 プレイヤー詳細のSNS共有**(ページビュー向上)。①静的OGP(`og:title`/`og:description`/`og:image`)は2026-09-20完了。②動的シェア画像生成(Player Card)は方針検討済み・未着手。上記「ChatGPTによるサイト評価・改善案」の⑤に相当
  - **方針(2026-09-20検討)**: 生成方式はJava 2D Graphics API(`BufferedImage`/`Graphics2D`/`ImageIO.write`、ブラウザエンジン不使用・低スペックVM向け)。第一段階の表示内容は名前・タグ・トロフィー・ランク戦順位・連勝連敗のみ(現在デッキの画像合成・実績/バッジ(D-10)は第二段階以降)。提供方式は`/player/{tag}`配下に画像エンドポイントを追加し、リクエストの都度その場で生成(ファイル保存なし、Caffeineで短時間キャッシュ)、DBなし方針に沿う
  - **事前調査メモ(着手時に再調査不要)**: プレイヤーデータは`web/PlayerController.java`→`service/PlayerService.java`→`client/dto/PlayerResponse.java`で取得済み(トロフィー=`trophies`/`bestTrophies`、ランク戦順位=`currentPathOfLegendSeasonResult`、連勝連敗=`currentWinLoseStreak`を`domain/WinLoseStreak.of(...)`で変換)、新規API呼び出し不要。本プロジェクトに画像バイナリ返却(`ResponseEntity<byte[]>`/`MediaType.IMAGE_PNG`)・Controller層テスト(`@WebMvcTest`等)の前例は無く、いずれも新規実装になる。多言語ラベルは`web/LabelResolver.java`の`message(key, locale, args...)`を再利用可能。画像処理ライブラリは`pom.xml`に無し(Java標準APIのみで実装するか要判断)。画像合成ロジックは`service`層に新規クラス切り出し+Mockito単体テストの方針が既存の慣習に沿う
- **D-13/C-8 クイズの最初の一歩**(勝ち筋3)。`/cards`のデータだけで「画像からカード名を当てる」「エリクサーコストを当てる」を作る。DB不要、既存データの使い回し
- **D-14 クラロワ急上昇YouTube動画の表示**(保留・優先度低)。勝ち筋のどれにも直接当てはまらない汎用コンテンツで、YouTube Data APIの無料枠クォータも少ないため。他の案の着想用に案として残す

**進め方の案**: まずA-1〜3(対応済み)で上げた使い勝手の続きとしてB-4を1つ目の「他に無い機能」にする。独自ドメイン・HTTPS化・OGP設定は機能追加と並行して進める。

**実装優先順位案(2026-09-20、着手はまだせずメモのみ)**: 上記の機能候補とChatGPT改善案を合わせて検討した順位案。**ユーザーはこの順位案自体の採用可否をまだ決めていない**(メモとして残すのみで、着手・承認はしていない)。次にこの話題に触れたときは、まず採用するかどうかから確認すること。

1. ~~D-11①静的OGP~~(2026-09-20完了。詳細は上記「ページビュー向上に関するTODO」参照)
2. B-4 クラン対戦の参加状況(既存の「公開優先順位」で決めていた第一候補)
3. ChatGPT改善案①プレイヤー情報画面のゲームUI風カード化(表と文章中心という総評の弱点に対応、DB不要)
4. D-11②SNS共有用Player Card画像生成(③で作るUIカードをそのまま画像化に転用、DB不要)
5. ChatGPT改善案④プレイヤー比較(既存API呼び出しの組み合わせ、DB不要)
6. D-13/C-8 クイズの最初の一歩(DB不要)
7. B-6 カードの育成状況 / D-10 実績・バッジの見える化(地味だが低コスト)
8. **(判断待ち)DB導入の検討**: ChatGPT改善案②戦績グラフ(トロフィー推移)・③デッキ勝率などの統計は、公式APIが時系列データ・全履歴を返さないため実現には自前のデータ蓄積(DB導入)が前提になる。現状の「DBなし」方針(`CLAUDE.md`の技術スタック参照)と衝突するため、着手するならDB導入自体を先に判断する必要がある
9. D-14 YouTube表示(保留のまま)

## 収益化(広告表示)

2026-09-20起票。作戦メモは`収益化（検討中）.md`を参照(**検討メモのみ、着手・承認はしていない**)。要点だけ再掲。

- 今すぐ広告を貼る段階ではない(実PV約18,000/月で収益は月数百〜数千円規模、AdSenseの最低支払額$100に届きにくい)。**判断の目安は実PV 10万/月**
- ただし前提整備は今から着手可: **プライバシーポリシー・Cookieポリシーのページが無い**(Cookie使用中なので広告と無関係に必要)、問い合わせ先の明示
- **ドメイン名に`clashroyale`を含む点がSupercellのファンコンテンツポリシー上のリスク**(商標を含むドメイン名・SNSアカウント名は書面合意が無い限り不可)。独自ドメインを取るならゲーム名を含まない名前にする
- 広告・寄付・コーチングによる収益化自体はポリシー上明示的に許容されている。非公式表示(`footer.unofficial`)は対応済み
- 寄付ボタンは独自ドメイン取得(実費発生)の後が望ましい。現状はサーバー代が実質ゼロで名目が弱く、規模的にも入金は見込み薄(2026-09-20に位置づけを見直し)
- **当面やることは「プライバシーポリシーのページ新設」と「PVを伸ばす」の2つだけ。収益化の手段選びはPVが育ってから**(2026-09-20、ユーザーは判断を保留)

## ChatGPTによるサイト評価・改善案(2026-09-20、外部所感のメモ)

ユーザーがChatGPTにサイトを見せた/巡回させた感想をもとにした改善案。良かった点(戦績の見やすさ、現在デッキの分析、対戦詳細でのデッキ比較、多言語対応、データの制約を明示している点など)は既存実装の裏付けとして参考。以下は指摘された改善余地。ChatGPT側の一意見であり、実施是非・優先度は要検討。

- **UI/UXが情報量に対して見劣りする**という指摘が総評の中心。「バックエンド・データ設計 ≫ UIデザイン」になっている、プレイヤー情報画面が文章と表中心、との評価
- ChatGPTが提案する優先順位: ①プレイヤー情報画面のゲームUI風カード化 → ②戦績のグラフ化(トロフィー推移など) → ③デッキ分析の強化(勝率・使用回数・平均タワーダメージ・平均試合時間・相性の悪いデッキ) → ④プレイヤー比較(2人のプレイヤーを並べて比較、URL共有想定) → ⑤SNS共有用Player Card画像生成
  - ③はTODO既存の「B-6 カードの育成状況」とは別観点(デッキ単位の分析)
  - ⑤は既存の「D-11 プレイヤー詳細のSNS共有」で動的シェア画像生成として既に構想済み(順序は揃っている)
- **カード詳細画面の強化**: カードごとの使用率・勝率・直近の使用デッキ・相性の良い/悪いカード・使用プレイヤーランキングを出すと「分析サイト」感が増すとの案(データ源の裏付けが必要、要検討)
- **favicon作り込みの提案**は2026-09-20に対応済み(タワープリンセス風→ユーザー提供キャラクターアイコンに変更、`進捗ログ.md`参照)

## ライバルサイトとの比較による追加機能案(2026-09-20、Web調査メモ)

ユーザー依頼「ライバルサイトと比較した、追加機能案を提示してメモしておいて」を受け、主要な競合サイトの機能をWeb検索で調べたメモ。上記の「ChatGPTによるサイト評価・改善案」とは別に、実際の競合の機能一覧から拾った案。**着手はまだせず、実施是非・優先度は次にこの話題に触れたときに確認すること**(既存の「実装優先順位案」への組み込み可否も未定)。

**調査した競合の主な機能**
- [RoyaleAPI](https://royaleapi.com/): デッキ統計(使用率・勝率・レーティング、7日/30日/全期間・1v1/2v2/チャレンジでフィルタ、週間数百万試合ベース)、カードの育成に必要な残り枚数、クランのRiver Race分析(参加率・累計貢献度・累計デッキ使用数の週ごとの推移)、クランファミリー機能
- [StatsRoyale](https://statsroyale.com/): デッキビルダー・ゲームへのデッキコピー、他プレイヤーの直近対戦履歴の閲覧、クラン検索の高度フィルタ、CRLプロリーグの追跡
- [DeckShop.pro](https://www.deckshop.pro/): 手持ちカードのレベルに応じたデッキ提案(ラダー/クランウォー/チャレンジ/2v2のモード別)、シナジー分析
- [RoyaleTracker](https://royaletracker.gg/guides/best-free-clash-royale-tools)のDeck Rater: デッキの勝ち筋・空対策・エリクサーカーブ・シナジーをスコア化し、実際のラダー勝率と比較して弱点を指摘

いずれも「数百万試合/週」規模のデータ収集を前提にしたデッキ統計・メタ分析が中心。ここは既存の「サービスの方向性」節のとおり正面から張り合わない前提で、以下は当サイトの勝ち筋(多言語・自分向け分析・クラン運営向け機能)に寄る案を優先して拾った。

**追加機能案(新規、既存TODO未記載分)**

1. **クラン検索の高度フィルタ**: 現状はタグ・クラン名の一致検索のみ。公式APIのクラン検索は`locationId`(国・地域)・`minMembers`/`maxMembers`(メンバー数範囲)・`minScore`(最低クランスコア)にも対応している([APIドキュメント](https://docs.royaleapi.com/endpoints/clan_search.html)で確認)。「入れるクランを探したい」需要に応えられ、既存のクラン名検索の延長でDB不要・実装コストも低め。StatsRoyaleの「クラン検索の高度フィルタ」相当
2. **デッキ診断(簡易レーティング)**: プレイヤー情報画面の使用中のデッキ・対戦詳細画面のデッキに、「勝ち筋カードの有無」「空対策カードの有無」等をルールベースで判定した一言診断を添える案。RoyaleTrackerのDeck Raterに近いが、当サイトは実戦データに基づく勝率比較はできないため、あくまでカード情報だけで判定できる簡易版に留める。既存のデッキ表示(平均エリクサー・4枚サイクル・平均カードレベル)への追加なので実装コストは小さめ。DB不要
3. **クラン対戦(リバーレース)参加状況の拡張**: 既存の「B-4 クラン対戦の参加状況」を、RoyaleAPIの「参加率・累計貢献度・週ごとの推移」のように複数週にわたる集計まで広げる案。`riverracelog`エンドポイントは過去分(直近10週分程度)を返すため、DB不要で実現できる見込み(要API仕様確認)。B-4に着手する際にあわせて検討する
4. **手持ちカードから組めるデッキの提案(簡易デッキビルダー)**: DeckShop.proのような、プレイヤーの所持カード・レベルをもとに組めるデッキを提案する機能。ただし「相性の良い組み合わせ」を判定するロジック・データの整備が必要で実装コストは大きめ。着想のみ記録、優先度は他の案より低い

**当サイトの既存の強みの裏付け(新規提案ではなく参考情報)**
- 8言語対応は競合に対する明確な差別化点(調査した競合サイトは基本的に英語中心)。既存の「勝ち筋1」の裏付けとして継続
- 大手サイトは広告収益モデルで画面が情報過多になりがちな傾向があるとみられる(個別サイトの具体的な苦情事例は今回のWeb検索では確認できず、一般論の域)。当サイトのシンプルさは差別化点として維持する方向
